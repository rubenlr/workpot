msrv = "1.96"

disallowed-methods = [
  { path = "std::result::Result::unwrap", reason = "use ? or explicit error handling in core" },
  { path = "std::result::Result::expect", reason = "use ? or explicit error handling in core" },
]

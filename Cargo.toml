[workspace]
members = ["crates/workpot-core", "crates/workpot-cli"]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.85"
version = "0.1.0"
license = "MIT"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"

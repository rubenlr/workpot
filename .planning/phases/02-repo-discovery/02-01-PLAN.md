---
phase: 02-repo-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/workpot-core/Cargo.toml
  - crates/workpot-core/src/domain/repo.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/infra/git.rs
  - crates/workpot-core/src/infra/mod.rs
  - crates/workpot-core/src/infra/migrations.rs
  - crates/workpot-core/src/infra/migrations/002_discovery.sql
  - crates/workpot-core/tests/bootstrap_test.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-core/tests/index_test.rs
autonomous: false
requirements:
  - INDEX-04
user_setup: []
must_haves:
  truths:
    - "Schema v2 applies with git_common_dir column and index_runs/index_changes tables"
    - "git rev-parse --git-common-dir resolves to canonical absolute path via infra/git.rs (D-05)"
  artifacts:
    - path: crates/workpot-core/src/infra/migrations/002_discovery.sql
      provides: git_common_dir column + index_runs/index_changes tables
      contains: "git_common_dir"
    - path: crates/workpot-core/src/infra/git.rs
      provides: resolve_git_common_dir
      contains: "git-common-dir"
  key_links:
    - from: crates/workpot-core/src/infra/git.rs
      to: git CLI
      via: fixed argv -C path rev-parse --git-common-dir
      pattern: "rev-parse.*git-common-dir"
---

## Phase Goal

**As a** macOS developer juggling many git repos, **I want** Workpot to discover repositories under my watch roots and rescan from the CLI, **so that** I do not manually register every nested project.

<objective>
Apply migration `002_discovery.sql`, lock walkdir/globset deps, add `infra/git.rs` for `git_common_dir` resolution, and land failing discovery/index integration tests (RED) for 02-02 to implement.

Purpose: Schema + git identity foundation without overloading one plan with discovery CLI.
Output: migration v2, git helper, RepoRecord.git_common_dir, Wave 0 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-VALIDATION.md

<interfaces>
From crates/workpot-core/src/domain/repo.rs:
- RepoRecord { path, name, registered_at, source } — extend with git_common_dir: String

From crates/workpot-core/src/infra/migrations/001_init.sql:
- repos(path PK, name, registered_at, source manual|scan, excluded)

From 02-RESEARCH.md Open Questions (RESOLVED):
- Column name: git_common_dir only (no repo_git_id)
- Per-path rev-parse failure: callers skip+log+skipped (implemented in 02-02/02-05 index)
- Backfill empty git_common_dir: task in 02-05
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify walkdir and globset on crates.io before lock</name>
  <files>crates/workpot-core/Cargo.toml</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Package Legitimacy Audit table)</read_first>
  <action>Human reviews walkdir 2.5.0 and globset 0.4.18 on crates.io before executor adds them to Cargo.toml (T-02-SC). Open https://crates.io/crates/walkdir and https://crates.io/crates/globset; confirm publisher BurntSushi, repo links, and versions match RESEARCH audit; reject typosquats.</action>
  <acceptance_criteria>Human confirms both packages are legitimate; executor may add them to workpot-core/Cargo.toml in Task 2.</acceptance_criteria>
  <verify>
    <human-check>User types "approved" after crates.io review</human-check>
  </verify>
  <done>Human approved walkdir and globset; Task 2 may lock dependencies</done>
  <what-built>Pre-install legitimacy review for walkdir 2.5.0 and globset 0.4.18</what-built>
  <how-to-verify>
    1. Open https://crates.io/crates/walkdir and https://crates.io/crates/globset
    2. Confirm publisher, download counts, and repo links match RESEARCH audit
    3. Confirm versions 2.5.0 and 0.4.18
  </how-to-verify>
  <resume-signal>Type "approved" to continue or describe blockers</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 002, git helper, deps, and failing discovery/index tests</name>
  <files>crates/workpot-core/Cargo.toml, crates/workpot-core/src/infra/migrations/002_discovery.sql, crates/workpot-core/src/infra/migrations.rs, crates/workpot-core/src/infra/git.rs, crates/workpot-core/src/infra/mod.rs, crates/workpot-core/src/domain/repo.rs, crates/workpot-core/src/error.rs, crates/workpot-core/tests/bootstrap_test.rs, crates/workpot-core/tests/discovery_test.rs, crates/workpot-core/tests/index_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (002_discovery.sql sketch, Pattern 3, Open Questions RESOLVED)</read_first>
  <behavior>
    - discovery_finds_repo_under_root: tempdir watch root with git worktree; DiscoveryService::scan_root returns canonical path (fails until 02-02)
    - discovery_skips_nested_git: parent repo + vendor/nested/.git; nested not in candidates (D-01)
    - index_full_rescan_minimal: one watch root, two repos; run_full adds both; second run idempotent (fails until 02-02)
  </behavior>
  <action>
    Per approved Task 1: add walkdir = "2.5.0" and globset = "0.4.18" to workpot-core/Cargo.toml.

    Create 002_discovery.sql per RESEARCH: ALTER repos ADD COLUMN git_common_dir TEXT NOT NULL DEFAULT ''; CREATE index_runs and index_changes with CHECK constraints; indexes on git_common_dir and run_id. Column name git_common_dir per RESOLVED OQ2 (D-05, D-06).

    Update apply_migrations: second M::up for 002; user_version 2. Update bootstrap_test expected user_version to 2.

    Add infra/git.rs resolve_git_common_dir(path): fixed argv git -C path rev-parse --git-common-dir; canonicalize stdout; return Err(WorkpotError::GitUnavailable) on failure — index callers in 02-02/02-05 map failure to skip+skipped per RESOLVED OQ3, not whole-index abort.

    Extend RepoRecord with git_common_dir: String default empty. Add WorkpotError::GitUnavailable.

    Write failing tests in discovery_test.rs and index_test.rs (tempfile + open_with_paths); reference discovery::scan_root and index::run_full not yet implemented.

    Export git from infra/mod.rs.
  </action>
  <acceptance_criteria>Migration applies user_version 2; discovery/index tests fail with not-found/panic until 02-02 implements services.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core bootstrap_test -- --nocapture 2>&1 | tail -5; cargo test -p workpot-core discovery_finds_repo_under_root 2>&1 | grep -qE 'FAILED|panicked|cannot find|not found'</automated>
  </verify>
  <done>Migration 002, git helper, deps, and RED tests ready for 02-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo path → git subprocess | Canonical path as fixed -C argument (no shell) |
| Paths → SQLite | params! only (Phase 1 pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | git argv injection | mitigate | fixed argv; directory validated before -C |
| T-02-SC | Tampering | cargo deps walkdir/globset | mitigate | blocking human checkpoint Task 1 before Cargo.toml lock |

</threat_model>

<verification>
- Fresh DB user_version 2 after bootstrap
- RED tests exist for discovery_finds_repo_under_root, discovery_skips_nested_git, index_full_rescan_minimal
</verification>

<success_criteria>
- Schema v2 with git_common_dir column name locked
- git helper callable from future discovery/index services
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-01-SUMMARY.md` when done
</output>

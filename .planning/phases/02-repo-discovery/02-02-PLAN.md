---
phase: 02-repo-discovery
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - crates/workpot-core/src/services/discovery.rs
  - crates/workpot-core/src/services/index.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-core/tests/index_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-04
  - INDEX-05
must_haves:
  truths:
    - "User with watch root in config can run workpot index and see discovered repos in workpot repo list"
    - "Nested .git under an indexed repo tree are not discovered (D-01)"
    - "Symlinks are not followed during discovery walks (D-02)"
    - "Each indexed row has canonical absolute git_common_dir (D-05, D-06)"
    - "Per-path git rev-parse failure skips path with skipped count, does not abort whole index (RESOLVED OQ3)"
  artifacts:
    - path: crates/workpot-core/src/services/discovery.rs
      provides: watch-root walk with prune-on-repo
      contains: "skip_current_dir"
    - path: crates/workpot-core/src/services/index.rs
      provides: minimal full scan orchestration
      contains: "run_full"
    - path: crates/workpot-cli/src/main.rs
      provides: workpot index subcommand
      contains: "Index"
  key_links:
    - from: crates/workpot-cli/src/main.rs
      to: services/index.rs
      via: AppContext::run_index
      pattern: "run_index|run_full"
    - from: crates/workpot-core/src/services/index.rs
      to: infra/git.rs
      via: resolve_git_common_dir; Err → skip path
      pattern: "skipped"
---

<objective>
First vertical slice: DiscoveryService walk, minimal IndexService::run_full, and `workpot index` one-line summary (D-17 counts only — history/caps deferred to 02-05).

Purpose: INDEX-04 detection + INDEX-05 CLI rescan after 02-01 schema/git foundation.
Output: discovery + index services, catalog upsert_scan, CLI index, green Wave 1 discovery/index tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-01-SUMMARY.md

<interfaces>
From catalog.rs (reuse):
- is_git_worktree, is_bare_repo — export pub(crate) for discovery (INDEX-04)
- register_manual, list_repos, remove_repo

From 02-01:
- resolve_git_common_dir(path) -> Result<PathBuf>
- RepoRecord.git_common_dir
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discovery walk and catalog upsert_scan</name>
  <files>crates/workpot-core/src/services/discovery.rs, crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/tests/discovery_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 1), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-01, D-02, D-04 partial)</read_first>
  <behavior>
    - discovery_finds_repo_under_root: scan_root returns git repo under watch root
    - discovery_skips_nested_git: nested .git under parent not in candidates (D-01)
    - discovery_skips_plain_dir: directory without .git/bare layout not a candidate (INDEX-04)
  </behavior>
  <action>
    Refactor is_git_worktree and is_bare_repo to pub(crate) in catalog.rs (INDEX-04).

    DiscoveryService::scan_root(root, exclude_set): WalkDir follow_links(false); empty GlobSet OK for this slice; on exclude match skip_current_dir for dirs; when dir is repo root push canonical path and skip_current_dir (D-01, D-02). Plain directories without .git/HEAD or bare layout must never be candidates — discovery_skips_plain_dir fixture: mkdir plain_dir only.

    catalog::upsert_scan(conn, path, git_common_dir): INSERT OR REPLACE; preserve source=manual on conflict (D-14 stub); source=scan for new; parameterized SQL with git_common_dir column.

    Linked worktrees from bare: deferred to 02-05.

    Make discovery_finds_repo_under_root, discovery_skips_nested_git, discovery_skips_plain_dir pass.
  </action>
  <acceptance_criteria>All three discovery_test behaviors pass.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core discovery_finds_repo_under_root discovery_skips_nested_git discovery_skips_plain_dir</automated>
  </verify>
  <done>Discovery walk and upsert_scan; INDEX-04 happy path</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal index merge and workpot index CLI</name>
  <files>crates/workpot-core/src/services/index.rs, crates/workpot-core/src/lib.rs, crates/workpot-core/tests/index_test.rs, crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Open Questions RESOLVED #3), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-13, D-17)</read_first>
  <behavior>
    - index_full_rescan_minimal: two repos under root; first run adds; second idempotent
    - index_skips_on_git_failure: mock or broken path where rev-parse fails; path not inserted; skipped count incremented; index continues (RESOLVED OQ3)
  </behavior>
  <action>
    index::run_full(conn, config) -> IndexSummary { added, removed, skipped }: for each watch_root scan candidates; for each path call resolve_git_common_dir — on Err log warning to stderr (eprintln), increment skipped, do NOT abort whole index (RESOLVED OQ3); on Ok upsert_scan with canonical git_common_dir string (D-05, D-06).

    Minimal stale: delete scan rows under roots not seen this run (simple set diff).

    AppContext::run_index(); CLI Index command; println summary "index: +{added} -{removed} skipped {skipped}" (D-17).

    No cap enforcement or index_runs persistence yet (02-05).

    Add index_skips_on_git_failure test: register non-repo path or use tempdir without git for one candidate if walk allows — prefer fixture where walk finds dir but rev-parse fails.

    Make index_full_rescan_minimal and index_skips_on_git_failure pass.
  </action>
  <acceptance_criteria>Index tests pass; workpot index exits 0 with summary when watch root has git fixture.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core index_full_rescan_minimal index_skips_on_git_failure && cargo test --workspace</automated>
  </verify>
  <done>workpot index discovers repos; per-path git failure skips without abort</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watch root → filesystem walk | User-configured read-only traversal |
| Repo path → git subprocess | Fixed -C argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | discovery walk | mitigate | follow_links(false); caps in 02-05 |
| T-02-02 | Elevation | symlink traversal | mitigate | follow_links(false) |
| T-02-03 | Tampering | SQL via paths | mitigate | params! in catalog/index |

</threat_model>

<verification>
- discovery + index tests green
- Manual: config watch_roots, workpot index, workpot repo list
</verification>

<success_criteria>
- INDEX-04 and INDEX-05 (CLI rescan) satisfied for happy path
- D-01, D-02, D-05, D-06, per-path git skip (OQ3)
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-02-SUMMARY.md` when done
</output>

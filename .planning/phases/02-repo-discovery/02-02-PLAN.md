---
phase: 02-repo-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - crates/workpot-core/src/domain/config.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/src/services/roots.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/services/index.rs
  - crates/workpot-core/tests/roots_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-01
must_haves:
  truths:
    - "User can run workpot roots add|list|remove to manage watch roots without hand-editing config only"
    - "Adding a watch root persists to config.toml and immediately scans that root (D-20)"
    - "Removing a watch root prunes indexed scan repos under it by default (D-21)"
    - "Config limits reject values above hard max (D-22, D-23, D-24)"
  artifacts:
    - path: crates/workpot-core/src/services/roots.rs
      provides: watch root CRUD + prune
      contains: "add_root"
    - path: crates/workpot-core/src/lib.rs
      provides: save_config helper
      contains: "save_config"
    - path: crates/workpot-cli/src/main.rs
      provides: workpot roots subcommands
      contains: "Roots"
  key_links:
    - from: crates/workpot-core/src/services/roots.rs
      to: config.toml
      via: save_config after mutation
      pattern: "save_config"
    - from: crates/workpot-core/src/services/roots.rs
      to: services/index.rs
      via: run_full or partial scan after add
      pattern: "run_full|scan_root"
---

<objective>
Deliver INDEX-01 watch-root management as a user-visible CLI slice: `workpot roots add|list|remove`, config persistence, immediate scan on add (D-20), prune-on-remove (D-21), and limits validation (D-22–D-24).

Purpose: Users configure discovery boundaries without editing TOML alone; reload on next open/index (D-19).
Output: roots service, Config Limits, save_config, roots integration tests, CLI wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config Limits and failing roots integration tests</name>
  <files>crates/workpot-core/src/domain/config.rs, crates/workpot-core/src/error.rs, crates/workpot-core/tests/roots_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-22, D-23, D-24), @.planning/phases/02-repo-discovery/02-RESEARCH.md (Config limits TOML), @.planning/phases/02-repo-discovery/02-PATTERNS.md (roots.rs analog)</read_first>
  <behavior>
    - roots_add_triggers_scan: roots::add_root on tempdir with nested git repo; list_repos contains discovered path after add
    - roots_remove_prunes: add root + index; remove without --skip-prune; repos under root gone from list
    - limits_reject_over_hard_max: load config with max_repos above 20000 returns Config error
  </behavior>
  <action>
    Add Config.limits: Limits { max_watch_roots: u32 default 100, max_repos: u32 default 1000 } with serde defaults under [limits] table per RESEARCH.

    Implement Config::validate(): clamp or error if max_watch_roots &gt; 5000 or max_repos &gt; 20000 (hard max per D-22, D-23); return WorkpotError::LimitsExceeded with clear message (D-24).

    Call validate() from load_config path in lib.rs after toml parse.

    Add WorkpotError::LimitsExceeded, WatchRootNotFound(PathBuf) as needed.

    Write failing roots_test.rs using open_with_paths + isolated config/db; tests call roots API not yet on AppContext.
  </action>
  <acceptance_criteria>roots_test tests fail before roots service exists; valid default limits deserialize from empty config.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_add_triggers_scan 2>&1 | grep -qE 'FAILED|panicked|cannot find'</automated>
  </verify>
  <done>Limits struct and failing roots tests in place</done>
</task>

<task type="auto">
  <name>Task 2: save_config and RootsService implementation</name>
  <files>crates/workpot-core/src/lib.rs, crates/workpot-core/src/services/roots.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/services/index.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-PATTERNS.md (save_config pattern, roots add scan), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-19, D-20, D-21)</read_first>
  <action>
    Extract save_config(config_path, &Config) in lib.rs using toml::to_string_pretty + fs::write; mirror ensure_default_config parent dir creation.

    AppContext: add save_config(&mut self) writing self.config to config_path; add reload_config(&mut self) re-read from disk (D-19).

    Implement roots.rs:
    - add_root(ctx, path): canonicalize; verify is_dir; reject duplicate root; enforce max_watch_roots against config.limits before append; push to watch_roots; save_config; call index::run_full or scan only new root (D-20)
    - list_roots: return watch_roots slice
    - remove_root(ctx, path, skip_prune: bool): remove from watch_roots; save_config; unless skip_prune, DELETE FROM repos WHERE path LIKE root_prefix% AND source='scan' (canonical prefix match — use path_key starts_with guard) (D-21)

    Wire AppContext delegators: roots_add, roots_list, roots_remove.

    Pass config.limits.max_repos into index for future cap (store on IndexSummary only if not enforcing yet).

    Make roots_add_triggers_scan and roots_remove_prunes pass.
  </action>
  <acceptance_criteria>roots_test.rs tests pass; hand-edited config.toml still loads on next open without overwrite (bootstrap_test pattern preserved).</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_add_triggers_scan roots_remove_prunes limits_reject_over_hard_max</automated>
  </verify>
  <done>RootsService and save_config work; roots tests pass</done>
</task>

<task type="auto">
  <name>Task 3: workpot roots CLI subcommands</name>
  <files>crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-PATTERNS.md (main.rs RepoCommands pattern), @.planning/REQUIREMENTS.md (INDEX-01)</read_first>
  <action>
    Add clap Roots subcommand: Add { path }, List, Remove { path, #[arg(long)] skip_prune }.

    Handlers: AppContext::open() as mut where needed; roots_add prints root path; roots_list one line per root; roots_remove prints pruned count or message.

    Map WorkpotError::LimitsExceeded and WatchRootNotFound to stderr + exit code 1.

    Run cargo test --workspace after wiring.
  </action>
  <acceptance_criteria>cargo run -p workpot-cli -- roots list works; roots add on temp fixture triggers discoverable repo in repo list.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_ && cargo test --workspace</automated>
  </verify>
  <done>workpot roots add|list|remove available; INDEX-01 and D-19–D-24 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → watch root path | User-supplied directory must exist and stay within intent |
| Config TOML → limits | Untrusted numeric fields parsed by serde |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | many watch roots | mitigate | max_watch_roots default 100 hard max 5000 (D-22) |
| T-02-06 | Tampering | config limits | mitigate | validate() rejects &gt; hard max (D-23, D-24) |
| T-02-07 | Tampering | prune DELETE | mitigate | parameterized SQL; prefix match on canonical path keys only |

</threat_model>

<verification>
- All roots_test.rs green
- Manual: workpot roots add ~/code; workpot roots list; config.toml contains entry
</verification>

<success_criteria>
- INDEX-01 satisfied via roots CLI + config persistence + scan on add
- D-19, D-20, D-21, D-22, D-23, D-24 implemented
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-02-SUMMARY.md` when done
</output>

---
phase: 02-repo-discovery
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - crates/workpot-core/src/services/discovery.rs
  - crates/workpot-core/src/services/index.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/index_test.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-cli/tests/cli_smoke.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-02
  - INDEX-05
must_haves:
  truths:
    - "Full workpot index merges scan results, validates manual-only repos, removes stale paths (D-15, D-16)"
    - "Manual source preserved when repo also under watch root (D-14)"
    - "Moved repos: stale path removed; rediscover creates new row (D-07)"
    - "Bare repo row plus linked worktree paths indexed (D-03, D-04)"
    - "Repo cap exceeded aborts with exit 1 and no partial durable merge (D-18)"
    - "Index run history and per-scan change log persisted (D-17)"
  artifacts:
    - path: crates/workpot-core/src/services/index.rs
      provides: transactional run_full with history
      contains: "index_runs"
    - path: crates/workpot-core/src/services/discovery.rs
      provides: bare worktree expansion
      contains: "worktree list"
  key_links:
    - from: crates/workpot-core/src/services/index.rs
      to: index_runs / index_changes tables
      via: single transaction
      pattern: "BEGIN|transaction"
    - from: crates/workpot-cli/src/main.rs
      to: IndexCapExceeded
      via: exit code 1
      pattern: "exit(1)|IndexCapExceeded"
---

<objective>
Complete Phase 2 index orchestration: transactional merge with caps (D-18), scan history (D-17), manual validation and source preservation (D-14–D-16), bare/worktree rows (D-03–D-04), stale path handling (D-07), and CLI exit semantics. INDEX-02 reaffirmed; INDEX-05 fully satisfied.

Purpose: Production-grade rescan behavior matching all locked CONTEXT rescan decisions.
Output: full IndexService, expanded discovery, catalog helpers, index/discovery tests, CLI smoke for cap failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-VALIDATION.md
@.planning/phases/02-repo-discovery/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bare and linked worktree discovery (D-03, D-04)</name>
  <files>crates/workpot-core/src/services/discovery.rs, crates/workpot-core/src/infra/git.rs, crates/workpot-core/tests/discovery_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 3 worktree list, D-04), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-03)</read_first>
  <behavior>
    - discovery_includes_bare_and_worktree: git init --bare + git worktree add fixture; scan returns bare path and worktree path sharing git_common_dir
    - discovery_skips_nested_git: still passes (regression)
  </behavior>
  <action>
    Extend infra/git.rs with list_worktree_paths(repo_path) -> Vec<PathBuf> parsing `git -C path worktree list --porcelain` (fixed argv, no shell).

    After registering bare repo candidate, append each worktree path as separate candidate with same git_common_dir resolution (D-04). Sibling worktrees outside tree discovered as separate walk hits (D-03).

    Skip duplicate path keys when canonical paths collide.

    Use real git in tempdir fixtures (git init / worktree) — skip test if git unavailable with #[ignore] only as last resort; prefer real git on macOS CI.

    Make discovery_includes_bare_and_worktree pass.
  </action>
  <acceptance_criteria>discovery_includes_bare_and_worktree passes on machine with git; nested git test still green.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core discovery_includes_bare_and_worktree discovery_skips_nested_git</automated>
  </verify>
  <done>Bare and linked worktree paths discovered per D-03/D-04</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transactional index merge, caps, and history (D-14–D-18, D-07)</name>
  <files>crates/workpot-core/src/services/index.rs, crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/error.rs, crates/workpot-core/tests/index_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 4 transactional cap), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-14–D-18, D-07), @.planning/phases/02-repo-discovery/02-PATTERNS.md (transaction — first use)</read_first>
  <behavior>
    - index_preserves_manual_source: manual add under watch root; run_full; row source still manual (D-14)
    - index_removes_stale_path: register path; delete directory; run_full removes row (D-15, D-07)
    - index_validates_manual_outside_roots: manual path outside roots still listed after index if still git (D-16)
    - index_cap_abort: max_repos=1 with two candidates; run_full returns IndexCapExceeded; repo count unchanged (D-18)
    - index_writes_history: successful run inserts index_runs row and index_changes for added/removed/skipped (D-17)
  </behavior>
  <action>
    Refactor run_full:
    1. Collect all candidates from watch roots (with worktree expansion).
    2. Build set of seen scan paths; compute stale: existing scan rows under any watch root prefix not in seen → mark removed (D-15). Do not delete manual rows for missing paths until validated — if path missing, remove (D-15).
    3. Validate manual-only rows outside roots: if !exists() or !git, remove with change log removed (D-16).
    4. Pre-check: current_repo_count - stale_removals + new_adds &gt; config.limits.max_repos → return Err(IndexCapExceeded { max, would_be }) BEFORE transaction (D-18).
    5. conn.transaction(): INSERT index_runs started; apply upserts (preserve manual source on conflict per D-14); DELETE stale; INSERT index_changes rows; UPDATE index_runs finished counts; COMMIT.
    6. On cap error: record index_runs status cap_exceeded if row started, ROLLBACK, no repo mutations.

    Add WorkpotError::IndexCapExceeded { max, found }.

    catalog: upsert_scan must UPDATE without changing source from manual to scan (D-14).

    Implement index_preserves_manual_source, index_removes_stale_path, index_validates_manual_outside_roots, index_cap_abort, index_writes_history tests.

    Summary line unchanged format; counts from transaction result (D-17).
  </action>
  <acceptance_criteria>All index_test behaviors pass; cap test proves zero net new repos after failed run.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core index_preserves_manual_source index_removes_stale_path index_validates_manual_outside_roots index_cap_abort index_writes_history index_full_rescan</automated>
  </verify>
  <done>Transactional index with history, caps, merge rules; D-14–D-18 and D-07 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: CLI exit codes, INDEX-02 smoke, and phase gate</name>
  <files>crates/workpot-cli/src/main.rs, crates/workpot-cli/tests/cli_smoke.rs</files>
  <read_first>@.planning/ROADMAP.md (Phase 2 success criteria), @.planning/phases/02-repo-discovery/02-VALIDATION.md (manual UAT)</read_first>
  <action>
    Map WorkpotError::IndexCapExceeded in main to eprintln + std::process::exit(1) (D-18).

    Extend cli_smoke.rs: temp HOME, roots add + index + repo list; optional cap test with tiny max_repos in seeded config if feasible.

    Verify INDEX-02: existing repo add/list still works (run catalog_test + smoke).

    Run cargo test --workspace and scripts/check-no-network-deps.sh.

    SUMMARY: document manual UAT steps from 02-VALIDATION.md (watch root nested repos, exclude rescan, index twice without restart).
  </action>
  <acceptance_criteria>Full workspace tests pass; workpot index returns exit 1 on cap exceeded in smoke or integration test.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-no-network-deps.sh && cargo test --offline --workspace</automated>
  </verify>
  <done>Phase 2 gate green; INDEX-02/05 complete; all D-01–D-24 covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Index transaction → SQLite | All-or-nothing repo mutations |
| Cap pre-check → memory | Candidate list bounded by max_repos config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | pathological repo count | mitigate | max_repos default 1000 hard 20000; abort before commit (D-18, D-23) |
| T-02-12 | Tampering | partial index on cap | mitigate | transaction rollback; pre-check (D-18) |
| T-02-13 | Tampering | history tables | mitigate | parameterized INSERT for index_runs/changes |
| T-02-14 | Repudiation | scan audit | mitigate | index_runs + index_changes per D-17 |

</threat_model>

<verification>
- All Phase 2 automated tests in 02-VALIDATION.md green
- ROADMAP success criteria 1–5 manually spot-checked per SUMMARY UAT section
- DATA-02: no new network deps
</verification>

<success_criteria>
- INDEX-02, INDEX-05 complete; all D-01–D-24 covered across phase plans
- Phase 2 goal achievable: auto-discovery, manual add, exclude, .git detection, CLI rescan
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-04-SUMMARY.md` when done
</output>

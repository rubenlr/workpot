---
phase: 02-repo-discovery
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - crates/workpot-core/src/services/discovery.rs
  - crates/workpot-core/src/services/excludes.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/excludes_test.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-03
must_haves:
  truths:
    - "User exclude globs prevent discovery from indexing matching paths (D-08)"
    - "Built-in default exclude globs apply in addition to user config (D-09)"
    - "workpot repo remove deletes row and appends parent/name exclude glob (D-10)"
    - "workpot repo add works when path matches exclude glob (D-11)"
    - "workpot excludes list|remove manages exclude globs (D-12)"
    - "Non-git directories under watch roots are not indexed (INDEX-04 via discovery_skips_plain_dir)"
  artifacts:
    - path: crates/workpot-core/src/services/excludes.rs
      provides: exclude glob CRUD
      contains: "list_excludes"
    - path: crates/workpot-core/src/services/discovery.rs
      provides: GlobSet built-in ∪ user
      contains: "GlobSet"
  key_links:
    - from: crates/workpot-core/src/services/discovery.rs
      to: config.excludes
      via: build_exclude_set
      pattern: "GlobSetBuilder"
---

<objective>
Deliver INDEX-03 exclude control: built-in + user glob pruning, `workpot excludes list|remove`, `workpot repo remove` with persisted exclude glob (D-10), manual add bypasses scan excludes (D-11).

Purpose: Block paths under watch roots without rescan re-adding them.
Output: exclude GlobSet, excludes service, catalog remove+exclude, tests, CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Built-in exclude GlobSet and failing exclude tests</name>
  <files>crates/workpot-core/src/services/discovery.rs, crates/workpot-core/tests/excludes_test.rs, crates/workpot-core/tests/discovery_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 2), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-08, D-09, INDEX-04)</read_first>
  <behavior>
    - exclude_blocks_rescan: user glob **/skip-me/**; discovery does not return path under skip-me
    - discovery_skips_plain_dir: directory without .git not a candidate — reinforce INDEX-04 (fixture: plain_dir only, no .git)
    - excludes_list_roundtrip: add glob via service; list contains it
  </behavior>
  <action>
    discovery::build_exclude_set(config) -> GlobSet: built_in_defaults() per RESEARCH table (**/node_modules/**, **/target/**, **/.Trash/**, **/build/**, **/dist/**, **/.git/modules/**, **/DerivedData/**, **/Library/Caches/**) chained with config.excludes (D-09).

    scan_root uses GlobSet; on is_match for dir entry skip_current_dir (D-08).

    index::run_full builds exclude set once per run.

    **discovery_skips_plain_dir (INDEX-04):** add/extend discovery_test.rs with tempdir containing only plain_dir (no .git, no bare HEAD/objects); assert scan_root candidates do not include plain_dir; only is_git_worktree || is_bare_repo paths qualify.

    Write failing excludes_test.rs for exclude_blocks_rescan and excludes_list_roundtrip.

    Document: globs use `/`; match absolute canonical paths where possible.
  </action>
  <acceptance_criteria>exclude_blocks_rescan fails before wiring; discovery_skips_plain_dir passes or fails RED until wired.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core exclude_blocks_rescan discovery_skips_plain_dir 2>&1 | grep -qE 'test result|FAILED'</automated>
  </verify>
  <done>Built-in + user GlobSet; plain-dir skip test explicit in action</done>
</task>

<task type="auto">
  <name>Task 2: excludes service and workpot excludes CLI</name>
  <files>crates/workpot-core/src/services/excludes.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/lib.rs, crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-12, D-11)</read_first>
  <action>
    excludes.rs: list_excludes, remove_exclude (save_config).

    register_manual does NOT consult exclude GlobSet (D-11) — test manual_add_ignores_exclude_glob.

    CLI Excludes List, Remove { glob }.

    Make exclude_blocks_rescan, excludes_list_roundtrip, manual_add_ignores_exclude_glob pass.
  </action>
  <acceptance_criteria>Exclude tests pass.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core exclude_blocks_rescan excludes_list_roundtrip manual_add_ignores_exclude_glob</automated>
  </verify>
  <done>workpot excludes list|remove; D-11 verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: repo remove appends exclude glob (D-10)</name>
  <files>crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/lib.rs, crates/workpot-cli/src/main.rs, crates/workpot-core/tests/excludes_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-10)</read_first>
  <behavior>
    - remove_appends_exclude: config.excludes contains {parent}/{name}/** after remove
    - remove_then_index_skips: run_full does not re-add removed path
  </behavior>
  <action>
    catalog::remove_repo_with_exclude: DELETE row; append "{parent_canonical}/{dir_name}/**" to config.excludes; save_config (D-10).

    AppContext::remove_repo uses remove_repo_with_exclude.

    Tests remove_appends_exclude, remove_then_index_skips.
  </action>
  <acceptance_criteria>Remove + rescan does not resurrect repo.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core remove_appends_exclude remove_then_index_skips</automated>
  </verify>
  <done>INDEX-03 and D-08–D-12 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User globs → discovery | User-controlled pruning |
| remove → config.excludes | Persisted patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | exclude glob injection | mitigate | globs from canonical paths only |
| T-02-09 | Denial of Service | broad user glob | accept | user intent; document POSIX globs |
| T-02-10 | Tampering | SQL on remove | mitigate | parameterized DELETE |

</threat_model>

<verification>
- INDEX-03 automated tests green
- Manual: repo remove; index does not re-add
</verification>

<success_criteria>
- INDEX-03 and D-08–D-12 complete
- discovery_skips_plain_dir green
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-04-SUMMARY.md` when done
</output>

---
phase: 02-repo-discovery
plan: 05
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified:
  - crates/workpot-core/src/services/discovery.rs
  - crates/workpot-core/src/services/index.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/index_test.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-cli/tests/cli_smoke.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-02
  - INDEX-05
must_haves:
  truths:
    - "Full workpot index merges scan, validates manual-only repos, removes stale paths (D-15, D-16)"
    - "Manual source preserved when repo also under watch root (D-14)"
    - "Moved repos: stale path removed; rediscover creates new row (D-07)"
    - "Bare repo row plus linked worktree paths indexed (D-03, D-04)"
    - "Repo cap exceeded aborts exit 1 with no partial durable merge (D-18)"
    - "Index run history and per-scan change log persisted (D-17)"
    - "Phase 1 rows with empty git_common_dir backfilled on index (RESOLVED OQ1)"
    - "Per-path git rev-parse failure recorded as skipped in change log (RESOLVED OQ3)"
  artifacts:
    - path: crates/workpot-core/src/services/index.rs
      provides: transactional run_full with history and backfill
      contains: "index_runs"
    - path: crates/workpot-core/src/services/discovery.rs
      provides: bare worktree expansion
      contains: "worktree list"
  key_links:
    - from: crates/workpot-core/src/services/index.rs
      to: index_runs / index_changes
      via: single transaction
      pattern: "transaction"
    - from: crates/workpot-core/src/services/index.rs
      to: repos.git_common_dir
      via: backfill empty column before merge
      pattern: "git_common_dir.*''"
---

<objective>
Complete Phase 2 index orchestration: transactional merge with caps (D-18), scan history (D-17), manual validation and source preservation (D-14–D-16), bare/worktree rows (D-03–D-04), stale paths (D-07), backfill Phase 1 git_common_dir (RESOLVED OQ1), skip+log+skipped for per-path git failures in change log (RESOLVED OQ3).

Purpose: Production-grade rescan matching locked CONTEXT decisions.
Output: full IndexService, expanded discovery, tests, CLI smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-VALIDATION.md
@.planning/phases/02-repo-discovery/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bare and linked worktree discovery (D-03, D-04)</name>
  <files>crates/workpot-core/src/services/discovery.rs, crates/workpot-core/src/infra/git.rs, crates/workpot-core/tests/discovery_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 3 worktree list)</read_first>
  <behavior>
    - discovery_includes_bare_and_worktree: bare + worktree add fixture; both paths share git_common_dir
    - discovery_skips_nested_git: regression
  </behavior>
  <action>
    infra/git.rs: list_worktree_paths(repo_path) via git worktree list --porcelain (fixed argv).

    After bare candidate, append linked worktree paths (D-04). Sibling worktrees outside tree as separate walk hits (D-03).

    discovery_includes_bare_and_worktree passes with real git fixtures.
  </action>
  <acceptance_criteria>Bare/worktree test green; nested git regression green.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core discovery_includes_bare_and_worktree discovery_skips_nested_git</automated>
  </verify>
  <done>Bare and linked worktree discovery (D-03, D-04)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill, transactional merge, caps, history, git skip in changelog (D-07, D-14–D-18, OQ1, OQ3)</name>
  <files>crates/workpot-core/src/services/index.rs, crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/error.rs, crates/workpot-core/tests/index_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 4, Open Questions RESOLVED)</read_first>
  <behavior>
    - index_backfills_git_common_dir: seed repo row with git_common_dir ''; run_full; row has non-empty canonical git_common_dir (RESOLVED OQ1)
    - index_preserves_manual_source: manual under watch root stays manual after run_full (D-14)
    - index_removes_stale_path: delete directory; row removed (D-15, D-07)
    - index_validates_manual_outside_roots: manual outside roots kept if still git (D-16)
    - index_cap_abort: max_repos=1, two candidates; IndexCapExceeded; count unchanged (D-18)
    - index_writes_history: index_runs + index_changes for added/removed/skipped (D-17)
    - index_git_failure_writes_skipped: rev-parse failure inserts index_changes action=skipped (RESOLVED OQ3)
  </behavior>
  <action>
    **Backfill (RESOLVED OQ1):** at start of run_full, SELECT repos WHERE git_common_dir = '' OR git_common_dir IS NULL; for each existing path if still on disk and is git, resolve_git_common_dir and UPDATE row; on failure skip with skipped changelog when history enabled.

    Refactor run_full:
    1. Backfill pass (above).
    2. Collect candidates from watch roots (worktree expansion).
    3. Stale scan rows under roots not in seen → removed (D-15). Missing manual paths → remove (D-15).
    4. Validate manual-only outside roots (D-16).
    5. For each candidate: resolve_git_common_dir — on Err eprintln warning, record index_changes skipped, increment skipped, continue (RESOLVED OQ3); on Ok upsert.
    6. Pre-check cap before transaction (D-18).
    7. transaction(): index_runs, upserts (preserve manual source D-14), DELETE stale, index_changes, commit.

    WorkpotError::IndexCapExceeded.

    Tests: index_backfills_git_common_dir, index_preserves_manual_source, index_removes_stale_path, index_validates_manual_outside_roots, index_cap_abort, index_writes_history, index_git_failure_writes_skipped, index_full_rescan.
  </action>
  <acceptance_criteria>All index_test behaviors pass; backfill test uses pre-seeded empty git_common_dir row.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core index_backfills_git_common_dir index_preserves_manual_source index_removes_stale_path index_validates_manual_outside_roots index_cap_abort index_writes_history index_git_failure_writes_skipped index_full_rescan</automated>
  </verify>
  <done>Full index orchestration with backfill, history, caps, git skip in changelog</done>
</task>

<task type="auto">
  <name>Task 3: CLI exit codes, INDEX-02 smoke, phase gate</name>
  <files>crates/workpot-cli/src/main.rs, crates/workpot-cli/tests/cli_smoke.rs</files>
  <read_first>@.planning/ROADMAP.md (Phase 2 success criteria), @.planning/phases/02-repo-discovery/02-VALIDATION.md</read_first>
  <action>
    Map IndexCapExceeded to eprintln + exit(1) (D-18).

    cli_smoke: roots add + index + repo list; optional cap test.

    Verify INDEX-02: catalog_test + smoke.

    cargo test --workspace; scripts/check-no-network-deps.sh.

    SUMMARY: manual UAT from 02-VALIDATION.md.
  </action>
  <acceptance_criteria>Workspace tests pass; cap returns exit 1 in test.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-no-network-deps.sh && cargo test --offline --workspace</automated>
  </verify>
  <done>Phase 2 gate green; INDEX-02/05 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Index transaction → SQLite | All-or-nothing repo mutations |
| Cap pre-check → memory | Bounded candidate list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | pathological repo count | mitigate | max_repos; abort before commit (D-18) |
| T-02-12 | Tampering | partial index on cap | mitigate | rollback; pre-check |
| T-02-13 | Tampering | history tables | mitigate | parameterized INSERT |
| T-02-14 | Repudiation | scan audit | mitigate | index_runs + index_changes (D-17) |

</threat_model>

<verification>
- All Phase 2 automated tests in 02-VALIDATION.md green
- ROADMAP success criteria 1–5 manual spot-check per SUMMARY
</verification>

<success_criteria>
- INDEX-02, INDEX-05 complete; D-01–D-24 covered across phase plans
- Backfill and per-path git skip in change log per RESOLVED research
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-05-SUMMARY.md` when done
</output>

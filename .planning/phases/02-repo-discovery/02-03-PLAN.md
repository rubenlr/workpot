---
phase: 02-repo-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - crates/workpot-core/src/services/discovery.rs
  - crates/workpot-core/src/services/excludes.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/excludes_test.rs
  - crates/workpot-core/tests/discovery_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-03
must_haves:
  truths:
    - "User exclude globs prevent discovery from indexing matching paths (D-08)"
    - "Built-in default exclude globs apply in addition to user config (D-09)"
    - "workpot repo remove deletes the row and appends a parent/name exclude glob so rescan does not re-add (D-10)"
    - "workpot repo add still works when path matches an exclude glob (D-11)"
    - "workpot excludes list|remove manages exclude globs (D-12)"
  artifacts:
    - path: crates/workpot-core/src/services/excludes.rs
      provides: exclude glob CRUD
      contains: "list_excludes"
    - path: crates/workpot-core/src/services/discovery.rs
      provides: GlobSet built from built-in ∪ user excludes
      contains: "GlobSet"
    - path: crates/workpot-core/src/services/catalog.rs
      provides: remove with exclude append
      contains: "remove_repo_with_exclude"
  key_links:
    - from: crates/workpot-core/src/services/discovery.rs
      to: config.excludes
      via: build_exclude_set(config)
      pattern: "GlobSetBuilder"
    - from: crates/workpot-core/src/services/catalog.rs
      to: config.toml
      via: save_config after append glob
      pattern: "excludes"
---

<objective>
Deliver INDEX-03 exclude control end-to-end: built-in + user glob pruning during discovery, `workpot excludes list|remove`, and `workpot repo remove` that persists an exclude glob (D-10) while manual add bypasses scan excludes (D-11).

Purpose: Users can permanently block paths under watch roots without repeated rescans re-adding them.
Output: exclude GlobSet wiring, excludes service, catalog remove+exclude, tests, CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Built-in exclude GlobSet and failing exclude tests</name>
  <files>crates/workpot-core/src/services/discovery.rs, crates/workpot-core/tests/excludes_test.rs, crates/workpot-core/tests/discovery_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 2 built-in table, globset API), @.planning/phases/02-repo-discovery/02-CONTEXT.md (D-08, D-09)</read_first>
  <behavior>
    - exclude_blocks_rescan: user glob **/skip-me/** on temp tree; discovery does not return path under skip-me
    - discovery_skips_plain_dir: directory without .git not a candidate (INDEX-04 reinforcement)
    - excludes_list_roundtrip: add glob via service; list contains it
  </behavior>
  <action>
    Add discovery::build_exclude_set(config) -> GlobSet: built_in_defaults() chained with config.excludes per RESEARCH table (**/node_modules/**, **/target/**, **/.Trash/**, **/build/**, **/dist/**, **/.git/modules/**, **/DerivedData/**, **/Library/Caches/**) (D-09).

    Wire scan_root to accept &GlobSet; on is_match for dir entry skip_current_dir (D-08).

    Update index::run_full to build exclude set once per run.

    Write failing excludes_test.rs and extend discovery_test.rs for exclude_blocks_rescan.

    Document in code comment: patterns use forward slashes; match on absolute canonical paths where possible.
  </action>
  <acceptance_criteria>exclude_blocks_rescan fails before GlobSet wired; built-in set compiles without error.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core exclude_blocks_rescan 2>&1 | grep -qE 'FAILED|panicked|assert'</automated>
  </verify>
  <done>Built-in and user GlobSet wired into discovery walk</done>
</task>

<task type="auto">
  <name>Task 2: excludes service and workpot excludes CLI</name>
  <files>crates/workpot-core/src/services/excludes.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/lib.rs, crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-12), @.planning/phases/02-repo-discovery/02-PATTERNS.md (excludes.rs analog)</read_first>
  <action>
    Implement excludes.rs:
    - list_excludes(config): return built-in labels (optional static names) + user config.excludes clone
    - remove_exclude(ctx, glob): remove exact string from config.excludes if present; save_config; error if missing

    register_manual path: do NOT consult exclude GlobSet (D-11) — verify with test manual_add_ignores_exclude_glob.

    AppContext: excludes_list, excludes_remove.

    CLI: Excludes subcommand List, Remove { glob }; print one glob per line on list.

    Make excludes_list_roundtrip and exclude_blocks_rescan pass.
  </action>
  <acceptance_criteria>cargo test -p workpot-core exclude_blocks_rescan excludes_list_roundtrip manual_add_ignores_exclude_glob.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core exclude_blocks_rescan excludes_list_roundtrip manual_add_ignores_exclude_glob</automated>
  </verify>
  <done>workpot excludes list|remove works; D-11 verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: repo remove appends exclude glob (D-10)</name>
  <files>crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/lib.rs, crates/workpot-cli/src/main.rs, crates/workpot-core/tests/excludes_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-10), @.planning/phases/02-repo-discovery/02-RESEARCH.md (Pattern 5 remove exclude)</read_first>
  <behavior>
    - remove_appends_exclude: remove_repo_with_exclude on registered path; read config.toml; excludes contains {parent}/{name}/** pattern
    - remove_then_index_skips: after remove, run_full does not re-add same path
  </behavior>
  <action>
    Implement catalog::remove_repo_with_exclude(conn, config_path, config, path): canonicalize path; DELETE row; compute glob "{parent_canonical}/{dir_name}/**" per D-10; append to config.excludes if not already present; save_config.

    Change AppContext::remove_repo to call remove_repo_with_exclude (mut self for config).

    Update CLI repo remove handler to use new method; print removed path and note exclude added.

    Add tests remove_appends_exclude and remove_then_index_skips.

    Ensure register_manual unchanged for exclude bypass (D-11).
  </action>
  <acceptance_criteria>remove_appends_exclude and remove_then_index_skips pass; rescan after remove does not resurrect repo.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core remove_appends_exclude remove_then_index_skips</automated>
  </verify>
  <done>repo remove persists exclude glob; INDEX-03 and D-08–D-12 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User globs → discovery | Misconfigured globs can hide legitimate repos (user intent) |
| remove → config.excludes | Persisted patterns affect future scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | exclude glob injection | mitigate | store literal glob strings from canonical paths only; no shell |
| T-02-09 | Denial of Service | overly broad user glob | accept | user-controlled; document POSIX-style patterns (A4) |
| T-02-10 | Tampering | SQL on remove | mitigate | parameterized DELETE; config write via toml serializer |

</threat_model>

<verification>
- INDEX-03 automated tests green
- Manual: workpot repo remove &lt;path&gt;; verify config excludes entry; workpot index does not re-add
</verification>

<success_criteria>
- INDEX-03 and D-08 through D-12 complete
- INDEX-04 plain-dir skip test passes in discovery_test
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-03-SUMMARY.md` when done
</output>

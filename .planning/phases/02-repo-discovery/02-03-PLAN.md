---
phase: 02-repo-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - crates/workpot-core/src/domain/config.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/src/services/roots.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/services/index.rs
  - crates/workpot-core/tests/roots_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - INDEX-01
must_haves:
  truths:
    - "User can run workpot roots add|list|remove without hand-editing config only"
    - "Adding a watch root persists to config.toml and immediately scans that root (D-20)"
    - "Removing a watch root prunes indexed scan repos under it by default (D-21)"
    - "Prune uses canonical path prefix match in Rust, not SQL LIKE (checker fix)"
    - "Config limits reject values above hard max (D-22, D-23, D-24)"
  artifacts:
    - path: crates/workpot-core/src/services/roots.rs
      provides: watch root CRUD + prune
      contains: "add_root"
    - path: crates/workpot-core/src/lib.rs
      provides: save_config helper
      contains: "save_config"
    - path: crates/workpot-cli/src/main.rs
      provides: workpot roots subcommands
      contains: "Roots"
  key_links:
    - from: crates/workpot-core/src/services/roots.rs
      to: repos rows
      via: Rust filter canonical path starts_with root prefix then DELETE
      pattern: "starts_with"
---

<objective>
Deliver INDEX-01 watch-root management: `workpot roots add|list|remove`, config persistence, immediate scan on add (D-20), prune-on-remove (D-21), limits validation (D-22–D-24).

Purpose: Users configure discovery boundaries without editing TOML alone; reload on next open/index (D-19).
Output: roots service, Config Limits, save_config, roots tests, CLI wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-repo-discovery/02-CONTEXT.md
@.planning/phases/02-repo-discovery/02-RESEARCH.md
@.planning/phases/02-repo-discovery/02-PATTERNS.md
@.planning/phases/02-repo-discovery/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config Limits and failing roots integration tests</name>
  <files>crates/workpot-core/src/domain/config.rs, crates/workpot-core/src/error.rs, crates/workpot-core/tests/roots_test.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-22, D-23, D-24)</read_first>
  <behavior>
    - roots_add_triggers_scan: add_root on tempdir with nested git repo; list_repos contains path
    - roots_remove_prunes: add root + index; remove without --skip-prune; repos under root gone
    - limits_reject_over_hard_max: max_repos above 20000 returns Config error
  </behavior>
  <action>
    Add Config.limits: Limits { max_watch_roots default 100, max_repos default 1000 } under [limits].

    Config::validate(): error if max_watch_roots &gt; 5000 or max_repos &gt; 20000 (D-22, D-23); WorkpotError::LimitsExceeded (D-24). Call from load_config.

    Write failing roots_test.rs.
  </action>
  <acceptance_criteria>roots tests fail before roots service; default limits deserialize.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_add_triggers_scan 2>&1 | grep -qE 'FAILED|panicked|cannot find'</automated>
  </verify>
  <done>Limits struct and failing roots tests</done>
</task>

<task type="auto">
  <name>Task 2: save_config and RootsService implementation</name>
  <files>crates/workpot-core/src/lib.rs, crates/workpot-core/src/services/roots.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/services/index.rs</files>
  <read_first>@.planning/phases/02-repo-discovery/02-CONTEXT.md (D-19, D-20, D-21)</read_first>
  <action>
    save_config(config_path, &Config) via toml::to_string_pretty + fs::write.

    AppContext: save_config, reload_config (D-19).

    roots.rs:
    - add_root: canonicalize; is_dir; duplicate check; max_watch_roots; append; save_config; run_full or scan new root only (D-20)
    - list_roots: return watch_roots
    - remove_root(ctx, path, skip_prune): remove from watch_roots; save_config; unless skip_prune, **prune in Rust**: SELECT path, source FROM repos WHERE source='scan'; filter rows where canonical path starts_with canonical removed root prefix (use same path_key/canonicalize helper as catalog); DELETE each matching path with parameterized SQL by path PK — **do not** use SQL LIKE or path_prefix% in SQL (D-21, checker fix)

    Wire AppContext: roots_add, roots_list, roots_remove.

    Make roots_add_triggers_scan, roots_remove_prunes, limits_reject_over_hard_max pass.
  </action>
  <acceptance_criteria>roots_test.rs green; prune test proves sibling path outside root prefix is not deleted.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_add_triggers_scan roots_remove_prunes limits_reject_over_hard_max</automated>
  </verify>
  <done>RootsService with Rust canonical-prefix prune</done>
</task>

<task type="auto">
  <name>Task 3: workpot roots CLI subcommands</name>
  <files>crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/REQUIREMENTS.md (INDEX-01)</read_first>
  <action>
    clap Roots: Add { path }, List, Remove { path, --skip-prune }.

    Handlers use AppContext roots_*; map LimitsExceeded and WatchRootNotFound to stderr + exit 1.

    cargo test --workspace.
  </action>
  <acceptance_criteria>workpot roots add|list|remove work on fixture.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core roots_ && cargo test --workspace</automated>
  </verify>
  <done>INDEX-01 and D-19–D-24 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → watch root path | Must exist as directory |
| Config TOML → limits | serde parse + validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | many watch roots | mitigate | max_watch_roots 100 / hard 5000 |
| T-02-06 | Tampering | config limits | mitigate | validate() rejects &gt; hard max |
| T-02-07 | Tampering | prune DELETE | mitigate | parameterized DELETE by path PK; prefix filter in Rust only |

</threat_model>

<verification>
- roots_test.rs green
- Manual: workpot roots add; config.toml updated
</verification>

<success_criteria>
- INDEX-01 complete
- D-19–D-24; prune not SQL LIKE
</success_criteria>

<output>
Create `.planning/phases/02-repo-discovery/02-03-SUMMARY.md` when done
</output>

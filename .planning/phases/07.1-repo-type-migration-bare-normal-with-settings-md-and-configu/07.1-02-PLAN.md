---
phase: 07.1
plan: 02
type: tdd
wave: 1
depends_on: ["07.1-01"]
files_modified:
  - crates/workpot-core/src/domain/config.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/infra/git.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/services/repo_convert.rs
  - crates/workpot-core/tests/repo_convert_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "MigrationConfig struct exists in config.rs with all five fields and serde defaults (per D-01, D-02, D-04, D-17, D-18)"
    - "ProjectNameSource enum exists with FolderName and Alias variants (per D-01)"
    - "Config struct has a migration: MigrationConfig field with #[serde(default)]"
    - "Config::validate() rejects empty temp_suffix, bare_repo_template missing {project}, worktree_template missing {project} or {worktree}"
    - "WorkpotError has ConversionPreflight(String) and ConversionFailed(String) variants"
    - "PreflightResult enum exported from repo_convert.rs with Ready, DirtyWorktree, NoUpstream, UnpushedCommits, HasStash variants"
    - "run_preflight(&path) returns DirtyWorktree when repo has uncommitted changes (D-05)"
    - "run_preflight(&path) returns NoUpstream when a branch has no upstream (D-06)"
    - "run_preflight(&path) returns UnpushedCommits when a branch is ahead of upstream (D-06)"
    - "run_preflight(&path) returns HasStash when any stash exists (D-07)"
    - "run_preflight(&path) returns Ready when repo is clean, all branches pushed, no stash"
    - "sanitize_worktree maps / to . in branch names (per D-04)"
    - "unique_worktree_name appends 6-char hash suffix on collision (per D-04)"
    - "resolve_template substitutes {project} and {worktree} placeholders (per D-02)"
  artifacts:
    - path: "crates/workpot-core/src/domain/config.rs"
      provides: "MigrationConfig struct, ProjectNameSource enum, Config.migration field"
      contains: "pub struct MigrationConfig"
    - path: "crates/workpot-core/src/services/repo_convert.rs"
      provides: "PreflightResult enum, run_preflight fn, sanitize_worktree fn, unique_worktree_name fn, resolve_template fn, ConvertTarget enum"
      exports: ["PreflightResult", "run_preflight", "sanitize_worktree", "unique_worktree_name", "resolve_template", "ConvertTarget"]
    - path: "crates/workpot-core/src/infra/git.rs"
      provides: "check_all_branches_synced, has_stash additions"
      contains: "pub fn has_stash"
    - path: "crates/workpot-core/src/error.rs"
      provides: "ConversionPreflight and ConversionFailed error variants"
      contains: "ConversionPreflight"
  key_links:
    - from: "crates/workpot-core/src/services/repo_convert.rs"
      to: "crates/workpot-core/src/infra/git.rs"
      via: "crate::infra::git::has_stash / check_all_branches_synced"
      pattern: "use crate::infra::git"
    - from: "crates/workpot-core/src/services/repo_convert.rs"
      to: "crates/workpot-core/src/domain/config.rs"
      via: "MigrationConfig, ProjectNameSource"
      pattern: "use crate::domain::config::MigrationConfig"
---

<objective>
Implement the GREEN pass for all Wave 0 RED tests from Plan 01. This plan delivers:
1. `MigrationConfig` + `ProjectNameSource` in `config.rs` (D-01, D-02, D-04, D-17, D-18)
2. Two new `WorkpotError` variants (`ConversionPreflight`, `ConversionFailed`)
3. Two new git infra functions: `has_stash` and `check_all_branches_synced`
4. The `services/repo_convert.rs` module with `PreflightResult`, `run_preflight`, `sanitize_worktree`, `unique_worktree_name`, `resolve_template`, and `ConvertTarget`

This is the first vertical slice: after this plan, the pure-function contracts and the preflight gate are fully tested and working. The conversion orchestration (rename→clone→health→catalog) comes in Plan 03.

Purpose: Establish the safe-to-migrate gate and core data types that all subsequent plans depend on.
Output: All Wave 0 tests turn GREEN; new module registered in services/mod.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-CONTEXT.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-RESEARCH.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-PATTERNS.md
@crates/workpot-core/src/domain/config.rs
@crates/workpot-core/src/error.rs
@crates/workpot-core/src/infra/git.rs
@crates/workpot-core/src/services/mod.rs
@crates/workpot-core/tests/repo_convert_test.rs
@crates/workpot-core/tests/common/mod.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: MigrationConfig, ProjectNameSource, and WorkpotError variants</name>
  <files>crates/workpot-core/src/domain/config.rs, crates/workpot-core/src/error.rs</files>

  <read_first>
    - crates/workpot-core/src/domain/config.rs (full file — Limits sub-struct pattern lines 39-54, Config struct lines 56-100, validate() method lines 102-162)
    - crates/workpot-core/src/error.rs (full file — WorkpotError enum shape, thiserror macros)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-RESEARCH.md (Pattern 1: Extending Config with [migration] section, full MigrationConfig shape)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-PATTERNS.md (Sub-struct pattern, Default impl pattern, Validation pattern, WorkpotError variant pattern)
  </read_first>

  <behavior>
    - `MigrationConfig::default()` produces: `temp_suffix = ".temp"`, `delete_original = false`, `bare_repo_template = "{project}/bare.git"`, `worktree_template = "{project}/wtrees/{worktree}"`, `project_name_source = ProjectNameSource::FolderName`
    - `ProjectNameSource` has variants `FolderName` and `Alias`; serializes as `"folder_name"` and `"alias"` (serde rename_all = "snake_case")
    - `Config::validate()` returns `Err("migration.temp_suffix must not be empty")` when `temp_suffix` is `""`
    - `Config::validate()` returns `Err("migration.bare_repo_template must contain {project}")` when template lacks `{project}`
    - `Config::validate()` returns `Err("migration.worktree_template must contain {project} and {worktree}")` when template lacks either placeholder
    - `Config` serializes to TOML and back with `migration` section preserving all defaults
    - `WorkpotError::ConversionPreflight("dirty")` displays as `"conversion preflight failed: dirty"`
    - `WorkpotError::ConversionFailed("clone failed")` displays as `"conversion failed: clone failed"`
  </behavior>

  <action>
    Extend `crates/workpot-core/src/domain/config.rs`:

    After `default_stale_dirty_days()` function (before the `Limits` struct), add:
    - Five default functions: `default_temp_suffix() -> String` returns `".temp"`, `default_delete_original() -> bool` returns `false`, `default_bare_repo_template() -> String` returns `"{project}/bare.git"`, `default_worktree_template() -> String` returns `"{project}/wtrees/{worktree}"`, `default_project_name_source() -> ProjectNameSource` returns `ProjectNameSource::FolderName`
    - `ProjectNameSource` enum: `#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]` with `#[serde(rename_all = "snake_case")]`, variants `FolderName` and `Alias`
    - `MigrationConfig` struct: `#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]`, fields `temp_suffix: String`, `delete_original: bool`, `bare_repo_template: String`, `worktree_template: String`, `project_name_source: ProjectNameSource` — each with `#[serde(default = "default_*")]`
    - `impl Default for MigrationConfig` calling all five default functions

    In `Config` struct, add after `stale_dirty_days` field:
    - `#[serde(default)] pub migration: MigrationConfig,`

    In `Config::default()`, add `migration: MigrationConfig::default()` to the struct literal.

    In `Config::validate()`, add three new checks at the end before `Ok(())`:
    - `if self.migration.temp_suffix.is_empty() { return Err("migration.temp_suffix must not be empty".into()); }`
    - `if !self.migration.bare_repo_template.contains("{project}") { return Err("migration.bare_repo_template must contain {project}".into()); }`
    - `if !self.migration.worktree_template.contains("{project}") || !self.migration.worktree_template.contains("{worktree}") { return Err("migration.worktree_template must contain {project} and {worktree}".into()); }`

    Also add `pub use crate::domain::config::MigrationConfig;` and `pub use crate::domain::config::ProjectNameSource;` to `crates/workpot-core/src/lib.rs` re-exports if needed for tests.

    Extend `crates/workpot-core/src/error.rs`:
    Add two new variants to `WorkpotError` enum after `IndexCapExceeded`:
    - `#[error("conversion preflight failed: {0}")] ConversionPreflight(String),`
    - `#[error("conversion failed: {0}")] ConversionFailed(String),`
  </action>

  <verify>
    <automated>cargo test -p workpot-core migration_config 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `config.rs` contains `pub struct MigrationConfig` with exactly five fields: `temp_suffix`, `delete_original`, `bare_repo_template`, `worktree_template`, `project_name_source`
    - `config.rs` contains `pub enum ProjectNameSource` with variants `FolderName` and `Alias`
    - `Config` struct has field `pub migration: MigrationConfig`
    - `Config::validate()` contains check for `migration.temp_suffix.is_empty()`
    - `Config::validate()` contains check for `bare_repo_template.contains("{project}")`
    - `Config::validate()` contains check for `worktree_template.contains("{project}")` and `worktree_template.contains("{worktree}")`
    - `error.rs` contains `ConversionPreflight(String)` and `ConversionFailed(String)` variants
    - `cargo test -p workpot-core migration_config` passes (tests from repo_convert_test.rs about config defaults)
  </acceptance_criteria>

  <done>MigrationConfig, ProjectNameSource, and two new WorkpotError variants added; config default/validate tests GREEN.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Infra additions (has_stash, check_all_branches_synced) and repo_convert module</name>
  <files>crates/workpot-core/src/infra/git.rs, crates/workpot-core/src/services/repo_convert.rs, crates/workpot-core/src/services/mod.rs</files>

  <read_first>
    - crates/workpot-core/src/infra/git.rs (full file — function signature pattern, detect_ahead_behind lines 170-205, list_worktree_paths lines 29-58, error mapping pattern)
    - crates/workpot-core/src/services/mod.rs (full file — pub mod registration pattern)
    - crates/workpot-core/tests/repo_convert_test.rs (RED stubs from Plan 01 — exact function names and expected PreflightResult variants)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-RESEARCH.md (Pattern 2: All-branches sync gate, Pattern 3: Stash detection, Pattern 7: worktree sanitization with collision hash, full PreflightResult type, code examples for sanitize_worktree, unique_worktree_name, resolve_template)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-PATTERNS.md (imports pattern, error mapping pattern, AppContext method delegation, ConvertTarget enum placement)
    - crates/workpot-core/src/domain/config.rs (to import MigrationConfig, ProjectNameSource)
    - crates/workpot-core/src/error.rs (WorkpotError variants now including ConversionPreflight)
  </read_first>

  <behavior>
    In `infra/git.rs`:
    - `has_stash(repo_path: &Path) -> Result<bool>`: returns `false` for a clean repo; requires `mut repo` binding (per RESEARCH Pitfall 2)
    - `check_all_branches_synced(repo_path: &Path) -> Result<Option<SyncBlocker>>` where `SyncBlocker` is an enum with `NoUpstream { branch: String }` and `UnpushedCommits { branch: String, count: usize }`: returns `None` when all branches are pushed; returns `Some(NoUpstream)` when a branch has no tracking remote; returns `Some(UnpushedCommits)` when ahead > 0; treats `branch.name()` returning `Ok(None)` as a blocking error (non-UTF8 branch name, per RESEARCH Pitfall 7)

    In `services/repo_convert.rs`:
    - `sanitize_worktree("feature/my-branch")` returns `"feature.my-branch"`
    - `sanitize_worktree("main")` returns `"main"`
    - `unique_worktree_name("feature/x", &[])` returns `"feature.x"`
    - `unique_worktree_name("feature/x", &["feature.x".to_string()])` returns a string starting with `"feature.x."` with a 6-char hex suffix
    - `resolve_template("{project}/bare.git", "myproject", "")` returns `"myproject/bare.git"`
    - `resolve_template("{project}/wtrees/{worktree}", "myproject", "feature.login")` returns `"myproject/wtrees/feature.login"`
    - `run_preflight(&dirty_repo_path)` returns `Ok(PreflightResult::DirtyWorktree { path: _ })`
    - `run_preflight(&no_upstream_repo_path)` returns `Ok(PreflightResult::NoUpstream { branch: _ })`
    - `run_preflight(&unpushed_repo_path)` returns `Ok(PreflightResult::UnpushedCommits { branch: _, count: _ })`
    - `run_preflight(&stash_repo_path)` returns `Ok(PreflightResult::HasStash)`
    - `run_preflight(&clean_synced_repo_path)` returns `Ok(PreflightResult::Ready)`
    - For bare repos, `run_preflight` opens each worktree path from `list_worktree_paths` and checks dirty state on each (per D-05)
  </behavior>

  <action>
    Add to `crates/workpot-core/src/infra/git.rs` (after existing public functions):

    Add public function `has_stash(repo_path: &Path) -> Result<bool>`:
    - Open with `let mut repo = git2::Repository::open(repo_path).map_err(|_| WorkpotError::GitUnavailable(repo_path.to_path_buf()))?;`
    - `let mut found = false;`
    - `repo.stash_foreach(|_i, _msg, _oid| { found = true; false }).map_err(|_| WorkpotError::GitUnavailable(repo_path.to_path_buf()))?;`
    - Return `Ok(found)`

    Add public function `check_all_branches_synced(repo_path: &Path) -> Result<Option<SyncBlocker>>`:
    - Define `pub enum SyncBlocker { NoUpstream { branch: String }, UnpushedCommits { branch: String, count: usize }, NonUtf8BranchName }` in `infra/git.rs` (pub, so repo_convert.rs can use it)
    - Open repo immutably (branches iteration does not require mut)
    - Call `repo.branches(Some(git2::BranchType::Local)).map_err(...)?`
    - For each branch: get name — if `branch.name()` returns `Ok(None)`, return `Ok(Some(SyncBlocker::NonUtf8BranchName))`; get upstream with `branch.upstream()` — if error, return `Ok(Some(SyncBlocker::NoUpstream { branch: name.to_string() }))`; get local OID from `branch.get().target()`, upstream OID from `upstream.get().target()`; call `repo.graph_ahead_behind(local_oid, upstream_oid)` — if `ahead > 0`, return `Ok(Some(SyncBlocker::UnpushedCommits { branch: name.to_string(), count: ahead }))`
    - If all branches pass, return `Ok(None)`
    - Map all git2 errors to `WorkpotError::GitUnavailable(repo_path.to_path_buf())`

    Create `crates/workpot-core/src/services/repo_convert.rs`:

    Import section (per PATTERNS.md imports pattern):
    - `use crate::domain::config::{MigrationConfig, ProjectNameSource};`
    - `use crate::domain::RepoRecord;`
    - `use crate::error::{Result, WorkpotError};`
    - `use crate::infra::git;`
    - `use crate::services::catalog::{is_bare_repo, is_git_worktree};`
    - `use std::path::{Path, PathBuf};`

    Define `pub enum ConvertTarget { Bare, Normal }` (this is the clap::ValueEnum too, but for now just as a plain enum; CLI plan adds the derive).

    Define `pub enum PreflightResult` with variants:
    - `Ready`
    - `DirtyWorktree { path: PathBuf }`
    - `NoUpstream { branch: String }`
    - `UnpushedCommits { branch: String, count: usize }`
    - `HasStash`
    - `NotInCatalog`  (non-TDD: defensive guard checked by Plan 03 convert_repo before clone; no unit-test RED stub needed — behavior is "reject unknown path" with no pure function to test independently)
    - `WrongLayout { current: &'static str, requested: &'static str }`  (non-TDD: same rationale — enum variant only; logic checked by Plan 03 integration test)

    Implement `pub fn sanitize_worktree(branch: &str) -> String`:
    - `branch.replace('/', ".")`

    Implement `pub fn unique_worktree_name(branch: &str, existing: &[String]) -> String`:
    - Compute `base = sanitize_worktree(branch)`
    - If `!existing.contains(&base)`, return `base`
    - Collision: use `std::collections::hash_map::DefaultHasher` — `use std::hash::{Hash, Hasher}; let mut h = std::collections::hash_map::DefaultHasher::new(); branch.hash(&mut h); let hash = format!("{:x}", h.finish());` — return `format!("{}.{}", base, &hash[..6])`

    Implement `pub fn resolve_template(template: &str, project: &str, worktree: &str) -> String`:
    - `template.replace("{project}", project).replace("{worktree}", worktree)`

    Implement `pub fn resolve_project_name(config: &MigrationConfig, record: &RepoRecord) -> String`:
    - Match `config.project_name_source`: `ProjectNameSource::Alias => record.alias.clone().unwrap_or_else(|| record.name.clone())`, `ProjectNameSource::FolderName => record.name.clone()`

    Implement `pub fn run_preflight(path: &Path) -> Result<PreflightResult>`:
    - Canonicalize path, map error to `WorkpotError::InvalidPath`
    - Detect if it is a bare repo via `is_bare_repo(&canonical)` — if bare, use `git::list_worktree_paths(&canonical)` to enumerate worktrees for dirty check; if normal, use `[canonical.clone()]` as the list
    - For each worktree path: call `git::detect_dirty(wt_path)` (RESEARCH.md §Don't Hand-Roll: reuse existing function, do NOT re-implement); if dirty, return `Ok(PreflightResult::DirtyWorktree { path: wt_path.to_path_buf() })`
    - Make `detect_dirty` in `infra/git.rs` `pub(crate)` (change `fn detect_dirty` → `pub(crate) fn detect_dirty`) so `repo_convert.rs` can call it directly
    - Call `git::check_all_branches_synced(&canonical)`: if `Some(SyncBlocker::NoUpstream { branch })`, return `Ok(PreflightResult::NoUpstream { branch })`; if `Some(SyncBlocker::UnpushedCommits { branch, count })`, return `Ok(PreflightResult::UnpushedCommits { branch, count })`; if `Some(SyncBlocker::NonUtf8BranchName)`, return `Err(WorkpotError::ConversionPreflight("non-UTF8 branch name found".into()))`
    - Call `git::has_stash(&canonical)`: if `true`, return `Ok(PreflightResult::HasStash)`
    - Return `Ok(PreflightResult::Ready)`

    Register in `crates/workpot-core/src/services/mod.rs`: add `pub mod repo_convert;` in alphabetical order (between `repo_fuzzy` and `repo_priority`).

    (`detect_dirty` pub(crate) change is already mandated above in run_preflight action.)

    Add inline unit tests in `repo_convert.rs` under `#[cfg(test)] mod tests`:
    - `sanitize_worktree_slash_to_dot`: asserts `sanitize_worktree("feature/x") == "feature.x"`
    - `resolve_template_no_op_for_missing_placeholder`: asserts `resolve_template("{project}/bare.git", "proj", "")` equals `"proj/bare.git"`
  </action>

  <verify>
    <automated>cargo test -p workpot-core repo_convert 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `cargo test -p workpot-core repo_convert` exits 0
    - All 12 tests from `repo_convert_test.rs` pass (GREEN): `sanitize_worktree_replaces_slash_with_dot`, `sanitize_worktree_no_slash_unchanged`, `unique_worktree_no_collision`, `unique_worktree_collision_appends_hash`, `resolve_template_substitutes_project`, `resolve_template_substitutes_both`, `migration_config_defaults_match_spec`, `migration_config_serde_round_trip`, `preflight_blocks_dirty`, `preflight_blocks_no_upstream`, `preflight_blocks_unpushed`, `preflight_blocks_stash`, `preflight_passes_clean_synced`
    - `services/repo_convert.rs` exports: `PreflightResult`, `ConvertTarget`, `run_preflight`, `sanitize_worktree`, `unique_worktree_name`, `resolve_template`, `resolve_project_name`
    - `infra/git.rs` exports: `has_stash`, `check_all_branches_synced`, `SyncBlocker`
    - `services/mod.rs` contains `pub mod repo_convert;`
    - `cargo test -p workpot-core --all-targets` exits 0 (no regressions)
  </acceptance_criteria>

  <done>All Wave 0 RED tests turn GREEN; preflight gate returns correct variants for all five scenarios; pure-function tests all pass; no existing tests regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user path → git2 open | Repo path from caller is canonicalized before passing to git2 |
| branch names → stash check | stash_foreach requires &mut repo; failure to open mutably would be a compile error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-02-01 | Tampering | `run_preflight(path)` path input | mitigate | Canonicalize path with `std::fs::canonicalize()` before all git2 operations; map `Err` to `WorkpotError::InvalidPath` |
| T-07.1-02-02 | Tampering | non-UTF8 branch names in preflight | mitigate | `branch.name()` returning `Ok(None)` treated as blocking error `ConversionPreflight("non-UTF8 branch name found")` — per RESEARCH Pitfall 7 |
| T-07.1-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all dependencies already in workspace Cargo.lock |
</threat_model>

<verification>
`cargo test -p workpot-core repo_convert` exits 0 with all test cases passing.
`cargo test -p workpot-core --all-targets` exits 0 (no regressions in existing tests).
`cargo clippy -p workpot-core` emits no new errors (disallowed_methods deny is respected; unwrap/expect not used in library code).
</verification>

<success_criteria>
- All 12 RED tests from Plan 01 are GREEN
- MigrationConfig and ProjectNameSource are in config.rs with correct serde defaults
- Config::validate() enforces all three migration template constraints
- WorkpotError has ConversionPreflight and ConversionFailed variants
- run_preflight returns correct PreflightResult for all five fixture scenarios
- Pure functions (sanitize, resolve, unique_worktree) produce correct outputs
</success_criteria>

<output>
Create `.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-02-SUMMARY.md` when done
</output>

---
phase: 07.1
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - crates/workpot-core/tests/repo_convert_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "RED tests exist for preflight gate (dirty, unpushed, stash, no-upstream)"
    - "RED tests exist for worktree name sanitization (slash-to-dot, collision hash)"
    - "RED tests exist for path template resolution (project substitution, worktree substitution)"
    - "RED tests exist for MigrationConfig serde defaults (temp_suffix, delete_original, templates)"
  artifacts:
    - path: "crates/workpot-core/tests/repo_convert_test.rs"
      provides: "All Wave 0 RED test stubs — must fail to compile or fail at runtime before implementation"
      min_lines: 80
  key_links:
    - from: "crates/workpot-core/tests/repo_convert_test.rs"
      to: "workpot_core::services::repo_convert"
      via: "use workpot_core::services::repo_convert"
      pattern: "use workpot_core::services::repo_convert"
---

## Phase Goal

**As a** developer with repos in both normal and bare layouts, **I want to** run `workpot repo convert <path> --to bare|normal` and have my repo safely migrated, **so that** I can switch between layouts without manual multi-step git operations.

<objective>
Create the Wave 0 RED test stubs for Phase 07.1. This plan establishes the test file that defines all contracts for the pure functions and integration flows before any implementation code is written. The file must fail to compile (or fail at test runtime) because the referenced symbols do not yet exist.

Purpose: Test-first contracts prevent ambiguity and ensure that implementation in Wave 1 satisfies observable behaviours, not just ad-hoc code.
Output: `crates/workpot-core/tests/repo_convert_test.rs` with all RED stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-CONTEXT.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-RESEARCH.md
@.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-PATTERNS.md
@crates/workpot-core/tests/common/mod.rs
@crates/workpot-core/tests/catalog_test.rs
@crates/workpot-core/src/domain/config.rs
@crates/workpot-core/src/error.rs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test stubs for pure functions and integration contracts</name>
  <files>crates/workpot-core/tests/repo_convert_test.rs</files>

  <read_first>
    - crates/workpot-core/tests/catalog_test.rs (file header, fixture helpers, git_cmd usage, AppContext isolation pattern)
    - crates/workpot-core/tests/common/mod.rs (git_cmd helper, seed_bare_repo)
    - crates/workpot-core/src/domain/config.rs (Config struct and Limits sub-struct pattern for MigrationConfig shape)
    - crates/workpot-core/src/error.rs (WorkpotError variants to understand error shape)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-RESEARCH.md (full preflight result type, integration fixture list, all code examples)
    - .planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-PATTERNS.md (TDD Wave 0 test structure, file header pattern)
  </read_first>

  <behavior>
    Unit RED stubs (no fixture needed — pure function calls that fail to compile):
    - Test `sanitize_worktree_replaces_slash_with_dot`: calls `workpot_core::services::repo_convert::sanitize_worktree("feature/my-branch")`, asserts result equals `"feature.my-branch"`
    - Test `sanitize_worktree_no_slash_unchanged`: calls `sanitize_worktree("main")`, asserts equals `"main"`
    - Test `unique_worktree_no_collision`: calls `workpot_core::services::repo_convert::unique_worktree_name("feature/x", &[])`, asserts equals `"feature.x"`
    - Test `unique_worktree_collision_appends_hash`: calls `unique_worktree_name("feature/x", &["feature.x".to_string()])`, asserts result starts with `"feature.x."` and has length > `"feature.x".len()`
    - Test `resolve_template_substitutes_project`: calls `workpot_core::services::repo_convert::resolve_template("{project}/bare.git", "myproject", "")`, asserts equals `"myproject/bare.git"`
    - Test `resolve_template_substitutes_both`: calls `resolve_template("{project}/wtrees/{worktree}", "myproject", "feature.login")`, asserts equals `"myproject/wtrees/feature.login"`
    - Test `migration_config_defaults_match_spec`: constructs `workpot_core::domain::config::MigrationConfig::default()`, asserts `temp_suffix == ".temp"`, `delete_original == false`, `bare_repo_template == "{project}/bare.git"`, `worktree_template == "{project}/wtrees/{worktree}"`
    - Test `migration_config_serde_round_trip`: serializes `MigrationConfig::default()` to TOML and deserializes back, asserts equals original

    Integration RED stubs (use temp git fixtures; must compile but behaviours assert against not-yet-implemented code):
    - Test `preflight_blocks_dirty`: create a temp normal repo with a tracked dirty file; call `workpot_core::services::repo_convert::run_preflight(&path)`, assert result is `PreflightResult::DirtyWorktree { .. }`
    - Test `preflight_blocks_no_upstream`: create a temp normal repo with a branch that has no upstream configured; call `run_preflight(&path)`, assert result is `PreflightResult::NoUpstream { .. }`
    - Test `preflight_blocks_unpushed`: create a temp normal repo where HEAD branch is ahead of upstream; call `run_preflight(&path)`, assert result is `PreflightResult::UnpushedCommits { .. }`
    - Test `preflight_blocks_stash`: create a temp normal repo with a stash entry; call `run_preflight(&path)`, assert result is `PreflightResult::HasStash`
    - Test `preflight_passes_clean_synced`: create a temp normal repo that is clean with all branches pushed; call `run_preflight(&path)`, assert result is `PreflightResult::Ready`
  </behavior>

  <action>
    Create `crates/workpot-core/tests/repo_convert_test.rs`. This is the Wave 0 RED file — it must fail to compile because `workpot_core::services::repo_convert` does not yet exist.

    File header (per PATTERNS.md — copy from catalog_test.rs):
    - Line 1: `#![allow(clippy::disallowed_methods)]`
    - Line 3: `mod common;`
    - Import `workpot_core::services::repo_convert` and the types it will export: `sanitize_worktree`, `unique_worktree_name`, `resolve_template`, `run_preflight`, `PreflightResult`
    - Import `workpot_core::domain::config::MigrationConfig`
    - Import `std::path::PathBuf`, `std::fs`, `tempfile`

    Fixture helper `normal_repo_clean_synced(parent: &Path) -> PathBuf`:
    - Creates a bare "remote" with `git init --bare -q -b main` then seeds it via `common::seed_bare_repo(bare_path)`
    - Clones it locally with `git clone -q <bare> <clone_path>`
    - Sets user.email and user.name via `git config`
    - Returns the clone path (clean, with upstream, 0 ahead)

    Fixture helper `dirty_normal_repo(parent: &Path) -> PathBuf`:
    - Call `normal_repo_clean_synced(parent)` to get a clean synced repo
    - Write a tracked file change: find a file or use `git_cmd().args(["commit", "--allow-empty", ...])` to ensure there is a tracked file, then modify it with `std::fs::write`
    - Return the path

    Fixture helper `unpushed_normal_repo(parent: &Path) -> PathBuf`:
    - Call `normal_repo_clean_synced(parent)`
    - Make a new empty commit: `git commit --allow-empty -m "local-only"`
    - Return the path (HEAD is 1 ahead of upstream)

    Fixture helper `stash_normal_repo(parent: &Path) -> PathBuf`:
    - Call `normal_repo_clean_synced(parent)`
    - Modify a tracked file, then `git stash`
    - Return the path

    Fixture helper `no_upstream_normal_repo(parent: &Path) -> PathBuf`:
    - `git init -q -b main <path>`
    - Set user.email and user.name
    - `git commit --allow-empty -m "init"`
    - Do NOT configure any remote or upstream
    - Return the path (branch main has no upstream)

    All test functions follow the pattern: `let dir = tempfile::tempdir().expect("tempdir"); let path = fixture_fn(dir.path()); let result = run_preflight(&path).expect("preflight"); assert!(matches!(result, PreflightResult::Expected { .. }));`

    For compile-time failure (symbols not yet existing), no `#[ignore]` attribute — tests should fail to compile with "unresolved module" or equivalent. Do not `#[ignore]` or suppress the RED state.
  </action>

  <verify>
    <automated>cargo test -p workpot-core repo_convert 2>&1 | grep -E "(error\[|FAILED|test result)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `crates/workpot-core/tests/repo_convert_test.rs` exists
    - File begins with `#![allow(clippy::disallowed_methods)]`
    - File contains `mod common;` on line 3
    - File imports `workpot_core::services::repo_convert`
    - `cargo test -p workpot-core repo_convert` produces a compile error referencing `repo_convert` module not found (RED state confirmed)
    - Test names present: `sanitize_worktree_replaces_slash_with_dot`, `unique_worktree_no_collision`, `unique_worktree_collision_appends_hash`, `resolve_template_substitutes_project`, `resolve_template_substitutes_both`, `migration_config_defaults_match_spec`, `migration_config_serde_round_trip`, `preflight_blocks_dirty`, `preflight_blocks_no_upstream`, `preflight_blocks_unpushed`, `preflight_blocks_stash`, `preflight_passes_clean_synced`
    - No `#[ignore]` attributes on any test
  </acceptance_criteria>

  <done>All Wave 0 RED stubs committed; `cargo test -p workpot-core repo_convert` fails with compile errors referencing the not-yet-created `repo_convert` module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → git subprocess | Test helpers invoke `git` CLI via `common::git_cmd()` which strips GIT_* env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-W0-01 | Tampering | test git fixtures | accept | Tests use tempdir isolation; no persistent state written outside tempdir |
| T-07.1-W0-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH.md § Standard Stack: "No new crates") |
</threat_model>

<verification>
`cargo test -p workpot-core repo_convert` fails with compile error (RED state).
File `crates/workpot-core/tests/repo_convert_test.rs` contains at minimum 12 named test functions covering all Wave 0 contracts.
</verification>

<success_criteria>
- Wave 0 RED file exists with all TDD stubs
- Tests fail to compile because `workpot_core::services::repo_convert` does not exist yet
- Every function named in VALIDATION.md § Wave 0 Requirements is represented
</success_criteria>

<output>
Create `.planning/phases/07.1-repo-type-migration-bare-normal-with-settings-md-and-configu/07.1-01-SUMMARY.md` when done
</output>

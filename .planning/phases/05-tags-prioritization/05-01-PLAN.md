---
phase: 05-tags-prioritization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/workpot-core/tests/org_test.rs
  - src/lib/tagFilter.ts
  - src/lib/tagFilter.test.ts
  - src/lib/pinOrder.ts
  - src/lib/pinOrder.test.ts
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-04

must_haves:
  truths:
    - "cargo test -p workpot-core org exits 0 (stubs compile, some tests may be ignored/pending)"
    - "npm test -- tagFilter exits 0 with at least 4 passing assertions"
    - "npm test -- pinOrder exits 0 with at least 3 passing assertions"
  artifacts:
    - path: "crates/workpot-core/tests/org_test.rs"
      provides: "Rust test stubs for tag CRUD, pin mutations, notes CRUD"
    - path: "src/lib/tagFilter.ts"
      provides: "parseTagFilter and matchesTags pure functions"
    - path: "src/lib/tagFilter.test.ts"
      provides: "Vitest tests for #tag parse and AND filter logic"
    - path: "src/lib/pinOrder.ts"
      provides: "reorderPinned and toPinOrderPayload array helpers"
    - path: "src/lib/pinOrder.test.ts"
      provides: "Vitest tests for drag-reorder array helpers"
  key_links:
    - from: "src/lib/tagFilter.ts"
      to: "src/lib/types.ts"
      via: "import type { RepoDto }"
      pattern: "import.*RepoDto.*from.*types"
    - from: "src/lib/pinOrder.ts"
      to: "src/lib/types.ts"
      via: "import type { RepoDto }"
      pattern: "import.*RepoDto.*from.*types"
---

<objective>
Create Wave 0 test infrastructure required by VALIDATION.md before any implementation tasks run.

This plan creates three test stubs (Rust org tests, TypeScript tag filter module + tests, TypeScript pin order module + tests). The Rust stubs compile but call functions that do not yet exist — they are marked `#[ignore]` so the test binary compiles and the suite exits 0. The TypeScript modules contain full implementations because they are pure array/string utilities with no dependency on Rust services.

Purpose: Unblock Wave 1 Rust and TypeScript tasks by providing the test harness that those tasks will make green. Also ships the complete tagFilter.ts and pinOrder.ts utility modules since they have no external dependencies.

Output: 5 new files. `cargo test -p workpot-core org` exits 0. `npm test -- tagFilter` exits 0 with passing tests. `npm test -- pinOrder` exits 0 with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@.planning/phases/05-tags-prioritization/05-RESEARCH.md
@.planning/phases/05-tags-prioritization/05-PATTERNS.md
@.planning/phases/05-tags-prioritization/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust org test stubs (org_test.rs)</name>
  <read_first>
    - crates/workpot-core/tests/tray_migration_test.rs (temp_db fixture pattern, imports)
    - crates/workpot-core/tests/catalog_test.rs (AppContext fixture pattern, assert style)
    - crates/workpot-core/src/infra/migrations.rs (to reference apply_migrations import path)
    - crates/workpot-core/src/lib.rs (AppContext public API surface)
  </read_first>
  <files>crates/workpot-core/tests/org_test.rs</files>
  <behavior>
    - Compiles with `cargo test -p workpot-core org --no-run`
    - Suite exits 0 — all tests that call non-existent functions are `#[ignore]`
    - `test_tag_crud_set_and_list` — ignored stub: calls org::set_tags and org::list_tags_for_repo
    - `test_tag_add_remove` — ignored stub: calls org::add_tag and org::remove_tag
    - `test_notes_set_and_get` — ignored stub: calls org::set_notes, verifies via SELECT
    - `test_pin_set_and_unpin` — ignored stub: calls org::set_pin(true) then set_pin(false)
    - `test_pin_order_batch_update` — ignored stub: calls org::set_pin_order with a slice
    - `test_db_fixture_compiles` — NOT ignored: verifies temp_db() returns a valid migrated Connection (will fail until migration 006 exists, so also mark ignored)
  </behavior>
  <action>
    Create `crates/workpot-core/tests/org_test.rs`.

    File-level attributes: `#![allow(clippy::disallowed_methods)]`

    Imports (pattern from tray_migration_test.rs and catalog_test.rs):
    - `use rusqlite::Connection;`
    - `use workpot_core::infra::{migrations, store};`
    - `use workpot_core::services::org;`
    - `use workpot_core::AppContext;`

    `temp_db()` helper function: creates a `tempfile::TempDir`, opens a `Connection::open(dir.path().join("workpot.db"))`, calls `migrations::apply_migrations(&mut conn)`, returns `(TempDir, Connection)`. Identical pattern to tray_migration_test.rs lines 10-16.

    Each test function: decorated with `#[test]` and `#[ignore]` (except as noted). Bodies contain the function calls the test will eventually exercise, written as comments with `todo!()` placeholders so the file compiles without the functions existing. Structure: `// TODO: call org::set_tags(...)` followed by `todo!()`.

    Test names must contain "org" substring so `cargo test -p workpot-core org` runs them.

    Mark `test_db_fixture_compiles` as `#[ignore]` too since migration 006 does not exist yet.
  </action>
  <verify>
    <automated>cargo test -p workpot-core org --no-run 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cargo test -p workpot-core org --no-run` exits 0 (compilation succeeds).
    File exists at `crates/workpot-core/tests/org_test.rs`.
    File contains `#[ignore]` on all test functions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tagFilter.ts + tagFilter.test.ts (full implementation)</name>
  <read_first>
    - src/lib/fuzzy.ts (import style, export style, function signature patterns)
    - src/lib/fuzzy.test.ts (describe/it structure, repo() factory pattern, import block)
    - src/lib/types.ts (current RepoDto interface — note: Phase 5 fields not yet present; use only existing fields in factory defaults, add tags: string[] to factory)
  </read_first>
  <files>src/lib/tagFilter.ts, src/lib/tagFilter.test.ts</files>
  <behavior>
    tagFilter.ts:
    - parseTagFilter("hello #backend #infra") returns { baseQuery: "hello", activeTags: ["backend", "infra"] }
    - parseTagFilter("#backend") returns { baseQuery: "", activeTags: ["backend"] }
    - parseTagFilter("hello world") returns { baseQuery: "hello world", activeTags: [] }
    - parseTagFilter("") returns { baseQuery: "", activeTags: [] }
    - matchesTags(repo with tags:["Backend"], ["backend"]) returns true (case-insensitive)
    - matchesTags(repo with tags:["backend","infra"], ["backend","infra"]) returns true (AND: both required)
    - matchesTags(repo with tags:["backend"], ["backend","infra"]) returns false (AND: infra missing)
    - matchesTags(repo with tags:[], []) returns true (no active tags = pass-all)
    tagFilter.test.ts:
    - At least 8 test cases covering the behaviors above
    - Uses the `repo()` factory function extended with `tags: string[]` field
  </behavior>
  <action>
    Create `src/lib/tagFilter.ts`:

    Single import: `import type { RepoDto } from "./types";`

    Implement `parseTagFilter(query: string): { baseQuery: string; activeTags: string[] }`:
    - Split `query.trim()` on `/\s+/` to get tokens array (handle empty string: return early with `{ baseQuery: "", activeTags: [] }`)
    - `activeTags`: tokens where `t.startsWith("#") && t.length > 1`, mapped with `t.slice(1).toLowerCase()`
    - `baseQuery`: tokens where `!t.startsWith("#")`, joined with `" "`, trimmed

    Implement `matchesTags(repo: RepoDto, activeTags: string[]): boolean`:
    - If `activeTags.length === 0` return `true`
    - `const repoTags = repo.tags.map(t => t.toLowerCase())`
    - Return `activeTags.every(at => repoTags.includes(at))` (AND logic per D-11)

    Create `src/lib/tagFilter.test.ts`:

    Imports: `import { describe, expect, it } from "vitest";`, `import { parseTagFilter, matchesTags } from "./tagFilter";`, `import type { RepoDto } from "./types";`

    `repo()` factory: minimal shape with `tags: partial.tags ?? []` — only include fields that exist in current types.ts. Use `as any` cast if RepoDto does not yet have tags field (it will be added in Wave 1 Plan 03, but tests must pass now). Cast the factory return to `any` only for compilation, not for assertion values.

    `describe("parseTagFilter")` block with at least 5 it() cases.
    `describe("matchesTags")` block with at least 3 it() cases covering AND logic and case-insensitivity.
  </action>
  <verify>
    <automated>npm test -- tagFilter 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm test -- tagFilter` exits 0.
    All parseTagFilter and matchesTags tests pass (no skipped).
    `src/lib/tagFilter.ts` exports `parseTagFilter` and `matchesTags`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pinOrder.ts + pinOrder.test.ts (full implementation)</name>
  <read_first>
    - src/lib/sort.ts (import style, export style, type patterns)
    - src/lib/sort.test.ts (describe/it structure, repo() factory lines 5-15)
    - src/lib/types.ts (current RepoDto interface)
  </read_first>
  <files>src/lib/pinOrder.ts, src/lib/pinOrder.test.ts</files>
  <behavior>
    pinOrder.ts:
    - reorderPinned([A,B,C], 0, 2) returns [B,C,A] with pin_order 0,1,2
    - reorderPinned([A,B,C], 2, 0) returns [C,A,B] with pin_order 0,1,2
    - reorderPinned([A,B,C], 1, 1) returns [A,B,C] unchanged (same index noop)
    - toPinOrderPayload([A,B,C]) returns [{ path: A.path, order: 0 }, { path: B.path, order: 1 }, { path: C.path, order: 2 }]
    pinOrder.test.ts:
    - At least 5 test cases
    - reorderPinned assigns contiguous pin_order values starting from 0 after every drag
  </behavior>
  <action>
    Create `src/lib/pinOrder.ts`:

    Single import: `import type { RepoDto } from "./types";`

    Implement `reorderPinned(repos: RepoDto[], from: number, to: number): RepoDto[]`:
    - If `from === to` return `repos` unchanged
    - `const result = [...repos]`
    - `const [item] = result.splice(from, 1)`
    - `result.splice(to, 0, item)`
    - Return `result.map((r, i) => ({ ...r, pin_order: i }))` — always re-sequences 0..N (Claude's discretion: re-sequence on every drop per RESEARCH.md anti-patterns note)

    Implement `toPinOrderPayload(repos: RepoDto[]): Array<{ path: string; order: number }>`:
    - Return `repos.map((r, i) => ({ path: r.path, order: i }))`

    Create `src/lib/pinOrder.test.ts`:

    Imports: `import { describe, expect, it } from "vitest";`, `import { reorderPinned, toPinOrderPayload } from "./pinOrder";`, `import type { RepoDto } from "./types";`

    `repo()` factory extended with Phase 5 fields (using `as any` for types not yet in RepoDto). For pin_order, use `partial.pin_order ?? null`.

    `describe("reorderPinned")` with at least 4 it() cases.
    `describe("toPinOrderPayload")` with at least 1 it() case.
  </action>
  <verify>
    <automated>npm test -- pinOrder 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm test -- pinOrder` exits 0.
    All reorderPinned and toPinOrderPayload tests pass.
    `src/lib/pinOrder.ts` exports `reorderPinned` and `toPinOrderPayload`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → SQLite | Temp DB created per-test; no real user data at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | org_test.rs temp DB | accept | Test-only; isolated tempdir per test run |
| T-05-W0-SC | Tampering | npm/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run in sequence after all tasks:
1. `cargo test -p workpot-core org --no-run` — must exit 0 (compilation gate)
2. `npm test -- tagFilter` — must exit 0, all tests pass
3. `npm test -- pinOrder` — must exit 0, all tests pass
4. `npm test` — full frontend suite must still exit 0 (no regressions)
</verification>

<success_criteria>
- `crates/workpot-core/tests/org_test.rs` exists and compiles
- `src/lib/tagFilter.ts` and `src/lib/tagFilter.test.ts` exist with all tests passing
- `src/lib/pinOrder.ts` and `src/lib/pinOrder.test.ts` exist with all tests passing
- `npm test` full suite exits 0
</success_criteria>

<output>
Create `.planning/phases/05-tags-prioritization/05-01-SUMMARY.md` when done.
</output>

---
phase: 05-tags-prioritization
plan: 04
type: execute
wave: 2
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04

must_haves:
  truths:
    - "record_to_dto maps pinned, pin_order, notes, tags, branches fields"
    - "list_repos IPC returns RepoDto with all Phase 5 fields populated"
    - "set_tags IPC validates tag max 64 chars, rejects empty or #-prefixed tags"
    - "set_notes IPC validates notes max 500 chars"
    - "show_repo_context_menu builds Tauri 2 popup and emits repo-context-action event"
    - "list_branches IPC returns Vec<String> branch names via git2"
    - "cargo build --package workpot-tray exits 0"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "All 9 new IPC commands: set_tags, add_tag, remove_tag, list_all_tags, set_notes, set_pin, set_pin_order, list_branches, show_repo_context_menu"
      contains: "pub fn set_tags"
    - path: "src-tauri/src/lib.rs"
      provides: "All new commands registered in invoke_handler; on_menu_event handler registered"
      contains: "set_tags"
  key_links:
    - from: "src-tauri/src/commands.rs"
      to: "crates/workpot-core/src/lib.rs"
      via: "ctx.set_tags / ctx.set_notes / ctx.set_pin"
      pattern: "ctx\\.set_"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands.rs"
      via: "generate_handler! registration"
      pattern: "commands::set_tags"
---

<objective>
Implement all new Tauri IPC commands for Phase 5 org features: tag mutations (set_tags, add_tag, remove_tag, list_all_tags), notes mutation (set_notes), pin mutations (set_pin, set_pin_order), branch list query (list_branches), and context menu (show_repo_context_menu). Extend record_to_dto and RepoDto struct in commands.rs with all Phase 5 fields.

Purpose: Connects the Rust org.rs service (Plan 02) to the Svelte frontend (Plans 05, 06). After this plan, Svelte can invoke all org mutations and receive complete RepoDto objects with tags, notes, and pin state.

Output: 2 files modified. `cargo build --package workpot-tray` exits 0. All IPC commands callable from Svelte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@.planning/phases/05-tags-prioritization/05-RESEARCH.md
@.planning/phases/05-tags-prioritization/05-PATTERNS.md
@src-tauri/src/commands.rs
@src-tauri/src/lib.rs
@crates/workpot-core/src/lib.rs
@crates/workpot-core/src/domain/repo.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend commands.rs — RepoDto, record_to_dto, org IPC commands, list_branches</name>
  <read_first>
    - src-tauri/src/commands.rs (full file — RepoDto struct, record_to_dto function, existing command signatures, lock pattern, all imports)
    - crates/workpot-core/src/lib.rs (AppContext public methods including new org methods from Plan 02)
    - crates/workpot-core/src/domain/repo.rs (RepoRecord fields after Plan 02 extension)
  </read_first>
  <files>src-tauri/src/commands.rs</files>
  <action>
    Edit `src-tauri/src/commands.rs`:

    1. Extend `RepoDto` struct — add after existing fields:
       `pub pinned: bool,`
       `pub pin_order: Option<i64>,`
       `pub notes: Option<String>,`
       `pub tags: Vec<String>,`
       `pub branches: Vec<String>,`

    2. Extend `record_to_dto` (or `repo_records_to_dtos`) mapping:
       - `pinned: record.pinned,`
       - `pin_order: record.pin_order,`
       - `notes: record.notes.clone(),`
       - `tags: record.tags.clone(),`
       - `branches: vec![]` — branches are NOT pre-loaded in list_repos (they come from list_branches IPC on detail pane open, per RESEARCH.md A4 resolution)

    3. Add these sync IPC commands (each follows the lock → call → map_err pattern from PATTERNS.md shared patterns):

       `set_tags(repo_path: String, tags: Vec<String>, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Validate BEFORE lock: each tag must be ≤ 64 chars after trim, non-empty after trim, must not contain `#` (reserved per RESEARCH.md security domain)
       - Return `Err("tag too long".to_string())` etc. if invalid
       - Lock ctx, call `ctx.set_tags(&repo_path, &tags.iter().map(|t| t.as_str()).collect::<Vec<_>>())`

       `add_tag(repo_path: String, tag: String, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Validate: tag ≤ 64 chars, non-empty, no `#` char
       - Lock ctx, call `ctx.add_tag(&repo_path, &tag)`

       `remove_tag(repo_path: String, tag: String, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Lock ctx, call `ctx.remove_tag(&repo_path, &tag)`

       `list_all_tags(state: State<'_, Arc<Mutex<AppContext>>>) -> Result<Vec<String>, String>`:
       - Lock ctx, call `ctx.list_all_tags()`

       `set_notes(repo_path: String, notes: Option<String>, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Validate BEFORE lock: if notes is Some, length must be ≤ 500 chars (per D-25)
       - Trim trailing whitespace from notes value before persisting
       - Lock ctx, call `ctx.set_notes(&repo_path, notes.as_deref())`

       `set_pin(repo_path: String, pinned: bool, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Lock ctx, call `ctx.set_pin(&repo_path, pinned)`

       `set_pin_order(items: Vec<(String, i64)>, state: State<'_, Arc<Mutex<AppContext>>>) -> Result<(), String>`:
       - Lock ctx, convert items to `Vec<(&str, i64)>` borrowing from the items vec
       - Call `ctx.set_pin_order(&items.iter().map(|(p, o)| (p.as_str(), *o)).collect::<Vec<_>>())`

    4. Add `list_branches` async command:
       ```
       pub async fn list_branches(repo_path: String) -> Result<Vec<String>, String>
       ```
       - Does NOT need AppContext state — reads git directly
       - Use `git2::Repository::open(&repo_path)` to open the repo
       - Call `.branches(Some(git2::BranchType::Local))` to list local branches
       - For each branch, call `.name()` to get the name string
       - Filter out None names and collect into `Vec<String>`
       - Return `Err(e.to_string())` on git2 error
       - git2 is already a dependency of workpot-core; the workpot-tray crate has a path dep on workpot-core which re-exports or the tray can depend on git2 directly. Check `src-tauri/Cargo.toml` — if git2 is not listed as a direct dep, add `git2 = { workspace = true }` following the workspace pattern.

    5. Add `show_repo_context_menu` command:
       ```
       pub async fn show_repo_context_menu(
           window: tauri::Window,
           app: tauri::AppHandle,
           repo_path: String,
           is_pinned: bool,
           tags: Vec<String>,
       ) -> Result<(), String>
       ```
       - Build a Tauri 2 `Menu` with items:
         - `MenuItem::with_id(app_handle, "pin", if is_pinned { "Unpin" } else { "Pin" }, true, None::<&str>)?`
         - `MenuItem::with_id(app_handle, "add_tag", "Add tag…", true, None::<&str>)?`
         - `MenuItem::with_id(app_handle, "remove_tag", "Remove tag…", tags.len() > 0, None::<&str>)?`
       - Call `menu.popup(window.clone())?`
       - Store `repo_path` in the app state for the MenuEvent handler to retrieve. Since MenuEvent handlers cannot easily capture per-popup context, the simplest approach: store the current context_menu_repo_path in a `Mutex<Option<String>>` state (add to app state setup in lib.rs), set it before popup, clear after event handled.
       - Return `Ok(())`
       - Imports needed: `use tauri::menu::{Menu, MenuItem};`, `use tauri::{AppHandle, Window};`
  </action>
  <verify>
    <automated>cargo build --package workpot-tray 2>&1 | tail -30</automated>
  </verify>
  <done>
    `cargo build --package workpot-tray` exits 0.
    `commands.rs` contains `pub fn set_tags`, `pub fn set_notes`, `pub fn set_pin`, `pub async fn list_branches`, `pub async fn show_repo_context_menu`.
    `RepoDto` struct contains `pub tags: Vec<String>`.
    `record_to_dto` maps all 5 new fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register commands in lib.rs + MenuEvent handler</name>
  <read_first>
    - src-tauri/src/lib.rs (full file — current invoke_handler, setup closure, on_menu_event if any)
    - src-tauri/src/commands.rs (new commands just added — verify exact function names)
  </read_first>
  <files>src-tauri/src/lib.rs</files>
  <action>
    Edit `src-tauri/src/lib.rs`:

    1. Add `ContextMenuState` to app state if needed for `show_repo_context_menu` (add `Arc<Mutex<Option<String>>>` for the active context menu repo path):
       - Define `pub struct ContextMenuRepo(pub Arc<Mutex<Option<String>>>);` in commands.rs or lib.rs
       - `.manage(ContextMenuRepo(Arc::new(Mutex::new(None))))` in the builder

    2. Register all new commands in `invoke_handler!`:
       Add to the `tauri::generate_handler![]` macro call:
       - `commands::set_tags`
       - `commands::add_tag`
       - `commands::remove_tag`
       - `commands::list_all_tags`
       - `commands::set_notes`
       - `commands::set_pin`
       - `commands::set_pin_order`
       - `commands::list_branches`
       - `commands::show_repo_context_menu`

    3. Register `on_menu_event` handler inside the `.setup(|app| { ... })` closure (or after builder, depending on existing pattern):
       ```rust
       app.on_menu_event(move |app, event| {
           let event_id = event.id().as_ref().to_string();
           // Retrieve the repo_path from ContextMenuRepo state
           let state = app.state::<ContextMenuRepo>();
           let repo_path = state.0.lock()
               .ok()
               .and_then(|g| g.clone())
               .unwrap_or_default();
           // Emit typed event for Svelte to handle
           let _ = app.emit("repo-context-action", serde_json::json!({
               "action": event_id,
               "repo_path": repo_path,
           }));
       });
       ```
       The event payload `{ action: string, repo_path: string }` is what Svelte will listen for.
       `serde_json` is already in the workspace. If not a direct dep of workpot-tray, add `serde_json = { workspace = true }`.

    4. `cargo build --package workpot-tray` must exit 0.
  </action>
  <verify>
    <automated>cargo build --package workpot-tray 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo build --package workpot-tray` exits 0.
    `lib.rs` `invoke_handler!` contains `commands::set_tags` and `commands::list_branches`.
    `lib.rs` contains `on_menu_event` handler that calls `app.emit("repo-context-action", ...)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Svelte webview → Tauri command | User-controlled tag/notes input crosses this boundary |
| Tauri command → org.rs | Validated strings pass to SQLite layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | set_tags — tag contains `#` reserved char | mitigate | Validate in command: reject tags containing `#`; return `Err("tag may not contain '#'")` |
| T-05-04-02 | Tampering | set_notes — notes > 500 chars | mitigate | Validate in command: `if notes.len() > 500 { return Err("notes exceed 500 characters") }` per D-25 |
| T-05-04-03 | Tampering | set_tags — tag > 64 chars causing DB bloat | mitigate | Validate in command: `if tag.trim().len() > 64 { return Err("tag too long") }` |
| T-05-04-04 | Tampering | list_branches — arbitrary path injection | mitigate | git2::Repository::open fails on non-git paths; error mapped to Err string; no shell exec involved |
| T-05-04-05 | Spoofing | repo_path in org commands not verified | mitigate | org.rs returns WorkpotError::NotFound for unknown paths; mapped to Err in command handler |
| T-05-04-06 | Tampering | show_repo_context_menu — menu event fires for wrong repo | mitigate | ContextMenuRepo state stores active path per popup; cleared after event consumed |
| T-05-04-SC | Tampering | npm/cargo installs | accept | No new npm packages; git2/serde_json already in workspace |
</threat_model>

<verification>
After both tasks:
1. `cargo build --package workpot-tray` exits 0
2. `grep -c "commands::set_tags" src-tauri/src/lib.rs` ≥ 1
3. `grep -c "on_menu_event" src-tauri/src/lib.rs` ≥ 1
4. `grep -c "pub fn set_tags" src-tauri/src/commands.rs` ≥ 1
5. `grep -c "pub async fn list_branches" src-tauri/src/commands.rs` ≥ 1
6. `grep -c "tags: Vec<String>" src-tauri/src/commands.rs` ≥ 1
</verification>

<success_criteria>
- All 9 new IPC commands compile and are registered
- record_to_dto maps all 5 Phase 5 RepoDto fields
- Input validation rejects tags > 64 chars, tags with #, notes > 500 chars
- MenuEvent handler emits repo-context-action event for Svelte to consume
- list_branches returns local branch names via git2 on demand
- cargo build --package workpot-tray exits 0
</success_criteria>

<output>
Create `.planning/phases/05-tags-prioritization/05-04-SUMMARY.md` when done.
</output>

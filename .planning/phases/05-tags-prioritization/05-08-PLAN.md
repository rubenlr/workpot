---
phase: 05-tags-prioritization
plan: 08
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-06
gap_closure: true
files_modified:
  - src-tauri/permissions/tray-commands.toml
  - src-tauri/capabilities/default.json
autonomous: false
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04

must_haves:
  truths:
    - "invoke set_pin, add_tag, remove_tag, set_notes, set_pin_order, list_branches, list_all_tags, show_repo_context_menu succeeds from panel webview (no 'not allowed. Command not found')"
    - "Detail pane opens via Right arrow and loads branches without IPC error"
    - "Pinned drag-to-reorder persists pin_order after tray reload"
    - "Context menu Pin/Unpin and Add/Remove tag mutate tray list without IPC error"
    - "Typing # in filter shows TagAutocomplete; selecting a tag filters with AND logic"
  artifacts:
    - path: src-tauri/permissions/tray-commands.toml
      provides: Tauri 2 ACL permission allowing all Phase 5 org IPC commands
      contains: allow-org-commands
    - path: src-tauri/capabilities/default.json
      provides: panel capability grants allow-org-commands
      contains: allow-org-commands
  decisions_covered:
    - D-06
    - D-09
    - D-11
    - D-12
    - D-14
    - D-16
  key_links:
    - from: src-tauri/capabilities/default.json
      to: src-tauri/permissions/tray-commands.toml
      via: permissions array references allow-org-commands identifier
      pattern: allow-org-commands
    - from: src/lib/components/DetailPane.svelte
      to: src-tauri/src/commands.rs
      via: invoke list_branches, set_pin, add_tag, remove_tag, set_notes, list_all_tags
      pattern: invoke\("set_pin"
    - from: src/routes/+page.svelte
      to: src-tauri/src/commands.rs
      via: invoke set_pin_order, set_pin, show_repo_context_menu, remove_tag
      pattern: invoke\("set_pin_order"
---

## Source audit (gap closure)

| SOURCE | ID | Feature | Plan | Status |
|--------|-----|---------|------|--------|
| VERIFICATION | human_needed | Tray UAT blocked on IPC | 08 | COVERED |
| UAT | test 1 | Detail pane keyboard + list_branches | 08 | COVERED |
| UAT | test 2 | Pinned drag set_pin_order | 08 | COVERED |
| UAT | test 3 | Context menu set_pin / tags | 08 | COVERED |
| UAT | test 4 | # autocomplete add_tag path | 08 | COVERED |
| ROADMAP | ORG-01..04 | Tray org features end-to-end | 08 | COVERED |
| CONTEXT | D-06 | Context menu IPC | 08 | COVERED |
| CONTEXT | D-16 | Drag pin_order IPC | 08 | COVERED |

**Root cause (from 05-HUMAN-UAT.md):** Commands exist in `src-tauri/src/lib.rs` `generate_handler!` but are absent from `src-tauri/capabilities/default.json` and `src-tauri/permissions/tray-commands.toml`. Tauri 2 blocks unlisted invokes with `{cmd} not allowed. Command not found`.

---

<objective>
Close Phase 5 human UAT blocker: register all org-related IPC commands in the Tauri 2 capability/permission system so the tray webview can invoke them.

Purpose: 05-04 implemented commands and 05-06 wired Svelte; runtime ACL was never updated (same class of bug flagged in 04-REVIEWS.md for `get_tray_config`).
Output: One consolidated `allow-org-commands` permission + capability entry; `cargo build -p workpot-tray` green; human re-runs 05-HUMAN-UAT.md (4 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tags-prioritization/05-VERIFICATION.md
@.planning/phases/05-tags-prioritization/05-HUMAN-UAT.md
@.planning/phases/05-tags-prioritization/05-04-PLAN.md
@.planning/phases/04-tray-finder-mvp/04-REVIEWS.md (capabilities omission pattern)
@src-tauri/permissions/tray-commands.toml
@src-tauri/capabilities/default.json
@src-tauri/src/lib.rs
@src/lib/components/DetailPane.svelte
@src/routes/+page.svelte
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel webview → IPC | Only `panel` window has these permissions; commands mutate local SQLite via AppContext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Elevation | org IPC surface | accept | Same trust model as existing `allow-list-repos`; local-only app, no network |
| T-05-08-02 | Tampering | list_branches git2 read | accept | Read-only branch enumeration; repo path already validated in core |

No new dependencies. No change to command implementations in `commands.rs`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add allow-org-commands Tauri permission + capability</name>
  <read_first>
    - src-tauri/permissions/tray-commands.toml (existing allow-list-repos pattern)
    - src-tauri/capabilities/default.json (permissions array)
    - src-tauri/src/lib.rs (full generate_handler list — must match commands.allow exactly)
    - src/lib/components/DetailPane.svelte (invoke names: list_branches, list_all_tags, set_pin, add_tag, remove_tag, set_notes)
    - src/routes/+page.svelte (invoke names: set_pin_order, set_pin, show_repo_context_menu, remove_tag)
  </read_first>
  <files>src-tauri/permissions/tray-commands.toml, src-tauri/capabilities/default.json</files>
  <action>
    1. Append to `src-tauri/permissions/tray-commands.toml` a new `[[permission]]` block:
       - `identifier = "allow-org-commands"`
       - `description = "Allows Phase 5 org IPC (tags, notes, pins, branches, context menu)."`
       - `commands.allow` MUST include exactly these command names (snake_case, match lib.rs):
         `set_tags`, `add_tag`, `remove_tag`, `list_all_tags`, `set_notes`, `set_pin`, `set_pin_order`, `list_branches`, `show_repo_context_menu`

    2. Append `"allow-org-commands"` to the `permissions` array in `src-tauri/capabilities/default.json` (after existing allow-* entries, before closing bracket).

    3. Do NOT remove or rename existing Phase 4 permissions.

    4. Do NOT modify `commands.rs` or `lib.rs` unless build proves a command name mismatch.
  </action>
  <acceptance_criteria>
    - `tray-commands.toml` contains `identifier = "allow-org-commands"` with all 9 commands in `commands.allow`
    - `default.json` `permissions` array includes `"allow-org-commands"`
    - `cargo build -p workpot-tray` exits 0
    - `rg 'allow-org-commands' src-tauri/permissions/tray-commands.toml src-tauri/capabilities/default.json` shows both files
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p workpot-tray && cargo test --workspace</automated>
  </verify>
  <done>
    `allow-org-commands` permission lists all nine org IPC commands; `default.json` grants it to the panel window; build and workspace tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run Phase 5 human UAT (05-HUMAN-UAT.md)</name>
  <read_first>
    - .planning/phases/05-tags-prioritization/05-HUMAN-UAT.md (four tests + expected results)
    - .planning/phases/05-tags-prioritization/05-VERIFICATION.md (human verification section)
  </read_first>
  <files>.planning/phases/05-tags-prioritization/05-HUMAN-UAT.md</files>
  <action>
    Human re-runs all four tray tests after rebuilding the tray app. Update each test `result` and `result_note` in 05-HUMAN-UAT.md; set Summary counts. No code changes unless a test still fails.
  </action>
  <what-built>Tauri ACL now allows org IPC from the tray panel webview.</what-built>
  <how-to-verify>
    Rebuild and launch tray app (`cargo tauri dev` or installed build). Run all four tests in 05-HUMAN-UAT.md:

    1. Detail pane: focus repo, Right → detail pane with branches/tags/notes/pin; Left/Esc → list. No console error containing "not allowed" or "Command not found".

    2. Pin reorder: pin 3 repos, drag middle to top in Pinned section, quit and reopen tray — order persists.

    3. Context menu: right-click repo → Pin/Unpin, Add tag, Remove tag — list updates without IPC errors.

    4. # autocomplete: type `#` in filter, pick tag with arrow+Enter — AND filter applies; chips hidden until `#` typed.

    Update 05-HUMAN-UAT.md result fields (passed/issue) and Summary counts.
  </how-to-verify>
  <resume-signal>Type "approved" when all four tests pass, or describe remaining failures.</resume-signal>
</task>

</tasks>

<verification>
1. `cargo build -p workpot-tray` exits 0
2. `cargo test --workspace` exits 0 (no regressions)
3. Human UAT: 4/4 tests in 05-HUMAN-UAT.md marked passed
4. Re-run `/gsd-verify-phase 5` — expect `status: passed` after UAT green
</verification>

<success_criteria>
- All Phase 5 org `invoke()` calls from Svelte succeed at runtime on macOS tray build
- 05-HUMAN-UAT.md shows 4 passed, 0 issues
- Phase 5 verification can move from `human_needed` to `passed`
</success_criteria>

<output>
After completion, create `.planning/phases/05-tags-prioritization/05-08-SUMMARY.md`
</output>

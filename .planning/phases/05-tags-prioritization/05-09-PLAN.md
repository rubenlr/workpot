---
phase: 05-tags-prioritization
plan: 09
type: execute
wave: 5
depends_on:
  - 05-06
  - 05-08
gap_closure: true
files_modified:
  - src/routes/+page.svelte
  - src/lib/components/DetailPane.svelte
  - src/lib/orgClient.ts
  - src/lib/orgClient.test.ts
autonomous: true
requirements:
  - ORG-01
must_haves:
  truths:
    - "Adding a tag in DetailPane (Enter or blur) persists to DB and appears on list + detail chips after refresh"
    - "Duplicate tag add shows explicit feedback; tag chips update without reopening tray"
    - "Context menu Remove tag removes a tag when repo has exactly one tag; multi-tag opens detail for pick"
    - "Context menu Add tag opens detail and focuses tag input"
  artifacts:
    - path: src/lib/components/DetailPane.svelte
      provides: tag add on Enter+blur, duplicate feedback, optional focus request
      contains: handleAddTag
    - path: src/routes/+page.svelte
      provides: refreshReposAndDetail reloads allTags; context menu tag actions
      contains: refreshReposAndDetail
  key_links:
    - from: src/lib/components/DetailPane.svelte
      to: src-tauri add_tag
      via: invoke on Enter/blur
      pattern: invoke\("add_tag"
---

## Source audit (gap closure)

| SOURCE | ID | Feature | Plan | Status |
|--------|-----|---------|------|--------|
| UAT | Gaps | tags not saved or edited | 09 | COVERED |
| VERIFICATION | ORG-01 | tag assign + filter | 09 | COVERED |

**Root cause (from UAT + code review):**
- `refreshReposAndDetail` reloads `list_repos` but not `list_all_tags`, so autocomplete/catalog can lag.
- Tag input only commits on Enter; blur does nothing — users expect blur-save (notes already blur-save).
- `add_tag` uses `INSERT OR IGNORE` — duplicate adds succeed silently with no new chip (looks like "not saved").
- Context menu `remove_tag` only sets `detailRepo` (per 05-06) with no removal when user expected menu action.
- `onPanelKeydown` should ignore events from detail-pane inputs/textareas before list handlers (belt-and-suspenders).

---

<objective>
Close UAT gap: tray tags must persist and be editable from the detail pane and context menu.

Purpose: ORG-01 is not satisfied if users cannot reliably add/remove tags in the tray after ACL fix (05-08).
Output: UI fixes + Vitest coverage; human re-runs tag-related UAT items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tags-prioritization/05-HUMAN-UAT.md
@.planning/phases/05-tags-prioritization/05-VERIFICATION.md
@.planning/phases/05-tags-prioritization/05-06-PLAN.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@src/routes/+page.svelte
@src/lib/components/DetailPane.svelte
@src/lib/orgClient.ts
@src/lib/detailRepoSync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reliable tag refresh and panel keyboard guard</name>
  <read_first>
    - src/routes/+page.svelte (refreshReposAndDetail, onPanelKeydown, repo-context-action)
    - src/lib/detailNavigation.ts
  </read_first>
  <files>src/routes/+page.svelte</files>
  <action>
    1. Change `refreshReposAndDetail` to:
       - `await loadRepos(clearError)`
       - `await loadAllTags()` (always, not only on mount)
       - `detailRepo = resyncDetailIfOpen(repos, detailRepo)`

    2. At top of `onPanelKeydown`, if `detailRepo !== null` and `e.target` is `HTMLInputElement` or `HTMLTextAreaElement` that is NOT `#repo-filter`, return immediately (detail pane owns keyboard).

    3. Context menu handler for `remove_tag`:
       - If repo has exactly one tag: `await invoke("remove_tag", ...)`, then `await refreshReposAndDetail()`.
       - Else: set `detailRepo = repo` and pass `focusTagInput={true}` to DetailPane (Task 2).

    4. Context menu `add_tag`: set `detailRepo = repo` and `focusTagInput={true}`.

    5. Add state `let focusTagOnDetailOpen = $state(false)`; pass to DetailPane as `requestTagFocus={focusTagOnDetailOpen}`; reset to false in DetailPane after focus (callback `onTagFocusDone`).
  </action>
  <acceptance_criteria>
    - `refreshReposAndDetail` calls `loadAllTags` after `loadRepos`
    - `onPanelKeydown` returns early for detail-pane input/textarea targets
    - Single-tag `remove_tag` from context menu invokes IPC without opening detail only
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && npm run check && npm test</automated>
  </verify>
  <done>Repo list and tag catalog stay in sync after every org mutation.</done>
</task>

<task type="auto">
  <name>Task 2: DetailPane tag commit, duplicate feedback, focus</name>
  <read_first>
    - src/lib/components/DetailPane.svelte
    - src/lib/orgClient.ts
    - crates/workpot-core/src/services/org.rs (add_tag INSERT OR IGNORE behavior)
  </read_first>
  <files>src/lib/components/DetailPane.svelte, src/lib/orgClient.ts, src/lib/orgClient.test.ts</files>
  <action>
    1. Add `orgClient.ts` helper `tagAlreadyOnRepo(tag: string, repoTags: string[]): boolean` (case-sensitive match per DB collation).

    2. In `handleAddTag`:
       - If tag already on `repo.tags`, set `tagError = "Tag already on this repo"` and return (do not invoke).
       - After successful `add_tag`, call `onMutated()` and clear input.
       - On invoke error, show message.

    3. On tag input `onblur`, call `handleAddTag(tagInput)` when trimmed non-empty (same validation).

    4. Props: `requestTagFocus?: boolean`, `onTagFocusDone?: () => void`. `$effect` when `requestTagFocus` true: focus tag input, call `onTagFocusDone`.

    5. Vitest: `orgClient.test.ts` cases for duplicate detection helper.
  </action>
  <acceptance_criteria>
    - Blur on non-empty tag input triggers add path
    - Duplicate tag shows "Tag already on this repo" without IPC
    - `orgClient.test.ts` passes
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && npm test -- orgClient</automated>
  </verify>
  <done>Users can add tags via Enter or blur; duplicates are visible failures, not silent no-ops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-verify tag UAT items</name>
  <read_first>
    - .planning/phases/05-tags-prioritization/05-HUMAN-UAT.md
  </read_first>
  <files>.planning/phases/05-tags-prioritization/05-HUMAN-UAT.md</files>
  <action>
    Rebuild tray. Verify:
    1. Detail pane: add tag via Enter and via blur — chip appears, survives Left/back to list.
    2. Context menu: Add tag focuses input; Remove tag on single-tag repo removes immediately.
    3. # filter still works (test 4).
    Update 05-HUMAN-UAT.md results and Summary.
  </action>
  <how-to-verify>
    `cargo tauri dev` — exercise tag add/remove in detail pane and context menu.
  </how-to-verify>
  <resume-signal>Type "approved" when tag save/edit works, or describe failure.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run check` and `npm test` pass
2. `cargo test --workspace` pass
3. Human UAT gap cleared in 05-HUMAN-UAT.md
</verification>

<success_criteria>
- User can add and remove tags from tray detail pane with visible persistence
- Context menu tag actions match D-06 minimum expectations
</success_criteria>

<output>
After completion, create `.planning/phases/05-tags-prioritization/05-09-SUMMARY.md`
</output>

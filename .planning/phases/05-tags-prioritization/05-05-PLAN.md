---
phase: 05-tags-prioritization
plan: 05
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - src/lib/components/DetailPane.svelte
  - src/lib/components/TagChip.svelte
  - src/lib/components/TagAutocomplete.svelte
  - src/lib/components/SectionHeader.svelte
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04

must_haves:
  truths:
    - "DetailPane accepts a RepoDto prop and renders branch list, tags, notes textarea, pin toggle"
    - "TagChip renders #tagname and calls onRemove on Cmd+Click (per D-05)"
    - "TagAutocomplete shows filtered dropdown when visible=true; arrow key + Enter selection works"
    - "SectionHeader renders a subtle gray uppercase section label"
    - "npm test exits 0 after adding components"
  artifacts:
    - path: "src/lib/components/DetailPane.svelte"
      provides: "Detail pane: branch list, tags (add/remove), notes textarea (blur save), pin toggle"
    - path: "src/lib/components/TagChip.svelte"
      provides: "Tag chip with Cmd+Click remove"
    - path: "src/lib/components/TagAutocomplete.svelte"
      provides: "Tag autocomplete dropdown for #tag input"
    - path: "src/lib/components/SectionHeader.svelte"
      provides: "Section header label (Pinned/Dirty/Recent/Rest)"
  key_links:
    - from: "src/lib/components/DetailPane.svelte"
      to: "src/lib/components/TagChip.svelte"
      via: "import TagChip"
      pattern: "TagChip"
    - from: "src/lib/components/DetailPane.svelte"
      to: "@tauri-apps/api/core"
      via: "invoke set_notes, add_tag, remove_tag, set_pin, list_branches"
      pattern: "invoke"
---

<objective>
Create the four new Svelte 5 components for Phase 5: DetailPane, TagChip, TagAutocomplete, and SectionHeader. These components are self-contained and can be built in parallel with Plan 04 (IPC commands), since they will use the IPC commands that will be registered by the time Plan 06 wires them into +page.svelte.

Purpose: Provides the component building blocks that Plan 06 (+page.svelte integration) will import and wire together. Each component is built against the RepoDto interface extended in Plan 03 and calls Tauri IPC commands from Plan 04.

Output: 4 new Svelte component files. `npm run check` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@.planning/phases/05-tags-prioritization/05-RESEARCH.md
@.planning/phases/05-tags-prioritization/05-PATTERNS.md
@src/routes/+page.svelte
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TagChip.svelte + TagAutocomplete.svelte + SectionHeader.svelte</name>
  <read_first>
    - src/routes/+page.svelte (full file — Svelte 5 runes patterns: $state, $derived, $props, invoke pattern, listen pattern, Tailwind dark: class usage, button onclick pattern, onkeydown pattern)
    - src/lib/types.ts (current RepoDto after Plan 03 extension)
  </read_first>
  <files>
    src/lib/components/TagChip.svelte,
    src/lib/components/TagAutocomplete.svelte,
    src/lib/components/SectionHeader.svelte
  </files>
  <action>
    Create the `src/lib/components/` directory if it does not exist.

    1. Create `src/lib/components/TagChip.svelte`:

       Script (Svelte 5 runes):
       ```
       let { tag, onRemove }: { tag: string; onRemove?: () => void } = $props();
       ```
       Template: A `<button type="button">` element with:
       - Text: `#{tag}`
       - `onclick={(e) => { if (e.metaKey && onRemove) onRemove(); }}`
       - Tailwind classes: `inline-flex items-center rounded-full bg-blue-100 px-2 py-0.5 text-xs font-medium text-blue-800 dark:bg-blue-900 dark:text-blue-200 cursor-default`
       - `title="Cmd+Click to remove"` attribute
       No remove button icon — Cmd+Click is the only remove gesture (per D-05).

    2. Create `src/lib/components/TagAutocomplete.svelte`:

       Script (Svelte 5 runes):
       ```
       let {
         allTags,
         visible,
         onSelect,
       }: { allTags: string[]; visible: boolean; onSelect: (tag: string) => void } = $props();

       let inputValue = $state("");
       let highlightedIndex = $state(-1);
       let filtered = $derived(
         inputValue.length === 0
           ? allTags
           : allTags.filter((t) => t.toLowerCase().startsWith(inputValue.toLowerCase()))
       );
       ```

       Keyboard handler on the input: ArrowDown increments highlightedIndex (capped at filtered.length-1), ArrowUp decrements (floor 0), Enter calls `onSelect(filtered[highlightedIndex])` if index valid, else calls `onSelect(inputValue)` if non-empty, then clears inputValue and resets index.

       Template: A `<div>` (only rendered when `visible`) containing:
       - A `<input type="text">` bound to `inputValue` for optional free-form tag entry
       - A `<ul>` listing `filtered` tags with `<li>` items; clicking a tag calls `onSelect(tag)`. Highlighted item styled with `bg-neutral-100 dark:bg-neutral-700`.
       - `onkeydown` on the wrapping div routes ArrowDown/ArrowUp/Enter as above.

       Tailwind classes for the container: `absolute z-10 mt-1 w-48 rounded-lg border border-neutral-200 bg-white py-1 shadow-lg dark:border-neutral-700 dark:bg-neutral-800`.

    3. Create `src/lib/components/SectionHeader.svelte`:

       Script (Svelte 5 runes):
       ```
       let { label }: { label: string } = $props();
       ```

       Template: A `<div>` with Tailwind: `px-3 py-1 text-xs font-semibold uppercase tracking-wide text-neutral-400 dark:text-neutral-500 select-none`
       Content: `{label}`

       No interactive behavior — purely presentational.
  </action>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run check` exits 0.
    Three files exist: `src/lib/components/TagChip.svelte`, `src/lib/components/TagAutocomplete.svelte`, `src/lib/components/SectionHeader.svelte`.
    TagChip calls onRemove on Cmd+Click.
    TagAutocomplete has keyboard nav (ArrowDown/Up/Enter).
    SectionHeader renders a plain label.
  </done>
</task>

<task type="auto">
  <name>Task 2: DetailPane.svelte</name>
  <read_first>
    - src/routes/+page.svelte (full file — invoke pattern, listen cleanup pattern, Svelte 5 runes patterns, Tailwind dark-mode classes, $effect for side effects, button + input patterns)
    - src/lib/types.ts (RepoDto interface — all fields)
    - src/lib/components/TagChip.svelte (just created — import path and prop interface)
  </read_first>
  <files>src/lib/components/DetailPane.svelte</files>
  <action>
    Create `src/lib/components/DetailPane.svelte`:

    Props (Svelte 5):
    ```
    let {
      repo,
      onClose,
      onMutated,
    }: {
      repo: import("../types").RepoDto;
      onClose: () => void;
      onMutated: () => void;
    } = $props();
    ```
    `onMutated` is called after any mutation (tag add/remove, pin toggle, notes save) to trigger a `list_repos` reload in the parent.

    State:
    - `let branches = $state<string[]>([])` — loaded on mount via list_branches IPC
    - `let branchError = $state<string | null>(null)`
    - `let notesValue = $state(repo.notes ?? "")`
    - `let tagInput = $state("")`
    - `let tagError = $state<string | null>(null)`
    - `let allTags = $state<string[]>([])` — loaded on mount via list_all_tags IPC

    On mount (`$effect(() => { ... })`):
    - Invoke `list_branches({ repoPath: repo.path })` → set branches (or branchError)
    - Invoke `list_all_tags()` → set allTags

    Structure (vertical flex column, `p-4 flex flex-col gap-4 h-full overflow-y-auto`):

    A. **Header row**: Close button (left arrow `←`) calls `onClose()` on click; repo name as `<h2>`.

    B. **Pin toggle section**: `<label>` containing `<input type="checkbox" checked={repo.pinned}>`; on change calls `invoke("set_pin", { repoPath: repo.path, pinned: !repo.pinned })` then `onMutated()`.

    C. **Branches section** (D-11 read-only):
    - Section heading: `Branches`
    - If branchError: show error text
    - `{#each branches as b}` — render `<span>` with branch name; current branch (`repo.branch`) highlighted with `font-semibold text-blue-600 dark:text-blue-400`

    D. **Tags section** (D-04):
    - Section heading: `Tags`
    - `{#each repo.tags as tag}` — render `<TagChip {tag} onRemove={() => handleRemoveTag(tag)} />`
    - An `<input type="text">` for adding a tag; on Enter (keydown): calls `handleAddTag(tagInput.trim())` then clears tagInput
    - `handleAddTag(tag)`: validate non-empty + not starting with `#`, then invoke `add_tag({ repoPath: repo.path, tag })` + `onMutated()`
    - `handleRemoveTag(tag)`: invoke `remove_tag({ repoPath: repo.path, tag })` + `onMutated()`
    - tagError displayed if present

    E. **Notes section** (D-24, D-25, D-26, D-27):
    - Section heading: `Notes`
    - `<textarea rows="3" maxlength="500" class="... resize-none" bind:value={notesValue} onblur={handleNotesSave}>`
    - `handleNotesSave()`: invoke `set_notes({ repoPath: repo.path, notes: notesValue.trim() || null })`; call `onMutated()` only if notes value changed
    - CSS max-height from 5 lines: `style="max-height: calc(5 * 1.5rem)"` (line-height ~1.5rem per Tailwind default)
    - `placeholder="Add notes..."`, `class="w-full rounded-md border border-neutral-200 bg-transparent p-2 text-sm dark:border-neutral-700 focus:outline-none focus:ring-1 focus:ring-blue-500"`

    Import TagChip: `import TagChip from "./TagChip.svelte";`
    Import invoke: `import { invoke } from "@tauri-apps/api/core";`

    Keyboard handler: an `onkeydown` on the entire pane container — if `e.key === "Escape"` and focus is NOT in the notes textarea, call `onClose()`.
  </action>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run check` exits 0.
    `src/lib/components/DetailPane.svelte` exists.
    File contains `invoke("set_pin"`, `invoke("add_tag"`, `invoke("set_notes"`.
    File imports TagChip.
    Notes textarea has `maxlength="500"` and `onblur` handler.
    Pin toggle has `type="checkbox"`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (tag text, notes) → invoke() | Unvalidated user strings enter IPC boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | DetailPane tag input — user can type any string | mitigate | Client-side: reject tags starting with `#`, empty after trim; server validates max 64 chars (Plan 04) |
| T-05-05-02 | Tampering | Notes textarea — user can type >500 chars | mitigate | `maxlength="500"` attribute enforces at HTML level; server validates in set_notes command (Plan 04) |
| T-05-05-03 | Tampering | XSS via tag/notes rendered in webview | accept | Svelte auto-escapes {value} interpolations; D-27 notes are plain text; no @html used |
| T-05-05-04 | Tampering | list_branches called with unvalidated repo_path | accept | repo_path is taken from already-loaded RepoDto; not user-typed; git2 open fails safely on non-repos |
| T-05-05-SC | Tampering | npm/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After both tasks:
1. `cd /Users/rubenlr/c/workpot && npm run check` exits 0
2. `ls src/lib/components/` lists all 4 component files
3. `grep -c "onRemove" src/lib/components/TagChip.svelte` ≥ 1
4. `grep -c "maxlength" src/lib/components/DetailPane.svelte` ≥ 1
5. `grep -c "set_pin" src/lib/components/DetailPane.svelte` ≥ 1
6. `npm test` exits 0 (no TypeScript regressions from new components)
</verification>

<success_criteria>
- All 4 component files exist and pass `npm run check`
- DetailPane renders: branch list (read-only), tags with Cmd+Click remove + add-tag input, notes textarea (blur save, maxlength=500), pin toggle
- TagChip renders #tag and responds to Cmd+Click with onRemove callback
- TagAutocomplete shows filtered dropdown with keyboard navigation
- SectionHeader renders a styled uppercase section label
</success_criteria>

<output>
Create `.planning/phases/05-tags-prioritization/05-05-SUMMARY.md` when done.
</output>

---
phase: 05-tags-prioritization
plan: 07
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - ORG-01

must_haves:
  truths:
    - "workpot tag add <repo-name-or-path> <tag> exits 0 and adds the tag"
    - "workpot tag remove <repo-name-or-path> <tag> exits 0 and removes the tag"
    - "workpot tag list <repo-name-or-path> prints one tag per line to stdout"
    - "workpot tag add with empty tag or tag >64 chars exits non-zero with error"
  artifacts:
    - path: "crates/workpot-cli/src/main.rs"
      provides: "Tag subcommand: add, remove, list sub-subcommands"
      contains: "Tag"
  decisions_covered:
    - D-07  # CLI tag subcommand (add/remove/list) is Phase 5 scope
    - D-18  # pin CLI management deferred (only tag CLI in this phase)
  key_links:
    - from: "crates/workpot-cli/src/main.rs"
      to: "crates/workpot-core/src/lib.rs"
      via: "AppContext::add_tag / remove_tag / list_tags_for_repo"
      pattern: "ctx\\.add_tag"
---

<objective>
Add the `workpot tag` CLI subcommand with three sub-subcommands: `add`, `remove`, and `list`. This fulfills D-07 (CLI tag management is Phase 5 scope) and completes ORG-01 for CLI consumers.

Purpose: Users managing many repos need CLI access to tags for scripting and terminal workflows. The implementation is a straightforward clap subcommand extension following the existing CLI pattern.

Output: 1 file modified (main.rs). `workpot tag add`, `workpot tag remove`, `workpot tag list` all work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@crates/workpot-cli/src/main.rs
@crates/workpot-core/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: workpot tag subcommand (add / remove / list)</name>
  <read_first>
    - crates/workpot-cli/src/main.rs (full file — existing Commands enum, existing subcommands e.g. Repo, Index, Roots; how AppContext is opened; how errors are handled; clap derive patterns)
    - crates/workpot-core/src/lib.rs (AppContext::add_tag, remove_tag, list_tags_for_repo method signatures)
  </read_first>
  <files>crates/workpot-cli/src/main.rs</files>
  <behavior>
    - `workpot tag add <path-or-name> <tag>` exits 0, prints nothing on success. Exits 1 with message "tag cannot be empty" if tag is blank. Exits 1 with "tag too long (max 64 chars)" if tag > 64 chars. Exits 1 with "tag may not contain '#'" if tag starts with `#`.
    - `workpot tag remove <path-or-name> <tag>` exits 0, prints nothing on success.
    - `workpot tag list <path-or-name>` prints each tag on its own line to stdout, or "(no tags)" if empty.
    - `workpot tag` with no subcommand prints help and exits 2.
    - `<path-or-name>` is passed directly to AppContext methods as a string (the AppContext internally handles resolution; if NotFound, print error and exit 1).
  </behavior>
  <action>
    Edit `crates/workpot-cli/src/main.rs`:

    1. Add a `Tag` variant to the top-level `Commands` enum (follow the same pattern as existing variants like `Repo`, `Index`):
       ```rust
       Tag {
           #[command(subcommand)]
           action: TagAction,
       },
       ```

    2. Define `TagAction` enum with `#[derive(Subcommand)]`:
       ```rust
       #[derive(Subcommand)]
       enum TagAction {
           Add {
               /// Repository path or name
               repo: String,
               /// Tag to add (max 64 chars, no #)
               tag: String,
           },
           Remove {
               repo: String,
               tag: String,
           },
           List {
               repo: String,
           },
       }
       ```

    3. In the match block that handles `Commands`, add the `Commands::Tag { action }` arm:
       - Open AppContext with `AppContext::open()?` (same as all other commands)
       - For `TagAction::Add { repo, tag }`:
         - Validate: `tag.trim().is_empty()` → `eprintln!("tag cannot be empty"); std::process::exit(1);`
         - `tag.len() > 64` → `eprintln!("tag too long (max 64 chars)"); std::process::exit(1);`
         - `tag.starts_with('#')` → `eprintln!("tag may not contain '#'"); std::process::exit(1);`
         - `ctx.add_tag(&repo, tag.trim())?;` (use `?` — anyhow propagation)
       - For `TagAction::Remove { repo, tag }`:
         - `ctx.remove_tag(&repo, &tag)?;`
       - For `TagAction::List { repo }`:
         - `let tags = ctx.list_tags_for_repo(&repo)?;`
           Wait — `list_tags_for_repo` takes `&str` repo path. Check AppContext public API from lib.rs to see if there's a name-to-path lookup or if the path must be exact. If AppContext only accepts canonical paths: resolve by checking `ctx.list_repos()` to find a repo by name prefix, then use its path. If AppContext accepts name strings directly, use it directly. Read lib.rs carefully to determine the right approach.
         - If tags is empty: `println!("(no tags)");`
         - Else: for each tag: `println!("{}", tag);`

    Follow the existing error handling pattern in main.rs — use `anyhow::Result` and `?` for AppContext errors; print to stderr and exit(1) for validation errors.

    `cargo test -p workpot-cli` and `cargo build` must pass after changes.
  </action>
  <verify>
    <automated>cargo build --package workpot-cli 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo build --package workpot-cli` exits 0.
    `main.rs` contains `TagAction` enum with `Add`, `Remove`, `List` variants.
    `main.rs` contains input validation for tag (empty, length, # prefix).
    `cargo test --workspace` exits 0 (no regressions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → AppContext::add_tag | Tag value from shell args; validated before DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | workpot tag add with oversized tag | mitigate | CLI validates: empty check, >64 chars check, # prefix check before calling AppContext |
| T-05-07-02 | Tampering | repo path injection via shell arg | accept | Path passed to AppContext::open() which validates against DB; NotFound error surfaced to user |
| T-05-07-SC | Tampering | cargo installs | accept | No new packages |
</threat_model>

<verification>
After task:
1. `cargo build --package workpot-cli` exits 0
2. `cargo test --workspace` exits 0
3. `grep -c "TagAction" crates/workpot-cli/src/main.rs` ≥ 1
4. `grep -c "tag cannot be empty" crates/workpot-cli/src/main.rs` ≥ 1
</verification>

<success_criteria>
- `workpot tag add <repo> <tag>` adds a tag and exits 0
- `workpot tag remove <repo> <tag>` removes a tag and exits 0
- `workpot tag list <repo>` prints tags or "(no tags)"
- Input validation rejects empty, too-long, and # tags
- cargo build --package workpot-cli exits 0; cargo test --workspace exits 0
</success_criteria>

<output>
Create `.planning/phases/05-tags-prioritization/05-07-SUMMARY.md` when done.
</output>

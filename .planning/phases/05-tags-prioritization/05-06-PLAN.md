---
phase: 05-tags-prioritization
plan: 06
type: execute
wave: 3
depends_on:
  - 05-04
  - 05-05
files_modified:
  - src/routes/+page.svelte
autonomous: false
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04

must_haves:
  truths:
    - "Tray list shows four labeled sections: Pinned, Dirty, Recent, Rest"
    - "Right arrow on repo row opens DetailPane; left arrow/Esc closes it"
    - "Tag filter #backend hides repos without 'backend' tag from all sections"
    - "Typing # in filter bar shows TagAutocomplete dropdown"
    - "Right-click context menu shows Pin/Unpin + Add tag + Remove tag"
    - "repo-context-action Tauri event triggers pin or tag mutations and reloads"
    - "Pinned repos drag-to-reorder updates pin_order via set_pin_order IPC"
    - "Notes field included in fuzzy filter (plain text query matches note content)"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Updated tray panel with 4-section rendering, detail pane, drag-to-reorder, context menu, #tag autocomplete"
      contains: "filterAndSectionRepos"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/trayList.ts"
      via: "filterAndSectionRepos"
      pattern: "filterAndSectionRepos"
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/DetailPane.svelte"
      via: "import DetailPane"
      pattern: "DetailPane"
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/SectionHeader.svelte"
      via: "import SectionHeader"
      pattern: "SectionHeader"
---

<objective>
Wire all Phase 5 features into +page.svelte: replace the flat repo list with a four-section grouped view (Pinned / Dirty / Recent / Rest), add detail pane navigation (right arrow open / left arrow + Esc close), implement drag-to-reorder for the Pinned section, add the #tag autocomplete dropdown on # keystroke, wire the right-click context menu event listener, and ensure notes are included in the fuzzy filter.

Purpose: This is the final Svelte integration plan — it consumes all components (Plan 05) and IPC commands (Plan 04) and produces the fully working Phase 5 tray UI. The checkpoint after implementation lets the user verify the visual and interactive behavior before Phase 5 is marked complete.

Output: 1 file modified (+page.svelte). Tray app is functionally complete for all ORG requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@.planning/phases/05-tags-prioritization/05-RESEARCH.md
@.planning/phases/05-tags-prioritization/05-PATTERNS.md
@src/routes/+page.svelte
@src/lib/types.ts
@src/lib/trayList.ts
@src/lib/sort.ts
@src/lib/tagFilter.ts
@src/lib/pinOrder.ts
@src/lib/components/DetailPane.svelte
@src/lib/components/TagChip.svelte
@src/lib/components/TagAutocomplete.svelte
@src/lib/components/SectionHeader.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-section rendering + detail pane navigation + #tag autocomplete</name>
  <read_first>
    - src/routes/+page.svelte (full file — all current state, logic, and template structure; read every line before modifying)
    - src/lib/trayList.ts (filterAndSectionRepos signature and SectionedRepos shape)
    - src/lib/sort.ts (SectionConfig interface)
    - src/lib/tagFilter.ts (parseTagFilter for detecting # in query)
    - src/lib/components/DetailPane.svelte (props: repo, onClose, onMutated)
    - src/lib/components/SectionHeader.svelte (props: label)
    - src/lib/components/TagAutocomplete.svelte (props: allTags, visible, onSelect)
    - src/lib/types.ts (RepoDto including all Phase 5 fields)
  </read_first>
  <files>src/routes/+page.svelte</files>
  <action>
    Read the entire current +page.svelte before making any changes. Then make ALL changes listed below in a single edit:

    **New imports to add:**
    - `import { filterAndSectionRepos } from "../lib/trayList";`
    - `import type { SectionConfig } from "../lib/sort";`
    - `import { parseTagFilter } from "../lib/tagFilter";`
    - `import DetailPane from "../lib/components/DetailPane.svelte";`
    - `import SectionHeader from "../lib/components/SectionHeader.svelte";`
    - `import TagAutocomplete from "../lib/components/TagAutocomplete.svelte";`
    - Keep all existing imports

    **New state to add (Svelte 5 $state):**
    - `let detailRepo = $state<import("../lib/types").RepoDto | null>(null)` — currently open detail pane repo
    - `let allTags = $state<string[]>([])` — loaded on mount via `list_all_tags` IPC
    - `let dragSourceIdx = $state<number | null>(null)` — drag state for pin reorder

    **Replace displayRepos $derived:**
    - Remove the current `displayRepos` derived value (which calls `filterAndSortRepos`)
    - Add: `let trayConfig = $derived<SectionConfig>({ maxRecentDays: trayConfig_raw?.max_recent_days ?? 14, minRecentCount: trayConfig_raw?.min_recent_count ?? 3 })`
      Note: `get_tray_config` already returns a `TrayConfig` object — read what fields it returns and map accordingly. If it does not return max_recent_days/min_recent_count, use hardcoded defaults (14, 3) per CONTEXT.md Claude's Discretion.
    - Add: `let sectionedRepos = $derived(filterAndSectionRepos(repos, filterQuery, trayConfig))`
    - Add: `let activeTagsDetected = $derived(parseTagFilter(filterQuery).activeTags.length > 0 || filterQuery.includes("#"))`

    **Load allTags on mount:**
    In the existing `onMount` / `$effect` that loads repos, also invoke `list_all_tags()` and store in `allTags`. Pattern: `allTags = await invoke<string[]>("list_all_tags");`

    **Keyboard navigation update:**
    The selection index now spans ALL visible repos across all sections. Create a `flatVisible` derived value:
    `let flatVisible = $derived([...sectionedRepos.pinned, ...sectionedRepos.dirty, ...sectionedRepos.recent, ...sectionedRepos.rest])`
    Replace all references to `displayRepos[selectedIndex]` with `flatVisible[selectedIndex]`.
    Update selectedIndex max bound to use `flatVisible.length - 1`.

    In `onFilterKeydown` and `onPanelKeydown` keyboard handlers:
    - Add `ArrowRight`: if selected repo exists, set `detailRepo = flatVisible[selectedIndex]`; `e.preventDefault()`
    - Add `ArrowLeft`: if `detailRepo !== null`, set `detailRepo = null`; `e.preventDefault()`
    - `Escape` already closes panel — do not change that; but also clear `detailRepo = null` on Escape

    **Template structure — replace flat list with sectioned list:**

    The current `{#each displayRepos as repo, i (repo.path)}` block becomes four section groups.

    When `detailRepo !== null`, render `<DetailPane repo={detailRepo} onClose={() => detailRepo = null} onMutated={loadRepos} />` instead of the repo list (full-pane replacement, not side-by-side).

    When `detailRepo === null`:
    - Render the `<TagAutocomplete allTags={allTags} visible={activeTagsDetected} onSelect={(tag) => { filterQuery = filterQuery.replace(/#\w*$/, "") + "#" + tag + " "; }} />`
      Position it relative to the filter input (use `relative` on the filter wrapper, `absolute` positioning on the autocomplete).
    - Render four section groups, each with: `{#if sectionedRepos.SECTION.length > 0}` guard, `<SectionHeader label="Pinned" />` (or Dirty/Recent/Rest), then `{#each sectionedRepos.SECTION as repo, i (repo.path)}` rendering the existing repo `<button>` element.
    - For each repo button, the `selected` highlight logic uses the same `selectedIndex === flatVisible.indexOf(repo)` check (or track per-section index offset).
    - Add `oncontextmenu` to repo `<button>`: `(e) => { e.preventDefault(); void invoke("show_repo_context_menu", { repoPath: repo.path, isPinned: repo.pinned, tags: repo.tags }); }`
    - In Pinned section only: add `draggable="true"` attribute, `ondragstart={(e) => handleDragStart(e, i)}`, `ondragover={(e) => e.preventDefault()}`, `ondrop={(e) => handleDrop(e, i)}` on each pinned repo button.

    **Drag handler functions (add to script):**
    ```
    function handleDragStart(e: DragEvent, idx: number) {
      dragSourceIdx = idx;
      e.dataTransfer!.effectAllowed = "move";
    }
    async function handleDrop(e: DragEvent, targetIdx: number) {
      e.preventDefault();
      if (dragSourceIdx === null || dragSourceIdx === targetIdx) { dragSourceIdx = null; return; }
      const { reorderPinned, toPinOrderPayload } = await import("../lib/pinOrder");
      const newOrder = reorderPinned(sectionedRepos.pinned, dragSourceIdx, targetIdx);
      dragSourceIdx = null;
      await invoke("set_pin_order", { items: toPinOrderPayload(newOrder) });
      await loadRepos();
    }
    ```
    Import `reorderPinned` and `toPinOrderPayload` from `"../lib/pinOrder"` at top of script (not dynamic import).

    **repo-context-action event listener (add in onMount / $effect):**
    ```
    const unlistenContextAction = listen<{ action: string; repo_path: string }>("repo-context-action", async (event) => {
      const { action, repo_path } = event.payload;
      if (action === "pin") {
        const repo = repos.find(r => r.path === repo_path);
        if (repo) await invoke("set_pin", { repoPath: repo_path, pinned: !repo.pinned });
        await loadRepos();
      } else if (action === "add_tag") {
        // Open detail pane for the repo so user can add tag via DetailPane
        const repo = repos.find(r => r.path === repo_path);
        if (repo) detailRepo = repo;
      } else if (action === "remove_tag") {
        const repo = repos.find(r => r.path === repo_path);
        if (repo) detailRepo = repo;
      }
    });
    ```
    Add cleanup in the return of onMount: `void unlistenContextAction.then(fn => fn());`
  </action>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run check` exits 0.
    `+page.svelte` imports `filterAndSectionRepos`.
    `+page.svelte` contains `DetailPane`, `SectionHeader`, `TagAutocomplete` component usage.
    `+page.svelte` has `handleDragStart` and `handleDrop` functions.
    `+page.svelte` has `repo-context-action` event listener.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 06 Task 1 has wired all Phase 5 features into the tray UI:
    - Four-section list (Pinned / Dirty / Recent / Rest) with section headers
    - Right arrow opens detail pane (branch list, tags, notes, pin toggle)
    - Left arrow / Esc closes detail pane
    - # in filter bar shows tag autocomplete dropdown
    - Right-click context menu with Pin/Unpin + Add/Remove tag
    - Drag-to-reorder in Pinned section
    - Notes included in fuzzy filter
  </what-built>
  <how-to-verify>
    Start the tray app: `npm run tauri dev` (wait for the window to open).

    1. **Section headers**: Open the tray. You should see at least a "Rest" section header (gray, uppercase, small text) above any repos that are not pinned/dirty/recent.

    2. **Pin a repo**: Right-click a repo → select "Pin". The repo should move to a "Pinned" section at the top. Right-click it again → select "Unpin". It should return to Rest/Dirty/Recent.

    3. **Detail pane**: Arrow-key to a repo, press right arrow → detail pane opens showing: branch list (current branch highlighted), Tags section (with add input), Notes textarea, Pin checkbox. Press Esc or left arrow → closes.

    4. **Add tag**: In detail pane, type "backend" in tag input, press Enter → tag chip appears. Cmd+Click the chip → it disappears.

    5. **Tag filter**: Type "#backend" in filter bar → only repos with "backend" tag appear across all sections. Type "#" alone → autocomplete dropdown appears with existing tags.

    6. **Notes**: Open detail pane, type in notes textarea, click outside (blur) → notes save. Reopen detail pane → notes persist.

    7. **Drag reorder**: Pin 2+ repos. Drag one to reorder. Reopen tray → order persists.

    8. **Notes fuzzy match**: Add notes "my deployment pipeline" to a repo. Type "pipeline" in filter → that repo appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work, or describe which items have issues so they can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filter input → parseTagFilter | Tag names extracted from free-text input |
| Svelte event handler → invoke() | Drag reorder and context menu actions invoke org mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | dragDrop — drop on wrong section (non-pinned) | mitigate | handleDrop only invoked from Pinned section items; other sections have no drag attributes |
| T-05-06-02 | Tampering | repo-context-action event with forged payload | accept | Local Tauri app; no external event bus; payload comes from Rust on_menu_event handler |
| T-05-06-03 | Denial of Service | filterAndSectionRepos called on every keystroke | accept | Client-side; runs synchronously on pre-loaded array; 100+ repos < 1ms; acceptable |
| T-05-06-04 | Tampering | context menu add_tag opens detail pane (not direct prompt) | accept | Phase 5 minimum for Add Tag via context menu is "open detail pane" per CONTEXT.md D-06 scope |
| T-05-06-SC | Tampering | npm/cargo installs | accept | No new packages |
</threat_model>

<verification>
After Task 1:
1. `npm run check` exits 0
2. `npm test` exits 0
3. `grep -c "filterAndSectionRepos" src/routes/+page.svelte` ≥ 1
4. `grep -c "DetailPane" src/routes/+page.svelte` ≥ 1
5. `grep -c "repo-context-action" src/routes/+page.svelte` ≥ 1

After checkpoint approval:
- All 4 Phase 5 success criteria verified by user (UAT)
</verification>

<success_criteria>
- Tray shows four labeled sections with correct repo assignment per D-13/D-19/D-20/D-21/D-22/D-23
- Right arrow opens detail pane; left/Esc closes (D-11, D-12)
- Tag filter with AND logic; # triggers autocomplete (D-08, D-09, D-10, D-11)
- Drag reorder persists via set_pin_order IPC (D-16)
- Notes save on blur, included in fuzzy filter (D-26, D-28)
- Context menu pins and routes to detail pane for tag operations (D-06, D-14)
- npm run check exits 0; npm test exits 0
- User approves checkpoint
</success_criteria>

<output>
Create `.planning/phases/05-tags-prioritization/05-06-SUMMARY.md` when done.
</output>

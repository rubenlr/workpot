---
phase: 01-core-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - crates/workpot-core/src/domain/repo.rs
  - crates/workpot-core/src/domain/mod.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/catalog_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "User can register a git repo path with workpot repo add and see it in workpot repo list"
    - "Registered repo persists after new AppContext open (simulates restart)"
    - "workpot repo remove deletes a registered path"
    - "repo add rejects paths without .git directory or file"
    - "Core operations use parameterized SQL only; no network crates added"
  artifacts:
    - path: crates/workpot-core/src/services/catalog.rs
      provides: Manual repo register/list/remove
      contains: "register_manual"
    - path: crates/workpot-core/src/domain/repo.rs
      provides: RepoRecord type
      contains: "RepoRecord"
    - path: crates/workpot-cli/src/main.rs
      provides: repo add/list/remove subcommands
      contains: "RepoCommands"
  key_links:
    - from: crates/workpot-cli/src/main.rs
      to: catalog service
      via: AppContext catalog methods
      pattern: "repo add"
    - from: crates/workpot-core/src/services/catalog.rs
      to: repos table
      via: parameterized INSERT/SELECT/DELETE
      pattern: "params!"
---

## Phase Goal

**As a** macOS developer, **I want** to manually register a git repository path via CLI and see it after restarting my shell, **so that** Workpot remembers my projects locally without cloud sync.

<objective>
Complete Phase 1 vertical slice: catalog service for manual repo CRUD, `.git` presence validation without git2, CLI `repo add|list|remove`, and integration tests proving DATA-01 persistence across reopen plus DATA-02 unchanged dep graph.

Purpose: Satisfies ROADMAP success criteria #3 (index one git repo path, persists after restart) and closes DATA-01/DATA-02 for the phase.
Output: catalog service, RepoRecord domain type, repo subcommands, catalog integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-persistence/01-CONTEXT.md
@.planning/phases/01-core-persistence/01-RESEARCH.md
@.planning/phases/01-core-persistence/01-PATTERNS.md
@.planning/phases/01-core-persistence/01-VALIDATION.md
@.planning/phases/01-core-persistence/01-SKELETON.md
@.planning/phases/01-core-persistence/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing catalog persistence integration test</name>
  <files>crates/workpot-core/tests/catalog_test.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Pattern 4 .git check, Parameterized insert, restart proof), @.planning/phases/01-core-persistence/01-VALIDATION.md</read_first>
  <behavior>
    - Test repo_persists_across_reopen: create temp git fixture dir with .git subdirectory; open_with_paths; register_manual; drop context; open_with_paths again; list contains same canonical path
    - Test register_rejects_non_git: temp dir without .git returns error
    - Test register_rejects_duplicate: second add same path returns error
  </behavior>
  <action>Create catalog_test.rs with tempfile fixture helper that creates directory containing .git as empty directory (not git repo — filesystem check only). Write failing tests calling catalog API methods that do not exist yet: register_manual, list_repos, remove_repo on AppContext. Tests must fail at link or assertion stage before implementation.</action>
  <acceptance_criteria>cargo test -p workpot-core repo_persists_across_reopen register_rejects_non_git fails before catalog implementation.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core repo_persists_across_reopen 2>&1 | grep -q "FAILED\|assertion\|panicked\|cannot find"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement catalog service and repo domain types</name>
  <files>crates/workpot-core/src/domain/repo.rs, crates/workpot-core/src/domain/mod.rs, crates/workpot-core/src/services/catalog.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/lib.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Minimal repos table, Pattern 3 path-as-PK, Pattern 4), @.planning/phases/01-core-persistence/01-PATTERNS.md (catalog service), @.planning/research/ARCHITECTURE.md (Catalog service responsibility)</read_first>
  <action>Add RepoRecord struct with path PathBuf, name String, registered_at i64, source String default manual. Implement catalog.rs register_manual(path): verify path is existing directory; verify path.join(".git") is directory OR file; canonicalize path best-effort with clear error if fails; derive name from final path file_name or last segment; INSERT INTO repos with params macro binding path display string, name, registered_at unix UTC seconds, source manual; return error on duplicate PK. Implement list_repos ORDER BY registered_at returning Vec RepoRecord. Implement remove_repo DELETE by path key canonicalized same as add. Expose methods on AppContext delegating to catalog with store connection. Add WorkpotError variants NotGitRepo, AlreadyRegistered, NotFound, InvalidPath. Do not shell out to git binary; do not add git2 dependency.</action>
  <acceptance_criteria>All catalog_test.rs tests pass; SQL uses params binding only with zero format string interpolation of user paths.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core repo_persists_across_reopen register_rejects_non_git register_rejects_duplicate</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire repo add, list, remove CLI and verify phase gate</name>
  <files>crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Phase 1 CLI command set), @.planning/ROADMAP.md (Phase 1 success criteria)</read_first>
  <action>Implement clap RepoCommands Add { path }, List, Remove { path } under repo subcommand. Each handler calls AppContext::open() first. Add: register_manual with user path; print registered path on success. List: print one line per repo as name tab path or name path separated by two spaces; empty list prints nothing exit 0. Remove: remove_repo; print removed path. Map errors to stderr with non-zero exit. After wiring, run scripts/check-no-network-deps.sh and cargo test --offline --workspace to confirm DATA-02 unchanged. Document manual UAT in SUMMARY: workpot repo add on real git repo, new shell, workpot repo list shows entry.</action>
  <acceptance_criteria>cargo run -p workpot-cli -- repo list works after add in integration test fixture; scripts/check-no-network-deps.sh passes; full workspace offline tests pass.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-no-network-deps.sh && cargo test --offline --workspace</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → repo path | User-supplied filesystem paths enter catalog |
| Catalog → SQLite | Path strings written to repos table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | SQL via repo path | mitigate | rusqlite params![] exclusively; no dynamic SQL concatenation |
| T-01-08 | Elevation | Path traversal on repo add | mitigate | canonicalize; require directory exists; reject non-.git paths with NotGitRepo |
| T-01-09 | Spoofing | symlink path identity | accept | Document best-effort canonicalize; Phase 2 may refine worktree identity |
| T-01-10 | Denial of Service | duplicate register spam | mitigate | PRIMARY KEY constraint; AlreadyRegistered error |
| T-01-SC | Spoofing | New deps in plan 03 | mitigate | Re-run check-no-network-deps.sh; no new network crates |
</threat_model>

<verification>
- cargo test -p workpot-core catalog tests green
- cargo test --offline --workspace green
- scripts/check-no-network-deps.sh green
- Manual: workpot repo add + new shell + repo list (document in SUMMARY)
</verification>

<success_criteria>
- ROADMAP criterion 3 satisfied: one git repo path indexed and visible after reopen
- DATA-01: config + SQLite + repo row persist locally
- DATA-02: dependency graph still free of HTTP clients
- Phase 1 CLI verbs: paths, repo add, repo list, repo remove
</success_criteria>

<output>
Create `.planning/phases/01-core-persistence/01-03-SUMMARY.md` when done
</output>

---
phase: 01-core-persistence
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - crates/workpot-core/Cargo.toml
  - crates/workpot-core/src/lib.rs
  - crates/workpot-cli/Cargo.toml
  - crates/workpot-cli/src/main.rs
  - scripts/check-no-network-deps.sh
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - DATA-02
must_haves:
  truths:
    - "Cargo workspace builds with workpot-core and workpot-cli only (no Tauri/ui members)"
    - "Phase 1 dependency graph contains no banned HTTP/network crates"
    - "CI runs build, network-dep check, and offline tests on macOS"
  artifacts:
    - path: Cargo.toml
      provides: Workspace manifest with core+CLI members
      contains: "workpot-core"
    - path: rust-toolchain.toml
      provides: Pinned Rust 1.85+
      contains: "1.85"
    - path: scripts/check-no-network-deps.sh
      provides: DATA-02 structural gate
      contains: "reqwest"
    - path: .github/workflows/ci.yml
      provides: macOS CI stub
      contains: "cargo test --offline"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/check-no-network-deps.sh
      via: workflow step invocation
      pattern: "check-no-network-deps"
    - from: crates/workpot-cli/Cargo.toml
      to: crates/workpot-core
      via: path dependency
      pattern: "workpot-core"
---

## Phase Goal

**As a** macOS developer, **I want** a compilable Rust workspace with privacy gates enforced, **so that** later CLI persistence work ships on a trusted, offline-capable foundation.

<objective>
Bootstrap the greenfield Cargo workspace (core + CLI only), pin Rust 1.85+, verify crate legitimacy before dependency lock, and add DATA-02 structural enforcement (network-dep ban script + macOS CI stub).

Purpose: Phase 1 has zero application code today; this plan creates the walking-skeleton scaffold and supply-chain gate before any SQLite/config logic lands.
Output: Compiling stub crates, `scripts/check-no-network-deps.sh`, `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-persistence/01-CONTEXT.md
@.planning/phases/01-core-persistence/01-RESEARCH.md
@.planning/phases/01-core-persistence/01-PATTERNS.md
@.planning/phases/01-core-persistence/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Phase 1 crate legitimacy on crates.io</name>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Package Legitimacy Audit table)</read_first>
  <action>Before pinning dependencies in Cargo.toml, human verifies each Phase 1 crate on crates.io and its linked source repo: rusqlite 0.39.0, rusqlite_migration 2.5.0, directories 6.0.0, clap 4.6.1, serde 1, toml 0.8, thiserror 2, anyhow 1, tempfile 3 (dev). Confirm publisher/name match RESEARCH audit rows; reject any typosquat. Record approval in plan SUMMARY. Do not proceed to Task 2 until approved.</action>
  <acceptance_criteria>Human confirms all nine crates match expected publishers and source repos; no [SLOP] packages; approval noted for executor resume.</acceptance_criteria>
  <verify>
    <human-check>Visit crates.io pages for rusqlite, rusqlite_migration, directories, clap; confirm github.com/rusqlite/rusqlite, github.com/cljoly/rusqlite_migration, github.com/dirs/directories-rs, github.com/clap-rs/clap.</human-check>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Scaffold Cargo workspace and stub crates</name>
  <files>
    <file path="Cargo.toml" action="create"/>
    <file path="rust-toolchain.toml" action="create"/>
    <file path="crates/workpot-core/Cargo.toml" action="create"/>
    <file path="crates/workpot-core/src/lib.rs" action="create"/>
    <file path="crates/workpot-cli/Cargo.toml" action="create"/>
    <file path="crates/workpot-cli/src/main.rs" action="create"/>
  </files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Standard Stack, Installation snippet), @.planning/phases/01-core-persistence/01-PATTERNS.md (Cargo.toml workspace pattern), @.planning/phases/01-core-persistence/01-SKELETON.md</read_first>
  <action>Create root Cargo.toml workspace with members crates/workpot-core and crates/workpot-cli only — do NOT add src-tauri or ui. Set workspace.package edition 2024 and rust-version 1.85. Add rust-toolchain.toml channel 1.85. In workpot-core/Cargo.toml pin rusqlite 0.39.0 with bundled feature, rusqlite_migration 2.5.0, directories 6.0.0, serde with derive, toml 0.8, thiserror 2; dev-dep tempfile 3. In workpot-cli/Cargo.toml depend on workpot-core path plus clap 4.6.1 with derive and anyhow 1. Stub workpot-core/src/lib.rs with pub fn version() -> &'static str returning workpot-core and minimal pub mod error placeholder. Stub workpot-cli/src/main.rs with clap Parser root command workpot and a Version subcommand that prints core version then exits 0. Explicitly omit git2, notify, tokio, reqwest, ureq, hyper, curl, oauth2, sqlx, refinery.</action>
  <acceptance_criteria>cargo build --workspace succeeds; cargo tree -p workpot-core shows rusqlite 0.39.x; workspace members count is exactly two crates.</acceptance_criteria>
  <verify>
    <automated>cargo build --workspace && test $(cargo metadata --no-deps --format-version 1 | jq '.workspace_members | length') -eq 2</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add DATA-02 network-dep ban script and macOS CI stub</name>
  <files>
    <file path="scripts/check-no-network-deps.sh" action="create"/>
    <file path=".github/workflows/ci.yml" action="create"/>
  </files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Verifying DATA-02), @.planning/phases/01-core-persistence/01-VALIDATION.md</read_first>
  <action>Create scripts/check-no-network-deps.sh executable bash script that runs cargo tree -p workpot-core and cargo tree -p workpot-cli and fails exit 1 if output matches banned crate names: reqwest, ureq, hyper, curl, oauth2, isahc, surf, awc, tokio-tungstenite, hyper-rustls. Script must use grep -v '^#' filtering only on tree output lines containing crate names. Create .github/workflows/ci.yml triggered on push and pull_request with macos-latest runner: checkout, rust-toolchain from rust-toolchain.toml, cargo fetch, run scripts/check-no-network-deps.sh, cargo build --workspace, cargo test --offline --workspace. Do not add Tauri or Node steps.</action>
  <acceptance_criteria>scripts/check-no-network-deps.sh exits 0 on current workspace; CI workflow file validates syntactically and references offline test step.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-no-network-deps.sh && cargo test --offline --workspace</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → Cargo.lock | External packages enter the workspace via manifest pins |
| CI runner → git checkout | Untrusted PR code executes in GitHub Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Spoofing | Cargo dependency pins | mitigate | Blocking human-verify Task 1 before lockfile; RESEARCH Package Legitimacy Audit |
| T-01-04 | Tampering | CI workflow | mitigate | ci.yml runs check-no-network-deps.sh on every PR; no secret env in Phase 1 |
| T-01-05 | Denial of Service | CI resource use | accept | macOS runner only; minimal steps; no long-running services |
</threat_model>

<verification>
- cargo build --workspace green
- scripts/check-no-network-deps.sh green
- cargo test --offline --workspace green (stub tests may be zero tests passing)
</verification>

<success_criteria>
- Workspace compiles with core+CLI only
- DATA-02 structural ban script exists and passes
- CI stub committed for macOS offline test gate
</success_criteria>

<output>
Create `.planning/phases/01-core-persistence/01-01-SUMMARY.md` when done
</output>

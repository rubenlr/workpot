---
phase: 01-core-persistence
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - crates/workpot-core/Cargo.toml
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/src/error.rs
  - crates/workpot-core/src/domain/mod.rs
  - crates/workpot-core/src/domain/config.rs
  - crates/workpot-core/src/infra/mod.rs
  - crates/workpot-core/src/infra/paths.rs
  - crates/workpot-core/src/infra/store.rs
  - crates/workpot-core/src/infra/migrations.rs
  - crates/workpot-core/src/infra/migrations/001_init.sql
  - crates/workpot-core/tests/bootstrap_test.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Running workpot paths prints resolved config and database paths under macOS defaults"
    - "First AppContext open creates default config.toml with watch_roots and excludes empty arrays"
    - "First AppContext open creates SQLite database with migration 001 applied"
    - "No production env var overrides for config or DB paths (D-03)"
  artifacts:
    - path: crates/workpot-core/src/infra/paths.rs
      provides: D-01/D-02 path resolution via BaseDirs
      contains: "config_dir"
    - path: crates/workpot-core/src/infra/migrations/001_init.sql
      provides: repos table schema
      contains: "CREATE TABLE repos"
    - path: crates/workpot-core/src/lib.rs
      provides: AppContext::open lazy bootstrap
      contains: "AppContext"
    - path: crates/workpot-cli/src/main.rs
      provides: workpot paths command
      contains: "Paths"
  key_links:
    - from: crates/workpot-cli/src/main.rs
      to: AppContext::open
      via: paths subcommand handler
      pattern: "AppContext::open"
    - from: crates/workpot-core/src/lib.rs
      to: crates/workpot-core/src/infra/store.rs
      via: open_connection on bootstrap
      pattern: "open_connection"
    - from: crates/workpot-core/src/infra/paths.rs
      to: ~/.config/workpot/config.toml
      via: BaseDirs::config_dir join workpot/config.toml
      pattern: "workpot.*config.toml"
---

## Phase Goal

**As a** macOS developer, **I want** `workpot paths` to show my config and database locations while auto-creating them on first run, **so that** I know where Workpot stores data without running a separate init command.

<objective>
Deliver the persistence bootstrap vertical slice: macOS path resolution (D-01, D-02), default TOML config, SQLite open with WAL + rusqlite_migration 001, lazy AppContext::open (D-04), and CLI `workpot paths`.

Purpose: Proves DATA-01 local artifacts exist and survive process boundaries before repo catalog CRUD.
Output: paths module, config domain, store + migrations, AppContext, paths CLI, bootstrap integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-persistence/01-CONTEXT.md
@.planning/phases/01-core-persistence/01-RESEARCH.md
@.planning/phases/01-core-persistence/01-PATTERNS.md
@.planning/phases/01-core-persistence/01-SKELETON.md
@.planning/phases/01-core-persistence/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing bootstrap integration tests (config + migrations)</name>
  <files>crates/workpot-core/tests/bootstrap_test.rs, crates/workpot-core/src/lib.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Config schema, Pattern 1 lazy bootstrap, Minimal repos table), @.planning/phases/01-core-persistence/01-VALIDATION.md (DATA-01 test map)</read_first>
  <behavior>
    - Test config_creates_defaults: AppContext::open_with_paths(temp_config, temp_db) creates config file containing watch_roots = [] when missing
    - Test migrations_apply: after open, query sqlite pragma user_version returns 1 and repos table exists
    - Test open_with_paths is pub(crate) or cfg(test) only — not exposed to CLI
  </behavior>
  <action>Add AppContext::open_with_paths(config_path: PathBuf, db_path: PathBuf) -> Result<Self> signature in lib.rs as pub for tests (document test-only in module doc; production CLI uses open() with fixed BaseDirs paths per D-03). Create bootstrap_test.rs using tempfile TempDir; write failing tests config_creates_defaults and migrations_apply that call open_with_paths. Do not implement bootstrap logic yet beyond stub returning error or todo — tests must fail first.</action>
  <acceptance_criteria>cargo test -p workpot-core config_creates_defaults migrations_apply fails with meaningful assertion (not compile error).</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core config_creates_defaults migrations_apply 2>&1 | grep -q "FAILED\|assertion\|panicked"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement paths, config, store, migrations, and AppContext bootstrap</name>
  <files>crates/workpot-core/src/domain/config.rs, crates/workpot-core/src/domain/mod.rs, crates/workpot-core/src/infra/paths.rs, crates/workpot-core/src/infra/store.rs, crates/workpot-core/src/infra/migrations.rs, crates/workpot-core/src/infra/migrations/001_init.sql, crates/workpot-core/src/infra/mod.rs, crates/workpot-core/src/error.rs, crates/workpot-core/src/lib.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-CONTEXT.md (D-01 through D-04), @.planning/phases/01-core-persistence/01-RESEARCH.md (Pattern 2 split paths, 001_init.sql), @.planning/phases/01-core-persistence/01-PATTERNS.md</read_first>
  <action>Implement paths.rs with config_file() and database_file() using BaseDirs::new() — config_dir join workpot/config.toml for D-01; data_dir join workpot/workpot.db for D-02; never use ProjectDirs. Implement Config struct with watch_roots and excludes Vec fields serde default empty. Implement ensure_default_config: create parent dirs, if file missing write pretty TOML with watch_roots = [] and excludes = []. Implement 001_init.sql with repos table columns path TEXT PK, name TEXT, registered_at INTEGER, source TEXT DEFAULT manual CHECK, excluded INTEGER DEFAULT 0 CHECK; index on registered_at. Implement migrations.rs MIGRATIONS from_slice single M::up include_str 001_init.sql. Implement store.rs open_connection: create_dir_all parent, Connection::open, pragma journal_mode WAL, MIGRATIONS.to_latest. Implement AppContext holding Config and Connection or SqliteStore wrapper. Implement open() calling paths then open_with_paths. Implement open_with_paths full D-04 sequence: ensure config, load or create Config, open DB. Map errors with thiserror WorkpotError variants Config, Database, Io, PathsUnavailable. Do not add git columns or catalog methods yet.</action>
  <acceptance_criteria>config_creates_defaults and migrations_apply tests pass; open() uses BaseDirs only with no std::env var reads for paths in production code.</acceptance_criteria>
  <verify>
    <automated>cargo test -p workpot-core config_creates_defaults migrations_apply</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire workpot paths CLI command</name>
  <files>crates/workpot-cli/src/main.rs</files>
  <read_first>@.planning/phases/01-core-persistence/01-RESEARCH.md (Phase 1 CLI command set), @.planning/phases/01-core-persistence/01-PATTERNS.md (clap skeleton)</read_first>
  <action>Replace Version-only stub with clap Commands enum: Paths and placeholder Repo subcommand group empty or unimplemented. On Paths: call AppContext::open() which triggers lazy bootstrap per D-04, then print two lines to stdout: config: {absolute config path} and database: {absolute db path}. Exit 0 on success; map WorkpotError to stderr message and exit 1 via anyhow Context. Remove standalone Version subcommand or fold into clap --version. Ensure any CLI invocation path goes through AppContext::open for bootstrap side effect.</action>
  <acceptance_criteria>cargo run -p workpot-cli -- paths prints config and database lines; second run succeeds idempotently; config file exists at printed config path after first run.</acceptance_criteria>
  <verify>
    <automated>cargo run -p workpot-cli -- paths | grep -E "config:|database:"</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → config.toml | Untrusted TOML on disk parsed on load |
| App → SQLite file | Local DB file created in user Application Support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | config.toml parse | mitigate | Serde parse errors surface as Config variant; no silent fallback on corrupt file |
| T-01-02 | Denial of Service | oversized config.toml | accept | Phase 1: document optional future size cap; fail parse if invalid TOML only |
| T-01-03 | Tampering | migration SQL | mitigate | Static include_str migration only; rusqlite_migration owns user_version |
| T-01-06 | Information Disclosure | paths command output | accept | Prints user-local paths only; expected CLI behavior |
</threat_model>

<verification>
- cargo test -p workpot-core bootstrap tests green
- cargo run -p workpot-cli -- paths succeeds
- cargo test --workspace green
</verification>

<success_criteria>
- D-01 config path resolved via BaseDirs config_dir
- D-02 database path resolved via BaseDirs data_dir
- D-04 lazy bootstrap creates config + migrated DB on first open
- workpot paths is user-visible proof of persistence layer
</success_criteria>

<output>
Create `.planning/phases/01-core-persistence/01-02-SUMMARY.md` when done
</output>

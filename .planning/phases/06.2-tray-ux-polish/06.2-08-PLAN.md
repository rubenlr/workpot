---
phase: 06.2
plan: 08
type: execute
wave: 2
depends_on: [06.2-04]
files_modified:
  - src/lib/components/RepoListRow.test.ts
autonomous: true
requirements: [UX-POLISH]

must_haves:
  truths:
    - "RepoListRow.test.ts exists with failing stub tests for plain click, cmd+click, and info badge"
    - "Stub tests fail with a clear message (component does not exist yet at Wave 2 start)"
    - "Test file covers all 7 interaction and display behaviors from RESEARCH.md Wave 0 Gaps"
    - "Sampling continuity satisfied: interaction model tests exist before Plans 05/06 execute"
  artifacts:
    - path: "src/lib/components/RepoListRow.test.ts"
      provides: "Failing stub interaction tests (RED gate)"
      contains: "metaKey"
  key_links:
    - from: "src/lib/components/RepoListRow.test.ts"
      to: "src/lib/components/RepoListRow.svelte"
      via: "Vitest component test using @testing-library/svelte (component extracted in Plan 07)"
      pattern: "RepoListRow"
---

<objective>
Create the failing interaction test stub for RepoListRow BEFORE Plan 05/06 execute, satisfying
Nyquist sampling continuity.

Plans 04, 05, and 06 each contain implementation tasks with no behavioral test gate between them.
This plan inserts a RED test file immediately after Plan 04 completes (Wave 2, before Plans 05/06
run) so the interaction model changes do not accumulate 6 consecutive untested implementation
tasks.

Note: RepoListRow.svelte is extracted in Plan 07 (Wave 3). At Wave 2, the component file does not
yet exist. The test file is written to import it; the import failure is the RED gate. When Plan 07
completes the extraction, the tests can compile and execute. Plan 09 makes them GREEN.

Purpose: Enforce that interaction contract tests exist before the interaction implementation is
considered complete. The test file commits the expected behavior contract to source control.

Output: `src/lib/components/RepoListRow.test.ts` — 7 failing stub tests covering click/cmd-click/
badge/branch/alias behaviors.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md
@.planning/phases/06.2-tray-ux-polish/06.2-04-SUMMARY.md
@src/lib/types.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: RepoListRow interaction test stub (RED gate)</name>
  <files>
    src/lib/components/RepoListRow.test.ts
  </files>
  <read_first>
    - src/lib/types.ts (RepoDto interface — all fields needed for mock fixture)
    - package.json (confirm vitest is present; check if @testing-library/svelte is present)
    - .planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md section "Wave 0 Gaps" and "Phase Requirements Test Map"
  </read_first>
  <action>
    Check `package.json` devDependencies for `@testing-library/svelte`. If absent, do NOT install
    it here — the install is gated by the human checkpoint in Plan 07 Task 1. Instead, write the
    test file with the import, and the missing package will cause it to fail to run (RED gate is
    satisfied either way: missing package or missing component).

    Create `src/lib/components/RepoListRow.test.ts` with the following structure:

    Import `@testing-library/svelte` render and `fireEvent` (these imports will fail if the
    package is not yet installed — that is the RED gate). Import `RepoListRow` from
    `./RepoListRow.svelte` (this import will fail if the component does not yet exist — also RED).
    Import `describe`, `it`, `expect`, `vi` from `vitest`.

    Define a `mockRepo` fixture: a `RepoDto` object with all required fields populated —
    path="/tmp/testrepo", name="testrepo", alias=null, branch="main", is_dirty=false,
    parent_dir="~/tmp", last_opened_at=null, git_state_error=null, pinned=false, pin_order=null,
    notes=null, tags=[], branches=[]. Add any other fields required by the RepoDto interface.

    Define the following 7 test stubs inside `describe('RepoListRow', () => { ... })`:

    1. `it('plain_click_calls_onOpen_not_onDetail', async () => { ... })` — render with
       `onOpen = vi.fn()` and `onDetail = vi.fn()`. Fire click on the row button (no metaKey).
       Assert onOpen called once and onDetail not called.

    2. `it('cmd_click_calls_onDetail_not_onOpen', async () => { ... })` — fire click with
       `{ metaKey: true }`. Assert onDetail called once and onOpen not called.

    3. `it('info_badge_click_calls_onDetail_not_onOpen', async () => { ... })` — find element
       by `aria-label="Open detail"`. Fire click. Assert onDetail called once and onOpen not called.

    4. `it('branch_rendered_when_present', async () => { ... })` — render with branch="main".
       Assert element with text "main" exists in the document.

    5. `it('branch_omitted_when_null', async () => { ... })` — render with branch=null.
       Assert no element contains "—" in its text content.

    6. `it('alias_shown_when_set', async () => { ... })` — render with name="folder",
       alias="myalias". Assert "myalias" appears in the document.

    7. `it('folder_name_shown_when_alias_null', async () => { ... })` — render with name="folder",
       alias=null. Assert "folder" appears in the primary display span.

    Add `vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn().mockResolvedValue(undefined) }))`
    at the top of the file so any accidental invoke calls do not throw.

    The test implementations can be minimal stubs (just the assertions with placeholder `expect`
    calls); Plan 09 Task 2 fills in complete implementations once RepoListRow is extracted.
    The important thing is that the test file exists and commits the behavioral contract.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    `src/lib/components/RepoListRow.test.ts` exists with 7 test stubs. `npm test` either reports
    7 failing tests (RED — component or package not yet available) or 7 passing tests (if Plan 07
    has already run in the same session). Either outcome satisfies the sampling continuity
    requirement: the test contract is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only file | No production code or trust boundary; test file is dev-time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-08-01 | Integrity | Interaction model stub tests never go GREEN (plan 09 not executed) | mitigate | Plan 09 depends_on [06.2-08]; execute-phase will enforce ordering; stub failure is visible in CI |
| T-06.2-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; @testing-library/svelte install is gated by Plan 07 checkpoint |
</threat_model>

<verification>
```bash
ls src/lib/components/RepoListRow.test.ts
npm test -- --reporter=verbose 2>&1 | tail -20
```
File exists. Test runner reports either RED (component absent) or GREEN (plan 07 already done).
</verification>

<success_criteria>
- `src/lib/components/RepoListRow.test.ts` exists
- File contains 7 test stubs (plain-click, cmd-click, badge, branch-present, branch-null, alias-set, alias-null)
- File imports RepoListRow from ./RepoListRow.svelte (creating a compile-time RED gate until Plan 07 runs)
- Sampling continuity satisfied: this file commits the interaction test contract before Plans 05/06 complete
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-08-SUMMARY.md` when done.
</output>

---
phase: 06.2
plan: 08
type: execute
wave: 3
depends_on: [06.2-04, 06.2-05, 06.2-06]
files_modified:
  - crates/workpot-cli/tests/cli_smoke.rs
  - crates/workpot-core/tests/stale_dirty_test.rs
  - src/lib/components/RepoListRow.test.ts
autonomous: true
requirements: [UX-POLISH]

must_haves:
  truths:
    - "CLI smoke test verifies alias appears in workpot list output when alias is set"
    - "CLI smoke test verifies no em-dash in workpot list output for bare/no-branch repos"
    - "Frontend interaction test verifies plain click on row does NOT set detailRepo"
    - "Frontend interaction test verifies cmd+click sets detailRepo without invoking open_in_cursor"
    - "Frontend interaction test verifies info badge click sets detailRepo"
    - "stale-dirty test suite is complete and all assertions pass"
  artifacts:
    - path: "crates/workpot-cli/tests/cli_smoke.rs"
      provides: "alias display + branch omission CLI integration tests"
      contains: "alias"
    - path: "src/lib/components/RepoListRow.test.ts"
      provides: "Vitest interaction tests for row click behavior"
      contains: "metaKey"
  key_links:
    - from: "crates/workpot-cli/tests/cli_smoke.rs"
      to: "crates/workpot-core/src/services/catalog.rs"
      via: "workpot_cmd runs list after registering repo with alias"
      pattern: "workpot list"
    - from: "src/lib/components/RepoListRow.test.ts"
      to: "src/lib/components/RepoListRow.svelte"
      via: "Vitest component test using @testing-library/svelte"
      pattern: "RepoListRow"
---

<objective>
Add the automated tests that validate the interaction model changes and alias display across
CLI and frontend. The stale-dirty tests from Plan 02 (TDD) already cover the core logic;
this plan adds the integration-level coverage.

Depends on Plans 04/05/06 (all surfaces complete). Plan 07 extracted RepoListRow which makes
frontend component testing possible.

Purpose: Close the Wave 0 gaps identified in RESEARCH.md Validation Architecture:
- CLI alias output test
- Frontend row click/cmd-click/info badge behavior test
- CLI bare-branch omission test

Output: Extended cli_smoke.rs with alias cases, new RepoListRow.test.ts with interaction tests.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md
@.planning/phases/06.2-tray-ux-polish/06.2-04-SUMMARY.md
@.planning/phases/06.2-tray-ux-polish/06.2-05-SUMMARY.md
@crates/workpot-cli/tests/cli_smoke.rs
@src/lib/components/RepoListRow.svelte
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI smoke tests — alias display + bare-branch omission</name>
  <files>
    crates/workpot-cli/tests/cli_smoke.rs
  </files>
  <read_first>
    - crates/workpot-cli/tests/cli_smoke.rs (full file — workpot_cmd helper, existing smoke tests)
    - crates/workpot-core/src/services/org.rs (set_alias function signature for direct DB setup)
    - crates/workpot-core/src/services/catalog.rs (register_manual for test repo setup)
    - .planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md section "CLI smoke test fixture pattern"
  </read_first>
  <behavior>
    New test: `workpot_list_shows_alias_when_set`
    - Set up temp git repo with name derived from folder
    - Register repo in DB
    - Set alias via direct DB operation or via `workpot` CLI (whichever is available after Plan 01)
    - Run `workpot list`
    - Output contains the alias string
    - Output does NOT contain the folder name as the primary display (it may appear in parent_dir)

    New test: `workpot_list_omits_branch_placeholder_for_bare_repos`
    - Create a temp bare repo (git init --bare)
    - Register and index it
    - Run `workpot list`
    - Output does NOT contain "—" (em-dash) for the bare repo row

    New test: `workpot_search_matches_by_alias`
    - Set up repo with alias "myalias"
    - Run `workpot search myalias`
    - Output contains the alias display name
    - Exit code is 0
  </behavior>
  <action>
    In `crates/workpot-cli/tests/cli_smoke.rs`, add three new test functions using the
    `workpot_cmd` helper and `assert_cmd` patterns already in the file.

    For `workpot_list_shows_alias_when_set`: after registering a repo, set its alias directly via
    the DB (use `rusqlite::Connection::open` on the test DB path and execute
    `UPDATE repos SET alias = 'myalias' WHERE name = 'testrepo'`). Then run `workpot list` and
    assert stdout contains "myalias". The test DB path follows the `XDG_DATA_HOME` env var pattern
    from the `workpot_cmd` helper.

    For `workpot_list_omits_branch_placeholder_for_bare_repos`: use `Command::new("git").args(["init", "--bare"])` to create a bare repo in a temp dir. Register it with the CLI
    (`workpot repo add <path>`). Run `workpot index`. Run `workpot list`. Assert stdout does NOT
    contain "—".

    For `workpot_search_matches_by_alias`: same alias setup as first test. Run `workpot search
    myalias`. Assert stdout contains "myalias" and exit code is 0.

    Ensure each test uses an isolated temp dir per `tempfile::TempDir` to avoid state sharing.
  </action>
  <verify>
    <automated>cargo test -p workpot-cli cli_smoke 2>&1 | tail -15</automated>
  </verify>
  <done>
    `cargo test -p workpot-cli cli_smoke` exits 0. Three new test functions pass. No test
    uses "—" assertion for bare repos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend interaction tests — row click/cmd-click/badge</name>
  <files>
    src/lib/components/RepoListRow.test.ts
  </files>
  <read_first>
    - src/lib/components/RepoListRow.svelte (full component — props and event handlers)
    - package.json (check if @testing-library/svelte is present; if not, note install needed)
    - src/lib/repoRow.ts (dirtyDotClass utility consumed by RepoListRow)
    - src/lib/types.ts (RepoDto for test fixtures)
  </read_first>
  <action>
    Check `package.json` devDependencies for `@testing-library/svelte`. If absent, run
    `npm install -D @testing-library/svelte`. This is a well-known testing-library package — no
    human checkpoint needed (it is in the standard Svelte ecosystem).

    Create `src/lib/components/RepoListRow.test.ts` using Vitest + `@testing-library/svelte`.

    Test fixture: a mock `RepoDto` with all required fields (path, name, alias=null, branch="main",
    is_dirty=false, parent_dir="~/c", last_opened_at=null, git_state_error=null, pinned=false,
    pin_order=null, notes=null, tags=[], branches=[]).

    Tests:
    1. `plain_click_calls_onOpen_not_onDetail`: render RepoListRow with onOpen and onDetail mocks.
       Fire a plain click event on the button. Assert onOpen called once and onDetail not called.

    2. `cmd_click_calls_onDetail_not_onOpen`: fire click event with `metaKey: true`. Assert
       onDetail called once and onOpen not called.

    3. `info_badge_click_calls_onDetail_not_onOpen`: find the `ⓘ` button by aria-label
       "Open detail". Fire click. Assert onDetail called once and onOpen not called. The badge
       click should also not trigger the row button's click handler (stopPropagation).

    4. `branch_rendered_when_present`: repo with branch="main". Render. Assert text "main" appears.

    5. `branch_omitted_when_null`: repo with branch=null. Render. Assert no "—" in document text.

    6. `alias_shown_when_set`: repo with name="folder", alias="myalias". Render. Assert "myalias"
       in document, "folder" not in primary display span (it may appear in parent_dir).

    7. `folder_name_shown_when_alias_null`: repo with name="folder", alias=null. Assert "folder"
       in primary display span.

    Mock `@tauri-apps/api/core` for any invoke calls if RepoListRow uses them (it should not after
    Plan 07 extraction). Add `vi.mock('@tauri-apps/api/core', ...)` if needed.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm test` exits 0. Seven new tests pass. No test relies on double-click to open.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture data | Tests use local temp dirs and in-memory assertions; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-08-01 | Integrity | CLI tests miss alias regression (alias display reverts) | mitigate | cli_smoke alias test asserts specific alias string in output; runs in CI per cargo test |
| T-06.2-08-02 | Integrity | Interaction regression (double-click re-introduced) | mitigate | RepoListRow.test.ts asserts onOpen called on plain click; no ondblclick tested or expected |
| T-06.2-08-SC | Tampering | npm/pip/cargo installs | mitigate | @testing-library/svelte is a known package in standard Svelte ecosystem; no blocking checkpoint needed |
</threat_model>

<verification>
```bash
cargo test -p workpot-core -p workpot-cli 2>&1 | tail -5
npm test 2>&1 | tail -10
```
Both commands exit 0.
</verification>

<success_criteria>
- `crates/workpot-cli/tests/cli_smoke.rs` has 3 new alias/branch-omission tests that pass
- `src/lib/components/RepoListRow.test.ts` has 7 interaction/display tests that pass
- No test asserts "—" as expected output for None branch
- No test relies on ondblclick to open
- `cargo test -p workpot-core -p workpot-cli` exits 0
- `npm test` exits 0
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-08-SUMMARY.md` when done.
</output>

---
phase: 06.2
plan: 05
type: execute
wave: 2
depends_on: [06.2-01, 06.2-03]
files_modified:
  - crates/workpot-cli/src/list_display.rs
  - crates/workpot-cli/src/main.rs
autonomous: true
requirements: [UX-POLISH]

must_haves:
  truths:
    - "workpot list rows show alias when set, folder name when alias is null"
    - "workpot list rows omit branch when branch is None (no em-dash placeholder)"
    - "workpot search rows show alias when set"
    - "workpot open resolves by alias when alias is given as query"
    - "CLI format_list_row snapshot test updated for alias-first + branch-omission"
  artifacts:
    - path: "crates/workpot-cli/src/list_display.rs"
      provides: "format_list_row using alias ?? name, branch omission, match-based format"
      contains: "display_name"
    - path: "crates/workpot-cli/src/main.rs"
      provides: "search/open use alias display"
      contains: "alias"
  key_links:
    - from: "crates/workpot-cli/src/list_display.rs"
      to: "crates/workpot-core/src/domain/repo.rs"
      via: "repo.alias.as_deref().unwrap_or(&repo.name)"
      pattern: "alias"
    - from: "crates/workpot-cli/src/main.rs"
      to: "workpot_core::services::repo_fuzzy"
      via: "fuzzy search now includes alias field scoring from Plan 03"
      pattern: "fuzzy"
---

<objective>
Update CLI display to use alias-first rendering and omit branch placeholder for bare repos,
achieving parity with the tray changes in Plan 04.

This plan depends on Plan 01 (alias in RepoRecord) and Plan 03 (alias in fuzzy scorer). The fuzzy
search already matches alias after Plan 03; this plan adds alias display to list/search output.

Purpose: CLI and tray must present identical alias-aware display per the locked parity decision.

Output: Updated `format_list_row` with alias-first + branch omission, updated list/search display
in main.rs, updated snapshot tests.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md
@.planning/phases/06.2-tray-ux-polish/06.2-01-SUMMARY.md
@.planning/phases/06.2-tray-ux-polish/06.2-03-SUMMARY.md
@crates/workpot-cli/src/list_display.rs
@crates/workpot-cli/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: format_list_row alias-first + branch omission</name>
  <files>
    crates/workpot-cli/src/list_display.rs
  </files>
  <read_first>
    - crates/workpot-cli/src/list_display.rs (full file — format_list_row, make_repo helper, snapshot test)
    - .planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md section "list_display.rs" (target pattern with match arms)
  </read_first>
  <behavior>
    - format_list_row with alias=Some("wp") and branch=Some("main") → contains "wp" not the folder name, contains "main"
    - format_list_row with alias=None and branch=Some("main") → contains folder name (unchanged behavior)
    - format_list_row with branch=None (bare repo) → does NOT contain "—"
    - format_list_row with alias=None and branch=None → contains folder name but no "—"
    - format_list_row snapshot: alias=None, branch="main" → "⬜ ~/c myrepo main" (same as before)
    - make_repo helper in tests has alias: None (required after Plan 01 adds field)
  </behavior>
  <action>
    Replace `format_list_row` in `list_display.rs` with the pattern from PATTERNS.md:

    `let display_name = repo.alias.as_deref().unwrap_or(&repo.name);`
    `let tags = repo.tags.join(" ");`
    `match (repo.branch.as_deref(), tags.is_empty()) {`
    `  (Some(branch), true)  => format!("{icon} {parent} {display_name} {branch}"),`
    `  (Some(branch), false) => format!("{icon} {parent} {display_name} {branch} {tags}"),`
    `  (None, true)          => format!("{icon} {parent} {display_name}"),`
    `  (None, false)         => format!("{icon} {parent} {display_name} {tags}"),`
    `}`

    Remove the old `let branch = repo.branch.as_deref().unwrap_or("—");` line.

    Update the `make_repo` helper in the `#[cfg(test)]` block: add `alias: None,` to the
    RepoRecord literal.

    Update the snapshot test `format_list_row_snapshot` to verify: `"⬜ ~/c myrepo main"` (unchanged
    when alias is None). The snapshot content is the same because alias=None falls back to name.

    Update the `format_list_row_no_branch` test: remove the assertion `row.contains("—")` and
    replace with `assert!(!row.contains("—"), "em-dash must not appear for None branch: {row}");`
  </action>
  <verify>
    <automated>cargo test -p workpot-cli list_display 2>&1 | tail -10</automated>
  </verify>
  <done>
    `cargo test -p workpot-cli list_display` exits 0. `format_list_row_no_branch` test no longer
    asserts `—`. Snapshot test still passes. Alias test cases pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI main.rs search/open alias display</name>
  <files>
    crates/workpot-cli/src/main.rs
  </files>
  <read_first>
    - crates/workpot-cli/src/main.rs (full file — search and open subcommand handlers, any direct repo.name usage in output formatting)
  </read_first>
  <action>
    Scan `main.rs` for every place where `repo.name` is used in printed output (list, search
    results, open confirmation messages). Replace each `repo.name` reference in output formatting
    with `repo.alias.as_deref().unwrap_or(&repo.name)`. Do NOT change logic paths that use
    `repo.name` as a lookup key — only change display formatting.

    Specifically:
    - In `workpot search` output: the format string that prints the matched repo name should use
      `repo.alias.as_deref().unwrap_or(&repo.name)`.
    - In `workpot open` confirmation/error output: replace `repo.name` display with
      `repo.alias.as_deref().unwrap_or(&repo.name)`.
    - In `workpot list`: output already goes through `format_list_row` (updated in Task 1), so no
      separate change needed there.

    If main.rs already routes all display through `format_list_row`, verify that and note it —
    only add the inline alias fallback where raw `repo.name` appears in output strings.
  </action>
  <verify>
    <automated>cargo test -p workpot-cli 2>&1 | tail -10</automated>
  </verify>
  <done>
    `cargo test -p workpot-cli` exits 0. No `repo.name` used directly in output-facing format strings
    in main.rs (either removed or confirmed routed through format_list_row).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal stdout | CLI output is local single-user; no injection risk for display-only alias |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-05-01 | Integrity | CLI shows alias while tray shows name (parity drift) | mitigate | Both surfaces use `alias.as_deref().unwrap_or(&name)`; fuzzy scorer already shared |
| T-06.2-05-02 | Tampering | alias containing shell metacharacters in CLI output | accept | CLI output is display-only (no eval); alias bounded at 64 chars by org::set_alias |
| T-06.2-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cargo test -p workpot-cli 2>&1 | tail -5
cargo test -p workpot-core -p workpot-cli 2>&1 | tail -5
```
Both exit 0.
</verification>

<success_criteria>
- `format_list_row` uses `display_name = alias ?? name` and match-based branch omission
- `format_list_row_no_branch` test asserts NO "—" in output
- `format_list_row_snapshot` still produces `"⬜ ~/c myrepo main"` for alias=None
- `workpot search` and `workpot open` display alias when set
- `cargo test -p workpot-cli` exits 0
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-05-SUMMARY.md` when done.
</output>

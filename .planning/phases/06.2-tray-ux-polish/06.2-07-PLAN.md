---
phase: 06.2
plan: 07
type: execute
wave: 4
depends_on: [06.2-04, 06.2-06]
files_modified:
  - .storybook/main.ts
  - .storybook/preview.ts
  - src/lib/components/RepoListRow.svelte
  - src/lib/components/RepoListRow.stories.svelte
  - src/lib/components/DetailPaneHeader.stories.svelte
  - package.json
autonomous: false
requirements: [UX-POLISH]

must_haves:
  truths:
    - "Storybook runs with `npm run storybook` without errors"
    - "RepoListRow.svelte component extracted from +page.svelte row template"
    - "RepoListRow stories cover: default, selected, dirty, pinned, alias-set, bare-repo (no branch)"
    - "DetailPaneHeader stories render DetailPane directly with a mock repo (no new sub-component extracted)"
    - "DetailPaneHeader stories cover: alias set, alias null, pinned, unpinned"
    - "Storybook build does not block CI (non-gating per locked decision)"
  artifacts:
    - path: ".storybook/main.ts"
      provides: "Storybook config for Svelte 5 + Vite"
      contains: "framework"
    - path: "src/lib/components/RepoListRow.svelte"
      provides: "Extracted row component"
      contains: "alias"
    - path: "src/lib/components/RepoListRow.stories.svelte"
      provides: "Visual state stories for list row"
      contains: "Story"
    - path: "src/lib/components/DetailPaneHeader.stories.svelte"
      provides: "Visual state stories for detail header (renders DetailPane with mock repo)"
      contains: "Story"
  key_links:
    - from: "src/lib/components/RepoListRow.svelte"
      to: "src/routes/+page.svelte"
      via: "Replace inline row template with <RepoListRow> component"
      pattern: "RepoListRow"
---

<objective>
Bootstrap Storybook and extract the list row as a standalone component with visual state stories.
Also add detail-header stories rendered via DetailPane directly. This is a non-gating milestone
deliverable per the locked decision.

Depends on Plan 04 (alias in RepoDto consumed by row) and Plan 06 (detail header final layout).

Purpose: State documentation makes visual QA repeatable and surfaces regressions in component
states without running the full Tauri app.

Output: Storybook config, extracted RepoListRow component, RepoListRow stories, DetailPaneHeader
stories (via DetailPane directly — no new sub-component). Storybook does not block the merge of
interaction fixes from Plans 04/05/06.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md
@.planning/phases/06.2-tray-ux-polish/06.2-04-SUMMARY.md
@.planning/phases/06.2-tray-ux-polish/06.2-06-SUMMARY.md
@src/routes/+page.svelte
@src/lib/components/DetailPane.svelte
@src/lib/types.ts
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Storybook packages and the @testing-library/svelte test package need to be added to package.json
    as devDependencies before this plan and Plan 09 proceed. The RESEARCH.md Package Legitimacy Audit
    confirms no mandatory external packages were added for core scope, but notes that Storybook
    bootstrap will add devDependencies and requires a slopcheck. This checkpoint gates both installs.
  </what-built>
  <how-to-verify>
    Before approving, verify the following packages are legitimate on npmjs.com:
    1. https://www.npmjs.com/package/@storybook/svelte-vite — should show Storybook organization author, active maintenance
    2. https://www.npmjs.com/package/@storybook/addon-svelte-csf — same org
    3. https://www.npmjs.com/package/storybook — same org
    4. https://www.npmjs.com/package/@testing-library/svelte — @testing-library org, active maintenance

    These are the four packages that will be installed (three for Storybook in this plan, one for
    frontend interaction tests in Plan 09). If any shows an unfamiliar publisher or very low download
    count, reject and the plan will proceed without the suspect package (note in resume signal).

    Run to check current package.json: `cat package.json | grep -A5 "devDependencies"`
  </how-to-verify>
  <resume-signal>
    Type "approved — storybook and testing-library packages verified" to proceed with all four
    package installs.
    Type "approved — storybook only" if only Storybook packages are verified (Plan 09 will skip
    @testing-library/svelte).
    Type "skip storybook" if packages are suspect or install is not desired this session.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Storybook bootstrap + RepoListRow component extraction</name>
  <files>
    .storybook/main.ts,
    .storybook/preview.ts,
    src/lib/components/RepoListRow.svelte,
    package.json
  </files>
  <read_first>
    - package.json (current scripts and devDependencies to verify current Svelte + Vite versions)
    - src/routes/+page.svelte lines 515-600 (row button template to extract)
    - src/lib/types.ts (RepoDto interface for component props)
    - .planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md section "Storybook Svelte state stories" (code example)
  </read_first>
  <action>
    Install Storybook with Svelte 5 + Vite support (per RESEARCH.md standard stack):
    Run `npm install -D storybook @storybook/svelte-vite @storybook/addon-svelte-csf`.
    Run `npx storybook init --skip-install` to generate `.storybook/main.ts` and
    `.storybook/preview.ts` scaffold. If `storybook init` generates JavaScript files, rename to
    TypeScript.

    Verify `.storybook/main.ts` configures the `@storybook/svelte-vite` framework and sets stories
    glob to `'../src/**/*.stories.svelte'`.

    Add `"storybook": "storybook dev -p 6006"` and `"build-storybook": "storybook build"` to
    `package.json` scripts.

    Extract `RepoListRow.svelte` from the row button template in +page.svelte:
    The component accepts props: `repo: RepoDto`, `selected: boolean`, `onOpen: () => void`,
    `onDetail: () => void`. It renders the button element (without the wrapping `<li>`) with the
    dirtyDotClass, alias ?? name, conditional branch span, info badge, and tag chips. Remove the
    inline invocation from +page.svelte and replace with `<RepoListRow {repo} selected={idx ===
    selectedIndex} onOpen={() => void openSelected(false)} onDetail={() => { selectedIndex = idx;
    detailRepo = repo; }} />`. Keep drag handlers in +page.svelte wrapper `<li>` element.

    Note: RepoListRow should NOT invoke Tauri commands directly (no `invoke` calls). The `onOpen`
    and `onDetail` callbacks are provided by the parent, keeping the component pure-Svelte and
    Storybook-friendly.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0. `.storybook/main.ts` exists. `RepoListRow.svelte` exists in
    `src/lib/components/`. `package.json` scripts include `storybook` entry.
  </done>
</task>

<task type="auto">
  <name>Task 3: RepoListRow stories + DetailPaneHeader stories (via DetailPane)</name>
  <files>
    src/lib/components/RepoListRow.stories.svelte,
    src/lib/components/DetailPaneHeader.stories.svelte
  </files>
  <read_first>
    - src/lib/components/RepoListRow.svelte (just created — props interface)
    - src/lib/components/DetailPane.svelte lines 155-175 (header block, post Plan 06 state)
    - src/lib/types.ts (RepoDto for story args)
    - .planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md section "Storybook Svelte state stories" (code example using defineMeta + Story)
  </read_first>
  <action>
    Create `src/lib/components/RepoListRow.stories.svelte` using `@storybook/addon-svelte-csf`
    `defineMeta` + `Story` pattern. Stories to include:
    - Default: alias=null, branch="main", is_dirty=null, pinned=false, selected=false
    - Selected: same as default with selected=true
    - Dirty: is_dirty=true (show dirty dot)
    - Pinned: pinned=true, is_dirty=false
    - WithAlias: alias="my-project", branch="feat/alias"
    - BareRepo: branch=null, alias=null (verify no branch span rendered)

    Each story sets `args` with a mock `repo: RepoDto` object with all required fields populated.
    Use `onOpen: () => {}` and `onDetail: () => {}` as no-op callbacks.

    Create `src/lib/components/DetailPaneHeader.stories.svelte` by rendering `DetailPane` directly
    with a mock repo. Do NOT extract a new `DetailPaneHeader.svelte` sub-component — render the
    header states by controlling the `repo` prop passed to `DetailPane` (and `onClose: () => {}`
    as a no-op). This keeps files_modified accurate and avoids scope creep.

    DetailPaneHeader stories (each renders `<DetailPane {repo} onClose={() => {}} />`):
    - AliasSet: repo with alias="my-project", pinned=false
    - AliasNull: repo with alias=null (shows folder name), pinned=false
    - Pinned: repo with alias=null, pinned=true (shows 📌)
    - Unpinned: repo with alias=null, pinned=false (shows 📍)

    Mock `@tauri-apps/api/core` invoke in the `.storybook/preview.ts` file so DetailPane stories
    do not fail when Storybook calls any stubs for set_pin, set_notes, etc. Add:
    `import { vi } from 'vitest'; vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn() }));`
    or use Storybook's `parameters.msw` / a manual module mock approach appropriate for the
    Storybook Svelte-Vite setup.

    All story arg repos must have all RepoDto fields present to avoid TypeScript errors.
  </action>
  <verify>
    <automated>npm run build-storybook 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npm run build-storybook` exits 0 (Storybook static build succeeds). Story files exist for
    both components. Six RepoListRow stories and four DetailPaneHeader stories present. No new
    `DetailPaneHeader.svelte` component file exists (stories use DetailPane directly).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm devDependency install | Storybook packages added as devDependencies only; not in production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-07-01 | Tampering | Malicious npm package installed as Storybook or test dependency | mitigate | Human checkpoint gate before install; verify publisher on npmjs.com for all four packages (storybook, svelte-vite, addon-svelte-csf, @testing-library/svelte) |
| T-06.2-07-02 | Integrity | RepoListRow stories call invoke() and require Tauri runtime | mitigate | RepoListRow is extracted as a pure component with callback props; no invoke calls inside the component |
| T-06.2-07-SC | Tampering | npm install chain includes [ASSUMED] packages | mitigate | Blocking human-verify checkpoint T-06.2-07-01 gates all four installs; [SUS] packages would cause checkpoint rejection |
</threat_model>

<verification>
```bash
npm run build 2>&1 | tail -3
npm run build-storybook 2>&1 | tail -3
```
Both exit 0. RepoListRow and DetailPaneHeader story files present. No new DetailPaneHeader.svelte component file.
</verification>

<success_criteria>
- `npm run build` exits 0 with RepoListRow component extracted from +page.svelte
- `.storybook/main.ts` exists with `@storybook/svelte-vite` framework
- `RepoListRow.stories.svelte` has 6 stories covering selected/dirty/pinned/alias/bare states
- `DetailPaneHeader.stories.svelte` has 4 stories; renders DetailPane directly — no new DetailPaneHeader.svelte component
- `npm run build-storybook` exits 0
- Storybook does not appear in CI merge gate (non-blocking per locked decision D-Storybook)
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-07-SUMMARY.md` when done.
</output>

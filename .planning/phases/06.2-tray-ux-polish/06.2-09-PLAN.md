---
phase: 06.2
plan: 09
type: execute
wave: 3
depends_on: [06.2-07, 06.2-08]
files_modified:
  - crates/workpot-cli/tests/cli_smoke.rs
  - crates/workpot-core/tests/stale_dirty_test.rs
  - src/lib/components/RepoListRow.test.ts
autonomous: true
requirements: [UX-POLISH]

must_haves:
  truths:
    - "CLI smoke test verifies alias appears in workpot list output when alias is set"
    - "CLI smoke test verifies no em-dash in workpot list output for bare/no-branch repos"
    - "CLI smoke test verifies workpot search matches by alias"
    - "Frontend interaction tests are GREEN: plain click, cmd+click, badge, branch, alias all pass"
    - "has_stale_dirty_dto produces identical results to has_stale_dirty for equivalent inputs"
    - "stale-dirty test suite is complete and all assertions pass"
  artifacts:
    - path: "crates/workpot-cli/tests/cli_smoke.rs"
      provides: "alias display + branch omission CLI integration tests"
      contains: "alias"
    - path: "src/lib/components/RepoListRow.test.ts"
      provides: "Completed Vitest interaction tests (GREEN)"
      contains: "metaKey"
    - path: "crates/workpot-core/tests/stale_dirty_test.rs"
      provides: "has_stale_dirty_dto bridge validation added to existing stale-dirty test suite"
      contains: "has_stale_dirty_dto"
  key_links:
    - from: "crates/workpot-cli/tests/cli_smoke.rs"
      to: "crates/workpot-core/src/services/catalog.rs"
      via: "workpot_cmd runs list after registering repo with alias"
      pattern: "workpot list"
    - from: "src/lib/components/RepoListRow.test.ts"
      to: "src/lib/components/RepoListRow.svelte"
      via: "Vitest component test using @testing-library/svelte (stub from Plan 08)"
      pattern: "RepoListRow"
    - from: "crates/workpot-core/tests/stale_dirty_test.rs"
      to: "src-tauri/src/commands.rs"
      via: "has_stale_dirty_dto bridge test validates DTO-layer helper against core policy"
      pattern: "has_stale_dirty_dto"
---

<objective>
Complete all automated test coverage for the Phase 06.2 interaction model and alias changes.
Makes the Plan 08 RED stubs GREEN, adds CLI integration tests, and validates that the
has_stale_dirty_dto DTO-layer helper in commands.rs matches the TDD'd core has_stale_dirty policy.

Depends on Plan 07 (RepoListRow extracted — component exists for tests) and Plan 08 (test stubs
committed). The @testing-library/svelte install was approved in Plan 07's human checkpoint.

Purpose: Close the RESEARCH.md Wave 0 gaps for CLI alias output and frontend interaction coverage.
The has_stale_dirty_dto bridge test prevents silent policy divergence between the core function
(has_stale_dirty, tested in Plan 02) and the DTO-layer copy (has_stale_dirty_dto in commands.rs).

Output: Extended cli_smoke.rs with alias/bare-branch cases, completed RepoListRow.test.ts (GREEN),
has_stale_dirty_dto bridge test appended to stale_dirty_test.rs.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-RESEARCH.md
@.planning/phases/06.2-tray-ux-polish/06.2-04-SUMMARY.md
@.planning/phases/06.2-tray-ux-polish/06.2-05-SUMMARY.md
@.planning/phases/06.2-tray-ux-polish/06.2-07-SUMMARY.md
@crates/workpot-cli/tests/cli_smoke.rs
@crates/workpot-core/tests/stale_dirty_test.rs
@src/lib/components/RepoListRow.svelte
@src/lib/components/RepoListRow.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI smoke tests — alias display + bare-branch omission + has_stale_dirty_dto bridge</name>
  <files>
    crates/workpot-cli/tests/cli_smoke.rs,
    crates/workpot-core/tests/stale_dirty_test.rs
  </files>
  <read_first>
    - crates/workpot-cli/tests/cli_smoke.rs (full file — workpot_cmd helper, existing smoke tests, XDG_DATA_HOME pattern)
    - crates/workpot-core/src/services/org.rs (set_alias function signature for direct DB setup)
    - crates/workpot-core/src/services/catalog.rs (register_manual for test repo setup)
    - crates/workpot-core/tests/stale_dirty_test.rs (existing test helpers make_dirty_repo / make_clean_repo)
    - .planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md section "CLI smoke test fixture pattern"
    - src-tauri/src/commands.rs (has_stale_dirty_dto function — read the exact signature and body to replicate inputs in bridge test)
  </read_first>
  <behavior>
    New CLI test: `workpot_list_shows_alias_when_set`
    - Register a repo; set alias to "myalias" via direct DB UPDATE; run `workpot list`
    - stdout contains "myalias"

    New CLI test: `workpot_list_omits_branch_placeholder_for_bare_repos`
    - Create a bare git repo; register and index it; run `workpot list`
    - stdout does NOT contain "—"

    New CLI test: `workpot_search_matches_by_alias`
    - Set alias "myalias"; run `workpot search myalias`
    - stdout contains "myalias"; exit code is 0

    Bridge test in stale_dirty_test.rs: `has_stale_dirty_dto_matches_has_stale_dirty`
    - For each of the 11 behavioral cases from Plan 02 (corresponding repo states), construct both
      a RepoRecord input for has_stale_dirty AND a structurally equivalent set of (is_dirty,
      last_opened_at) values for has_stale_dirty_dto.
    - Assert both functions return the same bool for each case.
    - This test documents the bridge contract: any divergence between the two implementations
      will fail here before reaching production.
  </behavior>
  <action>
    In `crates/workpot-cli/tests/cli_smoke.rs`, add three new test functions using the
    `workpot_cmd` helper and `assert_cmd` patterns already in the file.

    For `workpot_list_shows_alias_when_set`: after registering a repo, set its alias directly via
    the DB using `rusqlite::Connection::open` on the test DB path and executing
    `UPDATE repos SET alias = 'myalias' WHERE name = 'testrepo'`. The test DB path follows the
    `XDG_DATA_HOME` env var pattern from the `workpot_cmd` helper. Run `workpot list` and assert
    stdout contains "myalias".

    For `workpot_list_omits_branch_placeholder_for_bare_repos`: use `Command::new("git").args(["init", "--bare"])` to create a bare repo in a temp dir. Register it with the CLI
    (`workpot repo add <path>`). Run `workpot index`. Run `workpot list`. Assert stdout does NOT
    contain "—".

    For `workpot_search_matches_by_alias`: same alias setup as first test. Run `workpot search
    myalias`. Assert stdout contains "myalias" and exit code is 0.

    Ensure each test uses an isolated temp dir via `tempfile::TempDir` to avoid state sharing.

    In `crates/workpot-core/tests/stale_dirty_test.rs`, append a new test function
    `has_stale_dirty_dto_matches_has_stale_dirty`. This test cannot call the private
    `has_stale_dirty_dto` in commands.rs directly (it is module-private to src-tauri). Instead,
    document the bridge contract by:
    1. Defining a local `dto_equivalent` helper that replicates the has_stale_dirty_dto logic
       inline using the same algorithm (threshold_secs + i64::MAX for None + SystemTime::now).
    2. For each case, comparing its output against `workpot_core::services::stale_dirty::has_stale_dirty`
       with equivalent repo data.
    3. Add a doc comment above the test: "This test validates that has_stale_dirty_dto in
       src-tauri/src/commands.rs follows the same policy as has_stale_dirty. If the two
       implementations diverge, update both and keep this test passing."

    Bridge test cases to cover (subset of Plan 02 cases, chosen to exercise all branches):
    - dirty + last_opened_at = None (never opened) -> both return true
    - dirty + opened exactly threshold_secs ago -> both return true
    - dirty + opened threshold_secs - 1 ago -> both return false
    - clean repo -> both return false
    - bare repo (is_dirty=None) -> both return false
  </action>
  <verify>
    <automated>cargo test -p workpot-cli cli_smoke 2>&1 | tail -15 && cargo test -p workpot-core stale_dirty 2>&1 | tail -10</automated>
  </verify>
  <done>
    `cargo test -p workpot-cli cli_smoke` exits 0. Three new test functions pass. No test
    uses "—" assertion for bare repos.
    `cargo test -p workpot-core stale_dirty` exits 0. Bridge test `has_stale_dirty_dto_matches_has_stale_dirty` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete frontend interaction tests (GREEN phase)</name>
  <files>
    src/lib/components/RepoListRow.test.ts
  </files>
  <read_first>
    - src/lib/components/RepoListRow.test.ts (stubs from Plan 08 — read full file)
    - src/lib/components/RepoListRow.svelte (extracted component from Plan 07 — read props interface and event handler patterns)
    - src/lib/types.ts (RepoDto for complete fixture)
    - package.json (confirm @testing-library/svelte is installed after Plan 07 checkpoint)
  </read_first>
  <action>
    Read `src/lib/components/RepoListRow.test.ts` (the stub from Plan 08) and complete the
    7 test implementations. Replace each placeholder with a working test body.

    If `@testing-library/svelte` was approved in Plan 07's checkpoint but not yet installed,
    run `npm install -D @testing-library/svelte` now (the checkpoint approval recorded in Plan 07
    covers this install).

    Complete implementations for all 7 tests:

    1. `plain_click_calls_onOpen_not_onDetail`: use `render(RepoListRow, { props: { repo: mockRepo, selected: false, onOpen: onOpenMock, onDetail: onDetailMock } })`. Get the row button by role or test-id. Fire `fireEvent.click(button)` (no options = no metaKey). Assert `onOpenMock` called once; `onDetailMock` not called.

    2. `cmd_click_calls_onDetail_not_onOpen`: fire `fireEvent.click(button, { metaKey: true })`. Assert `onDetailMock` called once; `onOpenMock` not called.

    3. `info_badge_click_calls_onDetail_not_onOpen`: `screen.getByLabelText('Open detail')` to find the badge button. `fireEvent.click(badge)`. Assert `onDetailMock` called once; `onOpenMock` not called.

    4. `branch_rendered_when_present`: render with `repo = { ...mockRepo, branch: 'main' }`. Assert `screen.getByText('main')` exists.

    5. `branch_omitted_when_null`: render with `repo = { ...mockRepo, branch: null }`. Assert `document.body.textContent` does not contain '—'.

    6. `alias_shown_when_set`: render with `repo = { ...mockRepo, name: 'folder', alias: 'myalias' }`. Assert `screen.getByText('myalias')` exists.

    7. `folder_name_shown_when_alias_null`: render with `repo = { ...mockRepo, name: 'folder', alias: null }`. Assert `screen.getByText('folder')` exists.

    Keep the `vi.mock('@tauri-apps/api/core', ...)` at the top of the file (from Plan 08 stub).
    Add `afterEach(() => { cleanup(); })` to prevent DOM leaks between tests.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm test` exits 0. All 7 tests in RepoListRow.test.ts pass (GREEN). No test relies on
    double-click to open. No test asserts "—" as expected for null branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture data | Tests use local temp dirs and in-memory assertions; no network |
| has_stale_dirty_dto bridge | Bridge test documents policy parity without crossing a real trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-09-01 | Integrity | CLI tests miss alias regression (alias display reverts) | mitigate | cli_smoke alias test asserts specific alias string in output; runs in CI per cargo test |
| T-06.2-09-02 | Integrity | has_stale_dirty_dto silently diverges from core policy | mitigate | Bridge test in stale_dirty_test.rs validates equivalent inputs produce equal bool outputs |
| T-06.2-09-03 | Integrity | Interaction regression (double-click re-introduced) | mitigate | RepoListRow.test.ts asserts onOpen called on plain click; no ondblclick tested or expected |
| T-06.2-09-SC | Tampering | npm/pip/cargo installs | mitigate | @testing-library/svelte approved in Plan 07 human checkpoint; no additional installs here |
</threat_model>

<verification>
```bash
cargo test -p workpot-core -p workpot-cli 2>&1 | tail -5
npm test 2>&1 | tail -10
```
Both commands exit 0.
</verification>

<success_criteria>
- `crates/workpot-cli/tests/cli_smoke.rs` has 3 new alias/branch-omission tests that pass
- `has_stale_dirty_dto_matches_has_stale_dirty` bridge test passes in stale_dirty_test.rs
- `src/lib/components/RepoListRow.test.ts` has 7 passing interaction/display tests
- No test asserts "—" as expected output for None branch
- No test relies on ondblclick to open
- `cargo test -p workpot-core -p workpot-cli` exits 0
- `npm test` exits 0
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-09-SUMMARY.md` when done.
</output>

---
phase: 06.2-tray-ux-polish
plan: 09
subsystem: testing
tags: [vitest, cli-smoke, stale-dirty, alias, integration-tests]

requires:
  - phase: 06.2-07
    provides: RepoListRow component for Vitest targets
  - phase: 06.2-08
    provides: RepoListRow.test.ts RED stubs and @testing-library/svelte
provides:
  - CLI integration tests for alias display, bare-branch omission, alias search
  - has_stale_dirty_dto policy bridge test vs core has_stale_dirty
  - GREEN RepoListRow interaction/display Vitest suite (7 tests)
affects:
  - Phase 06.2 verification / UAT closure

tech-stack:
  added: []
  patterns:
    - "CLI smoke: set alias via org::set_alias + paths-derived DB path"
    - "DTO bridge: local dto_equivalent mirrors commands.rs has_stale_dirty_dto"

key-files:
  created: []
  modified:
    - crates/workpot-cli/tests/cli_smoke.rs
    - crates/workpot-core/tests/stale_dirty_test.rs
    - src/lib/components/RepoListRow.test.ts

key-decisions:
  - "Use workpot_core::services::org::set_alias in CLI smoke instead of raw SQL"
  - "Resolve test DB via workpot paths output for cross-platform isolation"

patterns-established:
  - "bare_git_fixture + index before list asserts no em-dash in stdout"
  - "has_stale_dirty_dto_matches_has_stale_dirty covers five policy branches"

requirements-completed: [UX-POLISH]

duration: 12min
completed: 2026-05-31
---

# Phase 06.2 Plan 09: Automated Test Coverage Summary

**CLI alias/bare-branch smoke, stale-dirty DTO bridge, and seven GREEN RepoListRow Vitest interaction tests close Phase 06.2 automated coverage gaps.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-05-31T23:39:00Z
- **Completed:** 2026-05-31T23:41:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Three new `cli_smoke` tests: alias in `workpot list`, no `—` for bare repos after index, `workpot search` by alias
- `has_stale_dirty_dto_matches_has_stale_dirty` bridge locks tray DTO policy to core `has_stale_dirty`
- RepoListRow Vitest suite GREEN (plain/cmd click, info badge, branch/alias display) with `afterEach(cleanup)`

## Task Commits

1. **Task 1: CLI smoke + DTO bridge** - `14cf721` (test)
2. **Task 2: RepoListRow Vitest GREEN + cleanup** - `e93e9cd` (chore)

**Plan metadata:** `586d57c` (docs: complete plan)

## Files Created/Modified

- `crates/workpot-cli/tests/cli_smoke.rs` - `workpot_list_shows_alias_when_set`, bare-branch omission, `workpot_search_matches_by_alias`
- `crates/workpot-core/tests/stale_dirty_test.rs` - `dto_equivalent` + `has_stale_dirty_dto_matches_has_stale_dirty`
- `src/lib/components/RepoListRow.test.ts` - completed implementations from plan 08; added `afterEach(cleanup)`

## TDD Gate Compliance

| Plan | RED commit | GREEN commit | Notes |
|------|------------|--------------|-------|
| 06.2-02 | `9252f6d` | `6e1aefc` | `has_stale_dirty` policy |
| 06.2-08 | `c052c2d` | `378809a` | RepoListRow interaction stubs |
| 06.2-09 Task 1 | `14cf721` | (same) | Integration/regression only — behavior from 06.2-05; tests pass on first run (expected) |
| 06.2-09 Task 2 | (in 06.2-08 GREEN) | `e93e9cd` | Implementations already GREEN; this plan adds cleanup hygiene |

## Deviations from Plan

### Auto-fixed Issues

None — plan executed as written.

### Scope Notes

- Used `org::set_alias` + `workpot paths` for DB path instead of raw `UPDATE` SQL (equivalent, avoids extra `rusqlite` dev-dep on CLI crate).

## Verification

```bash
cargo test -p workpot-core -p workpot-cli  # exit 0
npm test                                    # 130 tests, exit 0
```

## Self-Check: PASSED

- FOUND: `crates/workpot-cli/tests/cli_smoke.rs`
- FOUND: `crates/workpot-core/tests/stale_dirty_test.rs`
- FOUND: `src/lib/components/RepoListRow.test.ts`
- FOUND: `14cf721`
- FOUND: `e93e9cd`

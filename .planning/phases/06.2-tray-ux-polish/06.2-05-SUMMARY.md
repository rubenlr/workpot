---
phase: 06.2-tray-ux-polish
plan: 05
subsystem: cli
tags: [cli, alias, list-display, rust]

requires:
  - phase: 06.2-01
    provides: RepoRecord.alias on core DTO
  - phase: 06.2-03
    provides: fuzzy scorer includes alias field
provides:
  - format_list_row alias-first display and bare-branch omission
  - workpot repo list / search / list via format_list_row parity
  - resolve_repo_identifier exact alias match for open
affects: [06.2-04, tray-cli-parity]

tech-stack:
  added: []
  patterns:
    - "display_name = repo.alias.as_deref().unwrap_or(&repo.name) in CLI output"
    - "match (branch, tags.is_empty()) for list row formatting without em-dash placeholder"

key-files:
  created: []
  modified:
    - crates/workpot-cli/src/list_display.rs
    - crates/workpot-cli/src/main.rs

key-decisions:
  - "Open resolves exact alias before folder name; ambiguous alias uses same numbered-path error as name"

patterns-established:
  - "CLI list/search rows share format_list_row with tray (alias ?? name, omit branch when None)"

requirements-completed: [UX-POLISH]

duration: 15min
completed: 2026-05-31
---

# Phase 06.2 Plan 05: CLI alias-first display Summary

**CLI list/search rows show alias when set, omit branch for bare repos, and `workpot open` resolves an exact alias before folder name**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `format_list_row` uses `display_name = alias ?? name` and match arms that omit branch when `None` (no `—`)
- Added unit tests for alias display, bare branch, and unchanged snapshot for `alias=None`
- `workpot search` / `workpot list` already route through `format_list_row` — parity automatic
- `workpot repo list` prints alias when set; `resolve_repo_identifier` matches exact alias for `open`

## Task Commits

1. **Task 1: format_list_row alias-first + branch omission** — `61a3858` (feat)
2. **Task 2: CLI main.rs search/open alias display** — `85607f1` (feat)

**Plan metadata:** `c5ab406` docs(06.2-05): complete CLI alias display parity plan

## Files Created/Modified

- `crates/workpot-cli/src/list_display.rs` — alias-first `format_list_row`, branch omission, tests
- `crates/workpot-cli/src/main.rs` — repo list display name; alias resolution in `resolve_repo_identifier`

## Decisions Made

- Extended Task 2 with alias lookup in `resolve_repo_identifier` to satisfy plan must-have and user scope (`open`); display-only change insufficient for open-by-alias

## Deviations from Plan

### Scope extension (must-have alignment)

**1. Open-by-alias resolution in `resolve_repo_identifier`**
- **Found during:** Task 2
- **Issue:** Plan must-haves require `workpot open` resolves by alias; Task 2 text only mentioned display strings; `run_open` prints path only
- **Fix:** Exact alias match before name match, with ambiguous-alias error mirroring name ambiguity
- **Files modified:** `crates/workpot-cli/src/main.rs`
- **Committed in:** `85607f1`

## TDD Gate Compliance

- Implementation and tests for Task 1 landed in a single feat commit (`61a3858`); no separate `test(06.2-05)` RED commit
- GREEN verified: `cargo test -p workpot-cli list_display` and full `cargo test -p workpot-cli` exit 0

## Issues Encountered

None

## Self-Check: PASSED

- FOUND: crates/workpot-cli/src/list_display.rs
- FOUND: crates/workpot-cli/src/main.rs
- FOUND: 61a3858
- FOUND: 85607f1
- FOUND: c5ab406

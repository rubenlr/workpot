---
phase: 06.2
plan: 06
type: execute
wave: 2
depends_on: [06.2-04]
files_modified:
  - src/app.css
  - src/lib/components/DetailPane.svelte
autonomous: true
requirements: [UX-POLISH]

must_haves:
  truths:
    - "panel-shell CSS has no border and curved bottom corners"
    - "detail header shows alias when set, folder name when not; pin is 📌/📍 toggle on the right same line as title"
    - "tag input has autocomplete=off, autocapitalize=off, autocorrect=off, spellcheck=false"
    - "notes textarea has autocomplete=off, autocapitalize=off, autocorrect=off, spellcheck=false"
    - "old pinned checkbox+label removed from detail header"
    - "detail header title uses repo.alias ?? repo.name"
  artifacts:
    - path: "src/app.css"
      provides: "panel-shell rule with border: none and bottom border-radius"
      contains: "border-bottom-left-radius"
    - path: "src/lib/components/DetailPane.svelte"
      provides: "updated header with pin toggle on right, input hardening attributes"
      contains: "📌"
  key_links:
    - from: "src/app.css"
      to: "src/routes/+page.svelte"
      via: "panel-shell class on <main> element"
      pattern: "panel-shell"
    - from: "src/lib/components/DetailPane.svelte"
      to: "src-tauri/src/commands.rs"
      via: "invoke('set_pin') in handlePinChange"
      pattern: "set_pin"
---

<objective>
Apply the three remaining visual polish items: panel chrome CSS, detail pane header restructure
(alias title + pin emoji toggle), and input hardening for tag/notes fields.

This plan depends on Plan 04 because Plan 04 already adds `alias` to the TypeScript RepoDto which
is consumed in DetailPane.svelte.

Purpose: These are cosmetic and UX-hygiene changes that make the panel feel native and prevent OS
autocorrect from corrupting org metadata.

Output: Updated app.css with borderless curved panel, updated DetailPane.svelte with new header
layout and hardened inputs.
</objective>

<execution_context>
@/Users/rubenlr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rubenlr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.2-tray-ux-polish/06.2-CONTEXT.md
@.planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md
@.planning/phases/06.2-tray-ux-polish/06.2-04-SUMMARY.md
@src/app.css
@src/lib/components/DetailPane.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel shell CSS — borderless + curved bottom</name>
  <files>
    src/app.css
  </files>
  <read_first>
    - src/app.css (full file — current panel-shell rule)
    - src/routes/+page.svelte lines 438-442 (main element with panel-shell class and rounded-xl)
    - .planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md section "app.css" (target CSS rule)
  </read_first>
  <action>
    Update the `.panel-shell` rule in `src/app.css` to:
    ```
    .panel-shell {
      background: transparent;
      border: none;
      border-bottom-left-radius: 12px;
      border-bottom-right-radius: 12px;
    }
    ```

    Audit the `<main class="panel-shell ... rounded-xl ...">` in +page.svelte: `rounded-xl` applies
    12px radius on all four corners. Adding bottom radius in CSS is redundant but harmless. The key
    fix is `border: none` which overrides any host window or OS-level border that fights vibrancy.

    Do NOT add `overflow: hidden` to panel-shell — the existing `overflow-hidden` on the main
    element in +page.svelte already handles clipping and adding it to CSS would double-apply.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0. `src/app.css` panel-shell rule contains `border: none` and
    `border-bottom-left-radius: 12px` and `border-bottom-right-radius: 12px`.
  </done>
</task>

<task type="auto">
  <name>Task 2: DetailPane header restructure + input hardening</name>
  <files>
    src/lib/components/DetailPane.svelte
  </files>
  <read_first>
    - src/lib/components/DetailPane.svelte lines 155-244 (current header, pin checkbox, tag input, notes textarea)
    - .planning/phases/06.2-tray-ux-polish/06.2-PATTERNS.md section "DetailPane.svelte" (target header pattern, tag input hardening, notes textarea hardening)
    - src/lib/types.ts (verify alias: string | null is now on RepoDto from Plan 04)
  </read_first>
  <action>
    Header restructure (lines 155-175 of DetailPane.svelte):

    Replace the existing header block (which has a separate `<label>` with checkbox for pin) with
    a single `<div class="flex items-center gap-2">` containing:
    1. Back button (keep existing classes and onclick={onClose}, content ←).
    2. `<h2 class="min-w-0 flex-1 truncate text-lg font-semibold">{repo.alias ?? repo.name}</h2>`
    3. Pin toggle button: `<button type="button" class="shrink-0 text-lg leading-none"
       aria-label={repo.pinned ? "Unpin" : "Pin"} aria-pressed={repo.pinned}
       onclick={() => void handlePinChange()}>` with content `{repo.pinned ? "📌" : "📍"}`.

    Remove the old `<label class="flex cursor-pointer items-center gap-2 text-sm">` block entirely.

    Tag input hardening (line ~211 in DetailPane.svelte):
    Add to the existing `<input type="text" bind:this={tagInputEl} ...>` element:
    `autocomplete="off"` `autocapitalize="off"` `autocorrect="off"` `spellcheck="false"`
    Preserve all existing attributes: `bind:value`, `placeholder`, `class`, `onkeydown`, `onblur`.

    Notes textarea hardening (line ~233 in DetailPane.svelte):
    Add to the existing `<textarea ...>` element:
    `autocomplete="off"` `autocapitalize="off"` `autocorrect="off"` `spellcheck="false"`
    Preserve all existing attributes: `bind:this`, `rows`, `maxlength`, `bind:value`, `placeholder`,
    `class`, `style`, `onblur`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0. DetailPane.svelte has no `<label>` containing pin checkbox. Pin is a
    `<button>` with `aria-pressed` attribute. Header h2 uses `{repo.alias ?? repo.name}`. Tag input
    and notes textarea both have `autocomplete="off" autocapitalize="off" autocorrect="off"
    spellcheck="false"` attributes. Build exits clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser text input → OS autocorrect | Mitigated by explicit input attributes |
| Svelte component → Tauri invoke | set_pin already validated in commands.rs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-06-01 | Tampering | macOS autocorrect mutates tag values before save | mitigate | `autocorrect="off"` and `autocapitalize="off"` on tag input; `spellcheck="false"` on both inputs |
| T-06.2-06-02 | Tampering | macOS autocorrect mutates notes content | mitigate | Same four attributes applied to notes textarea |
| T-06.2-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
npm run build 2>&1 | grep -E "error"
```
Zero error lines. CSS and Svelte build clean.
</verification>

<success_criteria>
- `src/app.css` panel-shell rule: `border: none`, `border-bottom-left-radius: 12px`, `border-bottom-right-radius: 12px`
- DetailPane.svelte header: back button + `{repo.alias ?? repo.name}` h2 + 📌/📍 pin button, all on same flex row
- No `<input type="checkbox">` for pin in DetailPane.svelte
- Tag input has `autocomplete="off" autocapitalize="off" autocorrect="off" spellcheck="false"`
- Notes textarea has `autocomplete="off" autocapitalize="off" autocorrect="off" spellcheck="false"`
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/06.2-tray-ux-polish/06.2-06-SUMMARY.md` when done.
</output>

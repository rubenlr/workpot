---
phase: 06-cli-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/workpot-cli/src/list_display.rs
  - crates/workpot-cli/src/main.rs
  - crates/workpot-cli/tests/cli_smoke.rs
autonomous: true
requirements:
  - CLI-01
  - CLI-03

must_haves:
  truths:
    - "workpot list prints repos in flat_tray_ordered order with emoji prefix per tier"
    - "Each row: icon parent_dir name branch tags (D-01..D-03); parent_dir home-shortened"
  artifacts:
    - path: crates/workpot-cli/src/list_display.rs
      provides: format_list_row, priority_icon
      contains: "format_list_row"
    - path: crates/workpot-cli/src/main.rs
      provides: top-level List variant
      contains: "List"
  key_links:
    - from: crates/workpot-cli/src/main.rs
      to: workpot-core repo_priority
      via: flat_tray_ordered_repos
      pattern: "flat_tray_ordered"
---

<objective>
Ship `workpot list` — priority-ordered, emoji-prefixed rows matching tray default view.

Purpose: CLI-01 + first ROADMAP success criterion.
Output: `list_display.rs`, `Commands::List`, cli_smoke coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cli-parity/06-CONTEXT.md
@crates/workpot-cli/src/main.rs
@crates/workpot-cli/src/git_display.rs
@crates/workpot-core/src/services/repo_priority.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: list_display formatter</name>
  <files>crates/workpot-cli/src/list_display.rs, crates/workpot-cli/src/main.rs</files>
  <action>
    1. Add `mod list_display;` in main.rs.

    2. `list_display.rs`:
       - `priority_icon(repo: &RepoRecord, section: PrioritySection) -> &'static str` — 📌 pinned, 🟡 dirty, 🔥 recent, ⬜ rest (per 06-CONTEXT D-02; pass section from classifier OR infer: if pinned → 📌; else if is_dirty==Some(true) → 🟡; else if in recent slice → 🔥; else ⬜)
       - `shorten_parent_dir(path: &Path) -> String` — replace `$HOME` prefix with `~` for parent of repo name (parent_dir field on RepoRecord if set, else parent of path)
       - `format_list_row(repo: &RepoRecord, icon: &str) -> String` — `[icon] [parent] [name] [branch] [tags joined by space]`; branch `—` if None; omit extra columns from old `repo list`

    3. Helper `classify_repo_for_icon(repo, sectioned: &amp;SectionedRepos) -> &amp;'static str` — determine which section repo came from when iterating flat list (track during flat concat in caller, or pass section tag alongside repo in flat iterator).

    Recommended: `flat_tray_ordered_with_icons(config) -> Vec<(RepoRecord, &'static str)>` in list_display that calls core `section_sort` + assigns icon per section membership.
  </action>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && cargo test -p workpot-cli list_display</automated>
  </verify>
  <done>Unit tests for shorten_parent_dir and format_list_row snapshot strings.</done>
</task>

<task type="auto">
  <name>Task 2: workpot list command</name>
  <files>crates/workpot-cli/src/main.rs, crates/workpot-cli/tests/cli_smoke.rs</files>
  <action>
    1. Add top-level `List` variant to `Commands` enum (NOT under `Repo` — D-01).

    2. Handler:
       - `AppContext::open()`
       - `let now = chrono or std time — use same epoch seconds as TS (unix now)`
       - `let ordered = flat_tray_ordered_repos(&ctx.list_repos()?, ctx.config(), now)`
       - print each `format_list_row` to stdout (one per line)

    3. Keep existing `workpot repo list` unchanged (legacy format) — do not break cli_smoke repo list tests.

    4. cli_smoke: register temp repo, `workpot list` exits 0, stdout contains repo name and 📌 or ⬜ icon.
  </action>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && cargo test -p workpot-cli</automated>
  </verify>
  <done>`workpot list` works on empty and non-empty index.</done>
</task>

</tasks>

<verification>
1. `cargo test -p workpot-cli` passes
2. Manual: `workpot list` order matches tray panel with empty filter
</verification>

<success_criteria>
- ROADMAP SC #1 satisfied for list command
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-parity/06-03-SUMMARY.md`
</output>

---
phase: 06-cli-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/workpot-core/src/services/repo_priority.rs
  - crates/workpot-core/src/services/mod.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/repo_priority_test.rs
autonomous: true
requirements:
  - CLI-01
  - CLI-03

must_haves:
  truths:
    - "Rust flat tray order matches tray default: pinned (pin_order) then dirty then recent (window + min pad) then rest"
    - "Section classification uses max_recent_days and min_recent_count from Config"
  artifacts:
    - path: crates/workpot-core/src/services/repo_priority.rs
      provides: section_sort + flat_tray_ordered_repos
      contains: "flat_tray_ordered"
  key_links:
    - from: crates/workpot-core/src/services/repo_priority.rs
      to: crates/workpot-core/src/domain/repo.rs
      via: RepoRecord + Config fields
      pattern: "RepoRecord"
---

## Phase Goal

**As a** power user who lives in the terminal, **I want to** list, search, and open repos with the same order and data as the tray, **so that** I never need the menu bar to switch projects.

<objective>
Port tray four-tier ordering (`sectionSort` + flat list) into `workpot-core` so CLI and tray share one priority model.

Purpose: CLI-03 requires `workpot list` order to match tray default view (no `#` tag filter).
Output: `repo_priority.rs` + tests mirroring `src/lib/sort.test.ts` tier rules.

**CLI-03 boundary:** Prove ordering equivalence via Rust tests ported from `sort.test.ts`. Tray migration to call `workpot-core` is **out of scope** — tray keeps TypeScript `sort.ts` until follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cli-parity/06-CONTEXT.md
@.planning/phases/05-tags-prioritization/05-CONTEXT.md
@src/lib/sort.ts
@src/lib/sort.test.ts
@crates/workpot-core/src/domain/config.rs
@crates/workpot-core/src/domain/repo.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: repo_priority module</name>
  <read_first>
    - src/lib/sort.ts (sectionSort, traySort — source of truth for behavior)
    - src/lib/sort.test.ts (acceptance cases to port)
    - crates/workpot-core/src/domain/repo.rs
  </read_first>
  <files>crates/workpot-core/src/services/repo_priority.rs, crates/workpot-core/src/services/mod.rs, crates/workpot-core/src/lib.rs</files>
  <action>
    1. Add `services/repo_priority.rs` with:
       - `SectionedRepos { pinned, dirty, recent, rest }` (Vec<RepoRecord> each)
       - `section_sort(repos, config: &Config, now_seconds: i64) -> SectionedRepos` — port logic from `sectionSort` exactly (pin_order asc within pinned; dirty tier; recent window `max_recent_days * 86400`; pad to `min_recent_count`; rest alphabetical)
       - `flat_tray_ordered(sectioned: &SectionedRepos) -> Vec<RepoRecord>` — concat pinned, dirty, recent, rest (matches `flatSectioned` in trayList.ts)
       - `flat_tray_ordered_repos(repos: &[RepoRecord], config: &Config, now_seconds: i64) -> Vec<RepoRecord>` — convenience wrapper

    2. Export from `services/mod.rs` and re-export priority types/functions from `lib.rs` if other crates need them.

    3. Honor **D-19..D-22** (05-CONTEXT): dirty wins over recent; `last_opened_at IS NULL` → Rest; `max_recent_days` window + `min_recent_count` padding floor (repos outside window may pad Recent).

    4. Do NOT implement fuzzy or tag `#` filtering here — tray default list has no active tag tokens (D-07).
  </action>
  <acceptance_criteria>
    - `flat_tray_ordered_repos` output order matches `sectionSort` + `flatSectioned` from `src/lib/sort.ts` for the same fixture set
    - Pinned repos sort by `pin_order` ascending with `None` treated as 999
    - `min_recent_count` padding pulls additional repos when in-window recent count is below floor
    - `cargo test -p workpot-core repo_priority` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && cargo test -p workpot-core repo_priority -- --nocapture</automated>
  </verify>
  <done>Core ordering matches TS sort.test.ts tier assignment cases.</done>
</task>

<task type="auto">
  <name>Task 2: repo_priority_test.rs</name>
  <read_first>
    - src/lib/sort.test.ts (golden tier/order cases to port)
    - crates/workpot-core/tests/org_test.rs (RepoRecord fixture builders)
    - .planning/phases/06-cli-parity/06-VALIDATION.md (golden vector contract)
  </read_first>
  <files>crates/workpot-core/tests/repo_priority_test.rs</files>
  <action>
    Port the substantive cases from `sort.test.ts`:
    - pinned before non-pinned in flat output
    - dirty before clean non-pinned
    - recent window + min_recent_count padding
    - rest repos alphabetical
    - pin_order ordering within pinned

    Use `RepoRecord` test fixtures (see org_test.rs / git_display tests for builder pattern).
  </action>
  <acceptance_criteria>
    - At least 8 active tests covering pinned, dirty, recent window, min_recent padding, rest alphabetical, pin_order
    - No `#[ignore]` on repo_priority tests
    - Tests document D-20 (dirty beats recent) and D-22 (padding floor) explicitly in at least one case each
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rubenlr/c/workpot && cargo test -p workpot-core repo_priority</automated>
  </verify>
  <done>At least 8 active tests; 0 ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-memory sort only | No user input crosses this module; repos come from catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | repo_priority | accept | Pure ordering; no external I/O |
</threat_model>

<verification>
1. `cargo test -p workpot-core repo_priority` passes
2. `cargo clippy -p workpot-core -- -D warnings` passes
</verification>

<success_criteria>
- `workpot list` can call `flat_tray_ordered_repos` without reimplementing section logic in CLI
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-parity/06-01-SUMMARY.md`
</output>

---
phase: 04-tray-finder-mvp
plan: 01
type: execute
wave: 1
depends_on: []
executor_commit_rules: |
  Task 2 MUST land workspace membership, Cargo.lock, package-lock.json, ci.yml Linux-safe edits, and DATA-02 tray scan in ONE commit.
  Do NOT push or open a PR between partial Task 2 work and ci.yml fixes — ubuntu jobs use `cargo * --workspace` until ci.yml is updated.
  Task 3 is UI-only; safe to commit separately after Task 2 is complete on main/PR branch.
files_modified:
  - Cargo.toml
  - Cargo.lock
  - package-lock.json
  - scripts/check-no-network-deps.sh
  - crates/workpot-core/src/domain/config.rs
  - crates/workpot-core/src/domain/repo.rs
  - crates/workpot-core/src/services/catalog.rs
  - crates/workpot-core/src/infra/migrations/004_tray.sql
  - crates/workpot-core/src/infra/migrations.rs
  - crates/workpot-core/src/lib.rs
  - crates/workpot-core/tests/tray_migration_test.rs
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/build.rs
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/tray.rs
  - package.json
  - vite.config.ts
  - svelte.config.js
  - tailwind.config.js
  - src/app.html
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - src/lib/types.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Tray app builds as workspace member and shows menu bar icon (UI-01)"
    - "Clicking tray icon toggles a frameless panel window (D-09)"
    - "Panel lists indexed repos with name, branch, dirty dot, parent directory (D-10, UI-02)"
    - "Migration 004 adds last_opened_at column to repos (D-25)"
    - "config.toml supports launch_cmd and max_visible_rows with documented defaults (D-33, D-12)"
    - "list_repos Tauri command returns cached SQLite data via workpot-core (D-26 initial load)"
    - "Tauri app at repo root src-tauri/ + src/ as Cargo workspace member (D-05, D-06)"
    - "Svelte invoke() loads repos — IPC command pattern (D-29)"
  artifacts:
    - path: src-tauri/src/lib.rs
      provides: Tauri app entry with AppContext state
      contains: "AppContext::open"
    - path: src-tauri/src/commands.rs
      provides: list_repos IPC command
      contains: "#[tauri::command]"
    - path: crates/workpot-core/src/infra/migrations/004_tray.sql
      provides: last_opened_at schema
      contains: "last_opened_at"
    - path: src/routes/+page.svelte
      provides: Minimal repo list UI
      contains: "invoke"
  key_links:
    - from: src-tauri/src/commands.rs
      to: workpot-core AppContext::list_repos
      via: path dependency + Mutex lock
      pattern: "list_repos"
    - from: src/routes/+page.svelte
      to: src-tauri/src/commands.rs
      via: invoke('list_repos')
      pattern: "list_repos"
---

## Phase Goal (slice 1)

**As a** macOS engineer with many repos, **I want to** click a menu bar icon and see my indexed repos with git state, **so that** I can glance before switching context.

<objective>
Ship the thinnest end-to-end tray slice: Tauri 2 scaffold at repo root, core schema/config extensions, tray icon that toggles a panel, and a Svelte list showing cached repo metadata from SQLite.

Purpose: First Tauri crate in workspace; proves shared DB with CLI and delivers UI-01 + partial UI-02.
Output: Runnable tray binary, migration 004, config fields, minimal list UI.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path traversal via IPC list | low | list_repos reads DB only; no user-supplied paths |
| Shell injection | low | No launch in this plan |
| SQLite migration failure on upgrade | medium | Integration test applies 004 on temp DB |
</threat_model>

<execution_context>
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-01-PLAN.md
</execution_context>

<context>
@/Users/rubenlr/c/workpot/.planning/ROADMAP.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-CONTEXT.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-RESEARCH.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-PATTERNS.md
@/Users/rubenlr/c/workpot/crates/workpot-core/src/lib.rs
@/Users/rubenlr/c/workpot/crates/workpot-core/src/domain/config.rs
@/Users/rubenlr/c/workpot/crates/workpot-core/src/services/catalog.rs
@/Users/rubenlr/c/workpot/crates/workpot-core/src/infra/migrations.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core migration 004, config fields, RepoRecord extension</name>

  <read_first>
    - crates/workpot-core/src/infra/migrations/003_git_state.sql
    - crates/workpot-core/src/infra/migrations.rs
    - crates/workpot-core/src/domain/repo.rs
    - crates/workpot-core/src/domain/config.rs
    - crates/workpot-core/src/services/catalog.rs (list_repos SELECT)
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-25, D-33, D-12)
  </read_first>

  <action>
    1. Create `crates/workpot-core/src/infra/migrations/004_tray.sql`:
       `ALTER TABLE repos ADD COLUMN last_opened_at INTEGER NULL;`

    2. Register migration in `migrations.rs` after 003.

    3. Add to `RepoRecord`: `pub last_opened_at: Option<i64>`.

    4. Extend `catalog::list_repos` SELECT to include `last_opened_at` column; map in query_map.

    5. Add `catalog::touch_last_opened_at(conn, path) -> Result<()>` — UPDATE repos SET last_opened_at=? WHERE path=? using canonical path key (same pattern as git_state persist).

    6. Extend `Config` struct with:
       - `launch_cmd: String` — default `"cursor --new-window {path}"` via serde default fn
       - `max_visible_rows: u32` — default `15`
       Add both to `Config::validate()` if needed (max_visible_rows >= 1 and <= 100).

    7. Add `AppContext::touch_last_opened_at(&self, path: &Path) -> Result<()>` delegating to catalog.

    8. Create `crates/workpot-core/tests/tray_migration_test.rs`:
       - open temp DB, run migrations, INSERT repo, verify last_opened_at column exists and defaults NULL
       - verify default config deserializes launch_cmd and max_visible_rows when omitted from TOML
  </action>

  <acceptance_criteria>
    - `004_tray.sql` contains `last_opened_at INTEGER NULL`
    - `migrations.rs` includes migration id for 004
    - `RepoRecord` struct contains field `last_opened_at: Option<i64>`
    - `Config` default launch_cmd equals `cursor --new-window {path}`
    - `Config` default max_visible_rows equals 15
    - `cargo test -p workpot-core tray_migration` exits 0
  </acceptance_criteria>

  <verify>cargo test -p workpot-core tray_migration</verify>
</task>

<task type="auto">
  <name>Task 2: Tauri 2 workspace scaffold + list_repos command</name>

  <read_first>
    - Cargo.toml (workspace members)
    - crates/workpot-cli/Cargo.toml (path dep pattern)
    - .planning/phases/04-tray-finder-mvp/04-RESEARCH.md (Pattern 1)
    - .planning/phases/04-tray-finder-mvp/04-PATTERNS.md
    - https://v2.tauri.app/learn/system-tray/ (tray-icon feature)
  </read_first>

  <action>
    1. Scaffold Tauri 2 app at repo root:
       - `src-tauri/` with `Cargo.toml` package name `workpot-tray`
       - Dependencies: `tauri = { version = "2", features = ["tray-icon"] }`, `workpot-core`, `serde`, `serde_json`
       - Add `"src-tauri"` to root `Cargo.toml` `members` AND set Linux-safe defaults:
         ```toml
         members = ["crates/workpot-core", "crates/workpot-cli", "src-tauri"]
         default-members = ["crates/workpot-core", "crates/workpot-cli"]
         ```
         (`cargo test --workspace` on Ubuntu builds only default-members; tray is opt-in via `-p workpot-tray`)

    2. Frontend scaffold (D-01, D-02, D-03):
       - `package.json` with SvelteKit + Vite + TypeScript + Tailwind
       - `package.json` `engines.node`: `">=20"` (CI pins Node 22 LTS via `setup-node`)
       - SvelteKit SPA mode: `ssr = false` in `+layout.ts` or equivalent
       - `src/routes/+page.svelte`, `src/lib/types.ts` with `RepoDto` matching IPC JSON
       - Run `npm install` at repo root; commit `package-lock.json` (required for `npm ci` on macOS CI in same task)

    3. **Linux-safe CI in the same commit as workspace `members` change** (do not defer to Task 3):
       Update `.github/workflows/ci.yml` before pushing Task 2:
       - **Root cause:** `cargo * --workspace` builds every `members` entry including `src-tauri`; Tauri tray deps fail on `ubuntu-latest`. `default-members` only affects bare `cargo build/test` without `--workspace`.
       - **fmt job (ubuntu-latest):** replace `cargo clippy --workspace` with `cargo clippy -p workpot-core -p workpot-cli --all-targets -- -D warnings`
       - **test (ubuntu-latest):** replace `cargo test --workspace` with `cargo test -p workpot-core -p workpot-cli --all-targets`
       - **coverage (ubuntu-latest):** replace `cargo build --offline --workspace` and `cargo llvm-cov test --offline --workspace` with the same `-p workpot-core -p workpot-cli` package set (or bare `cargo build --offline` / `cargo llvm-cov test --offline` with no `--workspace`, which uses `default-members`)
       - **Node toolchain (macos-latest `test` + `release-build`):** before any `npm` step, add `actions/setup-node@v4` with `node-version: '22'` and `cache: 'npm'`
       - **test (macos-latest):** after existing offline workspace test, add `cargo build -p workpot-tray`, then `npm ci && npm run build` at repo root (after `setup-node`)
       - **release-build job:** add `setup-node` (same pin), then `cargo build -p workpot-tray` and `npm ci && npm run build` after CLI release build
       - **DATA-02:** extend `scripts/check-no-network-deps.sh` with `check_crate workpot-tray` after the crate exists; update success message to mention all three crates

    4. `tauri.conf.json`:
       - Product name Workpot
       - Window label `panel`, initial size ~400x500, `decorations: false`, `visible: false`, `skipTaskbar: true`
       - `build.beforeDevCommand` / `beforeBuildCommand` for npm
       - macOS-only target (bundle identifier com.workpot.app or similar)

    5. `src-tauri/src/lib.rs`:
       - `manage(Arc<Mutex<AppContext>>)` initialized with `AppContext::open()?`
       - Register `commands::list_repos`

    6. `src-tauri/src/commands.rs`:
       - Define `RepoDto` with: path, name, branch, is_dirty, parent_dir (computed: parent of path as shortened home prefix e.g. `~/c/`), last_opened_at, git_state_error
       - `#[tauri::command] fn list_repos(state: State<Arc<Mutex<AppContext>>>) -> Result<Vec<RepoDto>, String>`

    7. `capabilities/default.json`: allow invoke for list_repos

    8. After first `cargo build -p workpot-tray`, commit updated root `Cargo.lock` in this task (same commit as workspace + ci.yml).

    9. Unit test in `src-tauri/src/commands.rs` or `tests/` using `AppContext::open_with_paths` + tempdir verifying list_repos command logic (extract pure fn if needed for testability)
  </action>

  <acceptance_criteria>
    - Root `Cargo.toml` `members` includes `src-tauri`
    - Root `Cargo.toml` `default-members` is exactly `["crates/workpot-core", "crates/workpot-cli"]` (no `src-tauri`)
    - `src-tauri/Cargo.toml` contains `workpot-core = { path = "../crates/workpot-core" }`
    - `tauri.conf.json` window decorations is false
    - `commands.rs` contains `fn list_repos` with `#[tauri::command]`
    - `RepoDto` includes fields `branch`, `is_dirty`, `parent_dir`
    - `package-lock.json` exists at repo root and is committed (generated by `npm install` after scaffold)
    - `Cargo.lock` includes `workpot-tray` resolution and is committed
    - `.github/workflows/ci.yml` ubuntu `test` step runs `cargo test -p workpot-core -p workpot-cli` (not bare `--workspace` without default-members guard)
    - `.github/workflows/ci.yml` ubuntu `fmt` (clippy) job uses `-p workpot-core -p workpot-cli` (not `--workspace`)
    - `.github/workflows/ci.yml` ubuntu `coverage` job does not use `--workspace` flag (uses `-p workpot-core -p workpot-cli` or bare default-members)
    - `.github/workflows/ci.yml` macos `test` and `release-build` each include `actions/setup-node@v4` with `node-version: '22'` before the first `npm` command
    - `.github/workflows/ci.yml` macos jobs run `npm ci` (lockfile present)
    - `scripts/check-no-network-deps.sh` calls `check_crate workpot-tray`
    - `cargo build -p workpot-tray` exits 0
  </acceptance_criteria>

  <verify>cargo build -p workpot-tray</verify>
</task>

<task type="auto">
  <name>Task 3: Tray icon toggle + minimal Svelte repo list</name>

  <read_first>
    - src-tauri/src/lib.rs
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-09, D-10, D-04)
    - .planning/phases/04-tray-finder-mvp/04-RESEARCH.md (Pattern 3)
    - src/routes/+page.svelte (after Task 2)
  </read_first>

  <action>
    1. Create `src-tauri/src/tray.rs`:
       - `TrayIconBuilder::with_id("main")` with app icon
       - `on_tray_icon_event`: Left click toggles `panel` window show/hide; on show call `set_focus()`
       - Prevent app exit when window closes — hide instead (tray app pattern)

    2. Wire tray setup in `lib.rs` `.setup()`.

    3. `src/routes/+page.svelte`:
       - On mount: `invoke<RepoDto[]>('list_repos')` into reactive `repos`
       - Render list: each row shows repo name (primary), colored dot per D-10 (green = clean, amber or red = dirty, gray = unknown/null), branch text, parent_dir secondary line
       - Use Tailwind + `prefers-color-scheme` for dark/light (D-04) — no toggle UI
       - Basic scroll container (full max height for now; dynamic cap in plan 02)

    4. Add tray icon assets under `src-tauri/icons/` (use Tauri default icons if none provided).

    5. **Do not edit `ci.yml` in this task** — all CI/workspace/lockfile changes belong in Task 2 (single commit).
  </action>

  <acceptance_criteria>
    - `tray.rs` contains `TrayIconBuilder` and `on_tray_icon_event`
    - `+page.svelte` calls `invoke('list_repos')`
    - Row template renders repo name and branch
    - Dirty dot uses green for clean and amber or red for dirty (not green for dirty)
    - `cargo build -p workpot-tray` exits 0 on macOS
    - Manual: `cargo run -p workpot-tray` shows tray icon on macOS
  </acceptance_criteria>

  <verify>cargo build -p workpot-tray</verify>
</task>

</tasks>

<verification>
1. Linux CI path: `cargo test -p workpot-core -p workpot-cli --all-targets` passes (simulates ubuntu-latest; requires Task 2 ci.yml landed atomically with workspace members)
2. macOS: `cargo build -p workpot-tray` and `npm ci && npm run build` succeed (committed `package-lock.json`; CI uses setup-node@22)
3. `bash scripts/check-no-network-deps.sh` includes `workpot-tray`
4. Manual macOS: tray icon visible; click opens panel with repo list from existing index
</verification>

## PLAN COMPLETE
</output>

---
phase: 04-tray-finder-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/src/commands.rs
  - src-tauri/src/tray.rs
  - src-tauri/src/lib.rs
  - src/routes/+page.svelte
  - src/lib/fuzzy.ts
  - src/lib/sort.ts
  - src/lib/fuzzy.test.ts
  - src/lib/sort.test.ts
  - src/lib/types.ts
  - src/app.css
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - UI-02
  - UI-03
  - SRCH-01
  - SRCH-02
  - SRCH-03
requirement_traceability:
  SRCH-01: partial
  SRCH-01_note: "Phase 4 satisfies name, path, branch only. Tags and notes have no DB columns until Phase 5 (ORG-01, ORG-04); full SRCH-01 text completes in Phase 5 tray filter extension."
  SRCH-02: full
  SRCH-03: full
must_haves:
  truths:
    - "Filter input auto-focuses when panel opens (D-13)"
    - "Typing filters repo list in real time without IPC per keystroke (D-30, SRCH-02)"
    - "Fuzzy match scores name, path, and branch metadata only — SRCH-01 partial for Phase 4; tags/notes deferred Phase 5 (SRCH-03)"
    - "Empty filter shows inline 'No repos match' message (D-14)"
    - "Repos sorted dirty-first then last_opened_at descending (D-22)"
    - "Panel is frameless with rounded corners, blur, shadow; closes on focus loss (D-07, D-08)"
    - "Panel height grows dynamically up to max_visible_rows then scrolls (D-12)"
    - "Selected row has full-width accent highlight (D-11)"
    - "Arrow keys and Tab navigate selection; Esc closes panel (D-16, D-17, D-20)"
  artifacts:
    - path: src/lib/fuzzy.ts
      provides: Client-side fuzzy scorer
      contains: "score"
    - path: src/lib/sort.ts
      provides: Tray sort order
      contains: "last_opened_at"
    - path: src/routes/+page.svelte
      provides: Filter input + keyboard handlers
      contains: "filterQuery"
  key_links:
    - from: src/routes/+page.svelte
      to: src/lib/fuzzy.ts
      via: import and reactive filter
      pattern: "fuzzyMatch"
    - from: src/routes/+page.svelte
      to: src/lib/sort.ts
      via: sort before render
      pattern: "traySort"
---

## Phase Goal (slice 2)

**As a** user opening the tray, **I want to** type immediately to filter and arrow through repos, **so that** I can find the right project without the mouse (Raycast-like flow).

<objective>
Add panel chrome, client-side fuzzy filter, tray sort order, keyboard navigation, and selection UI on top of plan 01's repo list.

Purpose: Delivers UI-03 and SRCH-02/03 fully; **SRCH-01 partial** (name/path/branch only — tags/notes in Phase 5).
Output: Filter bar, sorted/filtered list, keyboard nav, polished panel window.

**SRCH-01 traceability:** REQUIREMENTS.md lists tags/notes; no schema until Phase 5. This plan documents deliberate partial satisfaction; verify-phase should not fail on missing tag/note fuzzy until Phase 5 extends `fuzzy.ts` fields.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| ReDoS via fuzzy on huge lists | low | Cap filter input length 256 chars; list already capped by max_repos config |
| XSS via repo names in Svelte | low | Svelte auto-escapes text bindings; do not use {@html} for repo fields |
</threat_model>

<execution_context>
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-02-PLAN.md
</execution_context>

<context>
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-CONTEXT.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-RESEARCH.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-01-PLAN.md
@/Users/rubenlr/c/workpot/src/routes/+page.svelte
@/Users/rubenlr/c/workpot/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel chrome — blur, rounded, focus-loss close, dynamic height</name>

  <read_first>
    - src-tauri/tauri.conf.json
    - src-tauri/src/tray.rs
    - src/routes/+page.svelte
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-07, D-08, D-12)
    - .planning/phases/04-tray-finder-mvp/04-RESEARCH.md (macOS vibrancy)
  </read_first>

  <action>
    1. Add `window-vibrancy` crate to `src-tauri/Cargo.toml` (macOS only cfg) — apply `apply_vibrancy(window, NSVisualEffectMaterial::HudWindow, ...)` on panel show.

    2. In `tray.rs` or window setup: register `on_window_event` for label `panel` — on `WindowEvent::Focused(false)` call `window.hide()` (D-08).

    3. Position panel below tray icon on show: use click `rect` from `TrayIconEvent::Click` to set window position (logical x/y below icon).

    4. `src/app.css` + Tailwind: panel container `rounded-xl shadow-2xl overflow-hidden`; inner list area scrollable.

    5. Add `#[tauri::command] fn get_tray_config(state: State<Arc<Mutex<AppContext>>>) -> Result<TrayConfigDto, String>` in `commands.rs` returning `{ max_visible_rows: u32 }` from `AppContext` config (do not overload `list_repos` / `RepoDto`). Register in `lib.rs` and capabilities. On panel show, `invoke('get_tray_config')` once; compute panel max-height as `max_visible_rows * ROW_HEIGHT_PX + FILTER_BAR_HEIGHT`.

    6. Toggle behavior (D-09): second tray click while visible hides panel (verify from plan 01, fix if missing).
  </action>

  <acceptance_criteria>
    - Panel window has decorations false and visible rounded container in CSS
    - `Focused(false)` handler hides panel window
    - List area has `overflow-y: auto` and max-height derived from max_visible_rows
    - `commands.rs` contains `fn get_tray_config` with `#[tauri::command]`
    - `+page.svelte` calls `invoke('get_tray_config')` (not config fields on `list_repos`)
    - `cargo build -p workpot-tray` exits 0
  </acceptance_criteria>

  <verify>cargo build -p workpot-tray</verify>
</task>

<task type="auto">
  <name>Task 2: Fuzzy filter, sort order, empty state</name>

  <read_first>
    - src/routes/+page.svelte
    - src/lib/types.ts
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-22, D-30, D-14, SRCH-01..03)
    - .planning/REQUIREMENTS.md (SRCH-01 scope)
  </read_first>

  <action>
    1. Create `src/lib/sort.ts`:
       - `export function traySort(a: RepoDto, b: RepoDto): number`
       - Dirty repos first: `is_dirty === true` before false before null
       - Within same dirty tier: `last_opened_at` descending (null last)
       - Tie-break: `name` localeCompare

    2. Create `src/lib/fuzzy.ts`:
       - `export function fuzzyMatch(query: string, repo: RepoDto): boolean`
       - `export function fuzzyScore(query: string, repo: RepoDto): number` for ranking
       - Match fields: `name`, `path`, `branch` (string fields only — SRCH-03 metadata; tags/notes explicitly excluded until Phase 5)
       - Algorithm: case-insensitive substring match minimum; bonus for consecutive char matches at start of name (simple fuzzy, no heavy deps required)

    3. Add Vitest devDependency + `src/lib/fuzzy.test.ts`, `src/lib/sort.test.ts`:
       - fuzzy: query "wp" matches "workpot"; "main" matches branch
       - sort: dirty repo ranks above clean; higher last_opened_at first

    4. Update `+page.svelte`:
       - Filter input at top bound to `filterQuery`
       - `$:` derived `displayRepos = repos.filter(r => fuzzyMatch(filterQuery, r)).sort(traySort)`
       - When `filterQuery` non-empty and `displayRepos.length === 0`: show "No repos match" (D-14)
       - Auto-focus filter input on panel open: listen for `panel-opened` event from Rust or call `focus()` in `onMount` + when window receives focus event
  </action>

  <acceptance_criteria>
    - `sort.ts` exports function `traySort`
    - `fuzzy.ts` exports function `fuzzyMatch`
    - `fuzzy.test.ts` contains at least 3 test cases and passes via `npm test`
    - `+page.svelte` contains `filterQuery` and "No repos match"
    - Filtering does not call `invoke` — only `Array.filter` on loaded repos
  </acceptance_criteria>

  <verify>npm test -- fuzzy sort</verify>
</task>

<task type="auto">
  <name>Task 3: Keyboard navigation and row selection</name>

  <read_first>
    - src/routes/+page.svelte
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-11, D-16, D-17, D-20)
  </read_first>

  <action>
    1. Add `selectedIndex` state (number, default 0).

    2. Reset `selectedIndex` to 0 when `filterQuery` or `displayRepos` changes; clamp to length-1.

    3. Global keydown handler on panel (when focused):
       - ArrowDown: increment selectedIndex
       - ArrowUp: decrement
       - Tab: increment selectedIndex (wrap)
       - Escape: invoke hide panel or `window.hide()` via `@tauri-apps/api/window` getCurrentWindow().hide()
       - Enter: defer open action to plan 04 — for now log or no-op stub OK if plan 04 adds handler; prefer wiring to placeholder `openSelected()` function

    4. Apply selected row class: full-width `bg-accent` or equivalent (D-11).

    5. Scroll selected row into view using `element.scrollIntoView({ block: 'nearest' })`.

    6. Ensure filter input auto-focus on panel open (D-13) without stealing arrow keys when input focused — arrows in input move cursor; when input empty and ArrowDown pressed, move selection to list (standard spotlight behavior).
  </action>

  <acceptance_criteria>
    - `+page.svelte` contains `selectedIndex` state
    - ArrowDown/ArrowUp change selectedIndex when list focused
    - Escape hides panel via Tauri window API
    - Selected row element has distinct background class from unselected rows
    - `npm run build` exits 0
  </acceptance_criteria>

  <verify>npm run build</verify>
</task>

<task type="auto">
  <name>Task 4: Frontend CI — Vitest on macOS tray job</name>

  <read_first>
    - .github/workflows/ci.yml (macos test + release-build jobs from plan 01)
    - package.json (test script from Task 2)
  </read_first>

  <action>
    1. Ensure `package.json` has `"test": "vitest run"` (or equivalent).

    2. In `.github/workflows/ci.yml` **macos-latest** `test` job: after `cargo test --offline --workspace`, add frontend steps **only after** `actions/setup-node@v4` (`node-version: '22'`, `cache: 'npm'` — from 04-01 Task 3 if not already present):
       - `npm ci`
       - `npm test`
       - `npm run build`

    3. Do not add npm steps to ubuntu-latest jobs (no tray frontend required on Linux CI).
  </action>

  <acceptance_criteria>
    - macos `test` job contains `actions/setup-node@v4` before the first `npm` step
    - macos `test` job contains step `npm test`
    - macos `test` job contains step `npm run build`
    - `npm test` exits 0 locally after fuzzy/sort tests land
  </acceptance_criteria>

  <verify>npm test</verify>
</task>

</tasks>

<verification>
1. `npm test` passes fuzzy and sort tests
2. `cargo build -p workpot-tray` passes
3. Manual: type filters list live; arrows move highlight; Esc closes; empty query shows message when no matches
</verification>

## PLAN COMPLETE

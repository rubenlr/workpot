---
phase: 04-tray-finder-mvp
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - crates/workpot-core/src/domain/config.rs
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - src-tauri/src/commands.rs
  - src-tauri/src/launch.rs
  - src-tauri/src/tray.rs
  - src-tauri/src/lib.rs
  - src/routes/+page.svelte
  - src/lib/types.ts
autonomous: true
requirements:
  - UI-04
  - LAUNCH-01
must_haves:
  truths:
    - "Enter opens selected repo in Cursor via launch_cmd template (UI-04, LAUNCH-01, D-33)"
    - "Default launch_cmd is cursor --new-window {path} (D-33)"
    - "Successful normal open updates last_opened_at and closes panel (D-35, D-25)"
    - "Cmd+Enter and Cmd+Click open without closing panel (D-36)"
    - "Launch failure shows non-modal error banner inside panel (D-34, ROADMAP criterion 5)"
    - "Right-click tray menu: Refresh index, Quit, About/version, Preferences opens config.toml (D-32)"
    - "Repo path validated against indexed repos before launch"
  artifacts:
    - path: src-tauri/src/launch.rs
      provides: launch_cmd parse and execute
      contains: "{path}"
    - path: src-tauri/src/commands.rs
      provides: open_in_cursor command
      contains: "open_in_cursor"
    - path: src/routes/+page.svelte
      provides: Enter handler + error banner
      contains: "errorBanner"
  key_links:
    - from: src/routes/+page.svelte
      to: open_in_cursor command
      via: invoke on Enter
      pattern: "open_in_cursor"
    - from: src-tauri/src/launch.rs
      to: std::process::Command
      via: spawn cursor CLI
      pattern: "Command::new"
---

## Phase Goal (slice 4)

**As a** user who found the right repo, **I want to** press Enter and land in Cursor, **so that** the daily driver loop is complete.

<objective>
Implement Cursor launch via configurable shell command, keyboard/click open actions, last_opened_at persistence, error banner, and tray context menu utilities.

Purpose: Delivers UI-04 and LAUNCH-01; completes Phase 4 ROADMAP success criteria including failed-launch visibility.
Output: launch.rs, open_in_cursor command, context menu, error UX.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Shell injection via launch_cmd | high | Parse with `shell-words` crate into program + args; reject `{path}` replacement containing newlines; validate path is indexed repo |
| Arbitrary command execution | high | Only execute config launch_cmd template; no user-provided command string from frontend |
| Opening wrong path | medium | Lookup repo by path key in SQLite before launch; canonicalize |
</threat_model>

<execution_context>
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-04-PLAN.md
</execution_context>

<context>
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-CONTEXT.md
@/Users/rubenlr/c/workpot/.planning/phases/04-tray-finder-mvp/04-RESEARCH.md
@/Users/rubenlr/c/workpot/.planning/ROADMAP.md (Phase 4 success criteria 4-5)
@/Users/rubenlr/c/workpot/crates/workpot-core/src/domain/config.rs
@/Users/rubenlr/c/workpot/src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: launch_cmd parser and open_in_cursor command</name>

  <read_first>
    - crates/workpot-core/src/domain/config.rs (launch_cmd field from plan 01)
    - crates/workpot-core/src/lib.rs (touch_last_opened_at)
    - crates/workpot-core/src/services/catalog.rs (path lookup)
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-33, D-34)
  </read_first>

  <action>
    1. Create `src-tauri/src/launch.rs`:
       - `pub fn build_command(template: &str, repo_path: &Path) -> Result<(String, Vec<String>), String>`
         Replace `{path}` with absolute path string; split with `shell_words` into program + args
       - `pub fn launch_repo(ctx: &AppContext, path: &str) -> Result<(), String>`
         Verify path exists in repos table; build command from config.launch_cmd; `Command::new(program).args(args).spawn()`; capture spawn error message

    2. Add `shell-words = "1"` to src-tauri/Cargo.toml.

    3. Command:
       ```rust
       #[tauri::command]
       fn open_in_cursor(path: String, background: bool, state: State<Arc<Mutex<AppContext>>>) -> Result<(), String>
       ```
       - On success: `touch_last_opened_at`
       - Return Err with stderr/spawn message on failure (D-34)
       - `background` flag returned to frontend for close-vs-stay decision (frontend handles panel hide)

    4. Unit tests in `launch.rs`:
       - Template `cursor --new-window {path}` + `/tmp/foo` → program `cursor`, args contain `--new-window` and `/tmp/foo`
       - Reject template with unbalanced quotes
  </action>

  <acceptance_criteria>
    - `launch.rs` exports `build_command` and `launch_repo`
    - `open_in_cursor` registered in invoke_handler
    - Successful launch calls touch_last_opened_at (verify in test with temp DB)
    - `cargo test -p workpot-tray launch` exits 0
    - Path not in index returns Err string containing "not found" or equivalent
  </acceptance_criteria>

  <verify>cargo test -p workpot-tray launch</verify>
</task>

<task type="auto">
  <name>Task 2: Enter/Cmd+Enter/click handlers and error banner</name>

  <read_first>
    - src/routes/+page.svelte (selectedIndex from plan 02)
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-18, D-19, D-35, D-36, D-34)
  </read_first>

  <action>
    1. Implement `async function openSelected(background: boolean)`:
       - Get `displayRepos[selectedIndex]`
       - try `await invoke('open_in_cursor', { path: repo.path, background })`
       - on success: if !background → `getCurrentWindow().hide()` (D-35); if background keep open (D-36)
       - on error: set `errorBanner = err message`; do NOT close panel (D-34)

    2. Wire Enter key: if filter input focused and query non-empty, Enter opens selected; if metaKey → background=true (D-19)

    3. Wire double-click or click on row: normal click opens with background=false; cmd+click background=true

    4. Error banner UI: dismissible strip at top of panel (red/warning styling), shows until user dismisses or next open attempt

    5. ROADMAP criterion 5: failed launch must NOT silent no-op — banner text must include command failure reason
  </action>

  <acceptance_criteria>
    - Enter key calls openSelected with background=false
    - Cmd+Enter calls openSelected with background=true
    - Panel hides on successful non-background open
    - Panel stays open on successful background open
    - `errorBanner` renders visible element when open fails
    - `npm run build` exits 0
  </acceptance_criteria>

  <verify>npm run build</verify>
</task>

<task type="auto">
  <name>Task 3: Tray context menu — index, quit, about, preferences</name>

  <read_first>
    - src-tauri/src/tray.rs
    - crates/workpot-core/src/lib.rs (run_index, config_path)
    - .planning/phases/04-tray-finder-mvp/04-CONTEXT.md (D-32)
    - https://v2.tauri.app/learn/system-tray/ (Menu, MenuItem)
  </read_first>

  <action>
    1. Build tray `Menu` with items:
       - `refresh_index` — "Refresh index"
       - `preferences` — "Preferences…"
       - `about` — "About Workpot"
       - separator
       - `quit` — "Quit Workpot"

    2. `on_menu_event` handlers:
       - refresh_index: spawn blocking task calling `AppContext::run_index()`; on complete emit `index-complete` + optional notification; refresh list
       - preferences: open config path in default editor — use `open` crate or `tauri-plugin-opener` with `open::that(config_path)`
       - about: show native dialog or emit event to frontend with version from `workpot_core::version()`
       - quit: `app.exit(0)`

    3. Left-click remains toggle panel (D-09); right-click shows menu (platform default).

    4. Add capabilities for shell/opener if using plugin.

    5. Commands `run_index` and `open_config` if needed from menu Rust-side directly without frontend.
  </action>

  <acceptance_criteria>
    - `tray.rs` Menu contains item id `refresh_index` and `quit`
    - refresh_index invokes run_index on AppContext
    - preferences opens config.toml path (uses config_path from AppContext)
    - about displays version string matching workpot_core::version()
    - `cargo build -p workpot-tray` exits 0
  </acceptance_criteria>

  <verify>cargo build -p workpot-tray</verify>
</task>

</tasks>

<verification>
1. `cargo test -p workpot-tray launch` passes
2. Manual UAT full loop:
   - Tray → filter → Enter → Cursor opens new window → panel closes
   - Cmd+Enter → Cursor opens → panel stays
   - Invalid launch_cmd → error banner visible
   - Right-click → Refresh index / Quit work
3. All Phase 4 ROADMAP success criteria satisfied
</verification>

## PLAN COMPLETE
